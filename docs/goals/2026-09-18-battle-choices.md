# Goal：三國演義 remake 把戰役裡「原版問人」的三處接回來（2026-09-18，第二輪）

以 `wicanr2/softworld_san1_remake` 的 open Issues 為唯一工作權威。承接
`2026-09-15-fidelity-loop.md` 的 §1 基本規則、§3 開 Issue 範本、§4 收尾，
以及同日第一輪 `2026-09-18-player-choices.md` 的做法（一條 Issue 一個 commit、
收據、關閉前回讀），這裡不重抄。repo 的 `CLAUDE.md`／`CONTEXT.md` 全部適用。

## 1. 起點（2026-09-18 盤點）

- open：#98、#99、#100 三條 `agent-ready`（都是同日第一輪普查開出來的）；
  #2 `blocked`。
- `main` 已推到 `2e7769c`，工作樹乾淨；dosgolem-san 的
  `san1-hercules-b0000` @ `92f84f0` 已推，沒有未推 commit。
- 第一輪收在 `2026-09-18-player-choices.md` §6：#82／#84／#65／#64／#71／#33
  六條關閉，回歸十支對拍全 PASS。

## 2. 既定決策（2026-09-18 裁定，不重問）

| Issue | 決策 |
|---|---|
| #98 | **跟著原版逐位問**：軍團整編問「分配那一位將軍」「將%s分到那一軍」，拿掉玩家那一方的自動分配 |
| #99 | **照原版讓玩家挑**：電腦聯合出兵打過來時，守方是玩家就問「聯合守方那一郡合守」 |
| #100 | **用電腦的判斷式處置**：沒有介面時玩家那一方抓到的人走電腦那一套，不再留 `FateNone` |

每條的裁定已經留言在各 Issue 上，標籤也改好了。

## 3. 順序與各條要點

順序是 **#98 → #99 → #100**：#98 動到 `formUp` 與戰役入口，#99 的停法沿用
#64 已經鋪好的 `PendingDefence`，#100 只動 `capture` 一條分支但**要先量骰序**，
放最後才不會在量的時候混進另外兩條的改動。

**三條共同的硬前提**：`internal/battle` 與 `internal/game` 的電腦分支**都要留著**。
無畫面的路（月度對拍、示範模式、批次跑、`EndMonth`）沒有人可以答——
這是 #64 與 `e27ce22` 已經踩過一次的坑（`runPrefectureTurns` 在交出去那一格
break，而月份照樣往前推，剩下的郡整個月沒跑，還不報錯）。

**#98 軍團整編**
- 位址與提示在 `docs/re/05` §12：整編 `0x20a30` 每個軍團一輪、援軍格 `0xFFFF`
  跳過；`0x21457`／`DS:0x7a3a`「分配那一位將軍(1-%d):」→ `0x21509`／`DS:0x7a60`
  「將%s分到那一軍(1-5):」→ 回頭再問將軍；空欄位 Enter 取消才出
  「分配完畢(Y/N)」（`0x21493`）。
- ⚠ **已分配過的人再選一次會被退回重問**（`0x214af`）：固定送同一個號碼是
  無限迴圈。收尾走法「選一位 → 分到某軍 → 再問將軍時送空的 Enter → `Y`」。
- 攻方（自己發動）與守方（#64 開著時）走同一條。
- 說明書 p.27 的四條規則逐條對一次（`docs/mechanics/40` §編隊）。
- 驗收：三張表逐位元組相同 ＋ **反向對照**（換回自動分配要紅）。

**#99 助守軍那一郡**
- **先讀那一問**：提示字串位址、清單是 `0x1d4ec` 那一支還是自己畫的、
  取消語意、一個鄰郡都沒有時走哪一條。hook 命中 0 次先當成攔錯地方
  （`CLAUDE.md` §7 第 21 條；第一輪的 #65 就是這樣繞了二十分鐘）。
- 停法沿用 #64：`PendingDefence` → `session.turnCell` 停 → `FinishDefence`
  收尾推游標。**不要另造一套停法。**
- 電腦那一邊仍取掃到的最後一個（那是量到的原版行為）。
- 驗收：三張表逐位元組相同、版面照 `docs/spec/014`、反向對照。

**#100 沒有介面時的捕獲**
- **先量再改**：加一個計數器（玩家那一方捕獲幾次），跑
  `TestZZMonthParity`／`Plus`／`TestZZUnitAIDayParity`／`Plus` 各一遍。
  **沒有紅不等於沒影響**——可能只是那個盤面沒走到。
- 量到會動骰序時**先回報數字再決定**（接受基準改變，或把行為掛在旗標後面
  讓對拍走舊路），不要自己挑一邊。
- 這一條是 **registered remake 差異**，不是「對回原版」：原版在同一刻是
  問人、不擲骰。`docs/spec/018` §7 那一列要從「缺口」改寫成「差異」。
- 驗收：單元測試釘住「沒有介面時四種處置都可能發生、`FateNone` 不再出現」，
  四支對拍的結論要明確寫出來（沒變／變幾個位元組／為什麼）。

## 4. 普查

三條做完後照 `2026-09-15-fidelity-loop.md` §3 再普查一次。**這一輪看戰役層**：
`internal/battle` 與 `internal/game` 裡「原版問人、remake 自己決定」還剩哪幾處
（第一輪掃的是主畫面命令那一層）。開前查重；A／B／C 三軌仍然停開。

已知會撈到、**不要重開**的兩處：弓箭一次射完整壺（原版一次一箭，
`battle.go` 的 registered 差異，理由是「一次一箭配一次一回合太細」）、
子選單按一鍵就進（原版是數字輸入，#95 的配套差異）。

## 5. 完成條件

#98、#99、#100 關閉並附收據；普查連續兩輪開不出新的 agent-ready；
剩下的 open 只有 #2 與新的 needs-human。最終回覆列：已關 Issue 與證據、
新的 needs-human 待答問題、dosgolem-san 未推 commit、Docker 與工作樹狀態。
