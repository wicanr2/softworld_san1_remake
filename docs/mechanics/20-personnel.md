# 人物：能力值、職位、登用、忠誠

## 1. 人物表的欄位

一筆 30 個位元組。已解出的欄位（`L0`／`[base]`，`docs/spec/003` §2，
實作 `internal/game/tables.go`）：

| 位移 | 型別 | 欄位 |
|---|---|---|
| 0 | 6 B Big5 | 姓名（空白補齊）|
| 7 | `u8` | 年齡 |
| 8 | `u8` | 體能 |
| 9 | `u8` | 謀略 |
| 10 | `u8` | 戰力 |
| 11 | `u8` | 魅力 |
| 12 | `u8` | 職位 |
| 13 | `u8` | 出身郡（`L2`，見 `docs/spec/003` §2.4）|
| 16 | `u8` | 忠心度 |
| 17 | `u8` | 身分 |
| 18 | `u8` | 效力的勢力（`0xFF` ＝ 在野）|
| 19 | `u8` | 所在郡（1–42）|
| 21 | `u8` | 兵種 |
| 22 | `u16` | 兵士數 |
| 24 | `u8` | 訓練度 |
| 25 | `u8` | 武裝度 |

**姓名以資料為準，不以說明書為準**（`CLAUDE.md` §2.2）。說明書是掃描轉錄，
已知案例：p.51「韓玄」印得像「韓亦」。

劇本 001 有 350 個槽，其中 346 個是人；**末尾 4 筆是填充不是空槽**
（`docs/formats/03` §4.1）。

## 身分的編碼：還有一個 10（`L0`、`[base]`）

原版的「登用人才」常式（`0xd0ae`）收兩種身分：

```
cmpb es:[bx+0x2221], 0x0a   ; 身分 10
je  收
cmpb es:[bx+0x2221], 8      ; 身分 8（在野露面）
jne 跳過
```

**身分 10 是 remake 沒有的編碼**（`internal/state` 只列了 0、1、2、3、
8、9、11）。它與 8 一樣可以被登用，但是什麼還沒解。

其餘的編碼都由碼確認過：**0 君主、2 太守、3 一般武將、8 在野露面、
9 在野未露面**（`docs/re/03` §1.4）。

## 訓練兵士：`(智/3 + 武/2) / 3`（`L0`、`[base]`）

從原版的碼直接讀出來的（`docs/re/03` §1.3）：

```
對該郡守軍的每一位：
    兵士數 == 0 → 訓練度 = 0
    否則        → 訓練度 = min(100, 訓練度 + (智/3 + 武/2) / 除數)
```

**逐項截斷**——智與武各自先整數除，再相加，再除。

兩件說明書沒寫的事：**訓練是對整個守軍跑一遍，不挑人**；
**沒有兵的人訓練度會被歸零**。

那個參數由兩個 thunk 決定（3 或 4），一個月裡 28 次走 3、4 次走 4，
兩者的呼叫端是同一個分派點，**分岔的條件還沒解**。`internal/game`
取 3（`game.TrainGain`）。

說明書只說「各將的能力影響其麾下的訓練度提升」——方向對，係數是碼裡
才有的。

## 徵兵：人口 1:1 變成兵（`L1`、`[base]`）

受控實驗（`internal/parity` 的 `TestZZAIProbe`）：同一個盤面只改金額，
讓原版的電腦諸侯自己決定要不要徵兵。

| 給的金 | 兵士 | 人口（沒徵兵時會是 164）|
|---|---|---|
| 5,000 | ＋33 | 131 ＝ 164 − 33 |
| 20,000 | ＋133 | 31 ＝ 164 − 133 |

**兵士增加多少，人口就少多少，一個不差。** 兩個量級各驗一次。
（兩欄存的都是實際值 ÷ 100。）


## 2. 職位與帶兵上限

`L0`＋`L2`／`[base]`。職位的編號是原版字串表的順序，文官與武官成對
（`docs/spec/003` §2.1）：

| 職位 | 帶兵上限 |
|---|---|
| 諸侯 | 5,000 |
| 軍師・大將 | 3,000 |
| 參軍・副將 | 2,500 |
| 主簿・裨將 | 2,000 |
| 謀士・牙將 | 1,500 |

上限來自說明書 p.18，而且**與原版資料對得上**：劇本 001 的 346 位人物
零人超標，九個職位裡有八個的實際最大兵數正好等於上限（謀士那一格
劇本裡最高只有 200，沒有頂到）。這個「零超標、八格頂到」是 `L2` 的證據，
比說明書單獨一句強。

## 3. 身分

`L0`／`[base]`（`BASEGEN` 位移 17，`docs/spec/003` §2.2）：
諸侯／軍師／太守／現役／在野／未登場。

「在野」有兩種：列入該郡在野數的，與**尋訪之後才會出現的**。
後者在資料裡是另一個身分值——所以「尋訪人才」找得到的人是本來就在那個郡的
（`internal/game/personnel.go` 的 `Search`）。

## 4. 人事四項

| 指令 | 規則 | 等級 |
|---|---|---|
| 尋訪人才 | 每次 5 金，謀略越高機會越大 | `L2`（說明書 p.22）|
| 登用人才 | 每人 30 金，**太守魅力**越高機會越大 | `L2`（p.22）|
| 賞賜金帛 | 上限 100 金，**每郡每月每人一次** | `L2`（p.23）|
| 撤職 | 遣散費 10 金，**主事者不能撤** | `L2`（p.23）|

成功率的係數是 **remake 選的**（`TuneSearchIntel` 等）。

⚠ **成功率型的指令在 remake 裡是決定性的**：用局面本身當種子
（年、月、郡、人物），同一個局面同一個決定永遠得到同一個結果。
理由與 AI 相同——帶系統亂數的話「這一次為什麼失敗」無法回答，對拍也無從
下手。**這不是原版的亂數**；原版的亂數產生器還沒反組譯到
（`internal/game/personnel.go` 的 `roll`）。

## 5. 君主五項

| 指令 | 規則 | 等級 |
|---|---|---|
| 指定軍師 | **謀略不得低於 80**、君主不得兼任、同時只能有一位 | `L2`（說明書 p.23）|
| 指定太守 | 指定**自身所在之外**的領地部將；不耗命令 | `L2`（p.23）|
| 郡縣自治 | 正常／內政／軍事／自治四種；不耗命令 | `L2`（p.23–24）|
| 賞賜物品 | **玉璽不能送人**；靠賞賜提升的能力上限 90 | `L2`（p.24）|
| 登用他國人才 | 每次 100 金；諸侯挖不動；**太守被挖角則全郡易主** | `L2`（p.24）|

寶物的效果是**原版的數字**（說明書 p.24，`L2`）：
兵書 +2 謀略、寶刀 +3 戰力、美女 +5 魅力、駿馬 +2 戰力 +3 魅力。

「上限 90」是**賞賜的上限不是能力的上限**——原本就 95 的人不會因為收到
禮物而掉到 90（`internal/game/personnel.go` 的 `raiseTo`）。

## 6. 年齡與死亡

`L2`／`[base]`（`internal/game/events.go`）：年齡在**每年的第一個春月**
增長一次，不是每個春月都加；體能隨年紀衰退，衰退幅度與死亡判定的係數是
**remake 選的**。未登場的人物到某個年齡才出頭（`TuneComingOfAge`，
remake 選的）。

## 7. 未解

- **兵種**（位移 21）的七個值：陸／山／水／山陸／水陸／山水／強力。
  名稱與適性方向來自說明書 p.18（`L2`），編號對應是從資料推的。
- **忠心度怎麼隨時間變**。現在只有賞賜與挖角會動它。
- 位移 6、14–15、20、26–29 這 8 個位元組還沒解。
