# CONTEXT — 三國演義（智冠 1991, DOS）remake

新 session 或對話被壓縮後，先讀這一份。規則在 `CLAUDE.md`，這裡是**現況**。

---

## 1. 現在做到哪裡

| | 狀態 | 日期 |
|---|---|---|
| 素材取得 | 兩版 js-dos bundle 下載完成、SHA-256 驗過 | 2026-09-06 |
| 專案骨架 | 建立；`CLAUDE.md`／`LICENSE`／`.gitignore` 就位 | 2026-09-06 |
| dosgolem 工作副本 | `~/cht/dosgolem-san`，分支 `san1-msc-oracle`（基於 `origin/master`）| 2026-09-06 |
| 說明書 | 46 頁解到 `workplace/manual/`，整理中 | 2026-09-06 |
| dosgolem probe | 兩版跑過，服務清單產出（`docs/re/00`）| 2026-09-06 |
| 反組譯 | IDA 管線成立（`docs/re/01`）；主程式模組已用 objdump 逐段對讀 | 2026-09-06 |
| **加強版跑得起來了** | `TestZZBootPlus` 把 `ASV.EXE` 載進 dosgolem，開 253 個檔、畫出標題。卡點是 `DATA0.GRP` 檔頭的 `e_cblp = 0xAA90`——那一格只有低九位有意義，dosgolem 沒遮就算出映像比檔案長而拒載（修在 dosgolem 的 `loader.go`）| 2026-09-07 |
| 格式解析 | 容器、州郡名、劇本三張表、存檔、配樂、點陣圖、戰場地圖都解出來了（`VERIFICATION-MATRIX` §2）| 2026-09-07 |
| Go 程式 | `assets`／`state`／`cells`／`font`／`ui` 五個套件 ＋ `cmd/san1`（Ebiten）＋ `cmd/san1dump`（CLI／PNG），測試全綠 | 2026-09-06 |
| 引擎畫面 | 州郡一覽可渲染：42 郡名、零缺字，Ebiten 與無頭 PNG 走同一份畫面程式 | 2026-09-06 |
| **對拍框架** | **成立**：`internal/parity` 走 `-tags oracle`，go workspace 接 `dosgolem-san`。盤面由對拍這一方寫進原版的記憶體（`oracle.SetBytes`），不靠原版的 `RND()` | 2026-09-06 |

| **remake 一局玩得完** | 十類指令、四季事件、戰役決勝、勝負判定；全電腦對戰約五十年分出勝負 | 2026-09-06 |
| **戰役的戰術層** | `internal/battle`：六方向格子、九種地形、移動力與兵種適性、五種戰鬥隊伍、快戰死戰弓箭、單挑、六種計謀與天氣、退兵、缺糧、卅天判定 | 2026-09-06 |
| **戰術層對回原版** | 戰場地圖是原版資料（州郡 offset 55–174，42 郡 42 張）；地形碼、移動花費、地形攻守值、兵種適性、移動力、將領戰力值、交戰傷亡、六種計謀的費用／門檻／**成功判定**、火攻水淹的地形殺傷、陷阱、燒糧、三十天判定全部換成量到的（`docs/re/05`）。退役十四個 `Tune*` | 2026-09-07 |
| **存讀檔** | 六個進度，三張表照原版版面逐位元組寫回，未解欄位原封不動（`docs/formats/05`）| 2026-09-06 |
| **主程式字串表** | `cmd/san1strings` 抽出，整理成 `docs/re/04`；選單、欄位標籤、計謀編號、每郡上限 50 位將軍都據此修正 | 2026-09-06 |
| **年號** | 中曆／西曆切換（其他 → 年號）。年號表在手冊給的六個時期上全部對上，原版開局後怎麼數**還沒查證** | 2026-09-06 |
| **戰役可親自指揮** | 主戰場畫面 ＋ 原版的兩層指令與方向鍵盤；不看的戰役交給電腦，逐日戰報留著 | 2026-09-06 |
| **配樂解出來了** | 六首（`MUS` 五首 ＋ `MUSV` 一首長的）＋ AdLib 音色庫；事件數與表頭精準吻合。音色的 56 個位元組是兩個運算子各十三項，拿原版填進 OPL2 的暫存器值驗過（`docs/formats/06`）| 2026-09-06 |
| **遊戲放得出配樂** | `internal/music` 有一顆 OPL2，事件流照 AdLib 的聲部分配邊播邊合成；音高與原版逐次相同（`TestNotesMatchTheOriginal`）| 2026-09-06 |
| **其他選單的開關** | 音樂／音效／語音／查看電腦戰役／年號／延時，狀態存進存檔 | 2026-09-06 |
| **點陣圖解出來了** | 613 張 `.IMG`／`.FAC` 同一個格式；平面與顏色的對應拿 dosgolem 的原版畫面逐像素比對確認（`docs/formats/07`）| 2026-09-06 |
| **多語系** | 畫面、提示、事件紀錄三層都走 `internal/i18n/lang/*.json`，繁中（原文）／英／日，372 個鍵；字型涵蓋率有測試盯著 | 2026-09-06 |
| **素材匯出** | `tools/assets.sh`：圖→PNG、劇本→JSON、配樂→OGG（Go 合成 WAV，容器裡的 ffmpeg 編碼），附來源雜湊的 manifest | 2026-09-06 |
| **原版跑到遊戲主畫面** | 開機 → 選劇本 → 選君主 → 設難度 → 十個命令的主畫面。配方在 `docs/re/02` | 2026-09-06 |
| **原版走得動月份** | 主畫面收得下命令（`9`＋`Enter` 開子選單，扣掉游標雜訊之後對照組是 0）；防拷密碼延後到第一道 ＊ 指令才問，而且**任何四位數都過得去**——兩個答案各走十六個月，十八份盤面逐位元組相同 | 2026-09-07 |
| **主選單也接上原版素材了** | 標題牌 `MENU0A`(40,27)／`MENU0B`(320,27) 各 **100%**，六個按鈕 `MENU2` 排在 x ∈ {152,376}、y ∈ {215,267,319}（95–96%，字寫在上面）。**這一張的位置只能靠畫面比對**——主選單跑在開機鏈第二層 `DATA0.GRP`，主程式的碼段 dump 涵蓋不到。`MENU1`（左側直牌）位置還沒定：最高 71.6%，而且那個位置會蓋到 100% 相符的 `MENU0A`，所以一定是錯的 | 2026-09-07 |
| **主畫面接上原版素材了** | 七張 `MAINMAP*` 依 `0x11ebc`–`0x11fc5` 的座標拼成 640×350 底圖，與原版畫面分區逐像素比：上方花邊與最右直條 **100%**，左側直條 94.3%、地圖區 68.7%（差的是後來蓋上去的年月、換色與編號）。州郡從記錄的地圖座標 `+0x50`／`+0x2c` 灌色（**位移讀碼不擬合**——擬合有 99 組解）。肖像在 (536,116)，判準是 256 張 `F###.FAC` 逐一比對只有 `F228` 100% 相符。規格 `docs/spec/005` | 2026-09-07 |
| **整張主戰場都接上原版素材了** | 底紋 `8x8PAT0`、左欄四個小框、三個面板（176×96 在 x 64／256／448）、肖像與肖像框（**攻方那張左右翻**）全部逐像素對回原版；面板裡的字是 remake 自排，位置與字色照原版。還沒接的是場地四周的階梯邊框（下緣兩列原版是白的）| 2026-09-07 |
| **主戰場也接上原版素材了** | 地形是 `EICON.GRP` 的 36 張 48×32 圖塊（編號即地形碼），78 格裡 73 格逐像素全中；部隊是 `DATA1` 的 24 面旗，四組 `D0`／`D1`／`A0`／`A1` ＝ 主守／助守／主攻／助攻，旗面上印帥、先、左、右、後（第六張是城門圖示）。旗在格子左上角加 (8,0)，兵力牌 40×17 在旗下 15。**選到的部隊畫成反白**（每格顏色 `^15`）——五個標記全數 100%，先前「守軍的標記找不到」是拿原色去掃的結果（R20）| 2026-09-07 |
| **關寨蓋在哪一格解出來了** | 兩道門（`0x1aeba`–`0x1aed1`）：`DS:0x7134[地形碼] != 0`——那張十六格的表只有 2、7、8、10、11、12 是 1，對上地形就是**山丘、平原、樹林**；以及 `(格 & 0xF0) >= 0xA0`，擋掉**通往鄰郡的通道格**。寫入是 `(格 & 0xF6) | 6`，高四位留著。remake 原本只挑「沒標記的平原」，平原用完時會說蓋不了而原版蓋得起來 | 2026-09-07 |
| **`es:0x5b02` 是「有沒有人在玩」** | 開局問「請問有幾人玩(0-16)」，答 0 就印「電腦自動示範模式」（`DS:0x5fcb`）：延時壓到 2、查看電腦戰役開啟、旗標設 0；有人玩設 `0xFFFF`。三處判斷因此讀得通——示範模式每郡跑完問要不要中斷、月底多跑一支，而**絕嗣檢查只在有人玩時做** | 2026-09-07 |
| **統一的條件不看玉璽** | `0x15852`：所有**有主**的郡同屬一方就結束（無主的郡跳過），除了郡的所屬與君主姓名之外什麼都沒讀。說明書 p.37 那句在碼裡沒有對應（R19）。另一個結束條件是 `0x15924`：十六個諸侯槽裡沒有任何一個「玩家操縱且君主欄不是 0xFFFF」——**輸的定義是絕嗣不是沒地** | 2026-09-07 |
| **州郡 offset 35 是南北分群** | 24 個郡是 11、18 個是 12，六個劇本、十二個存檔槽、兩版完全相同；荊州與益州被長江切開，揚州與交州整個在南。**兩版的碼都沒有讀它**（掃過全部 `[bx+disp16]` 存取，同一支掃描在 offset 34 掃得到 6 處，所以掃描有效）。原本記的「與有主／無主一致」是 24／18 撞在一起的巧合（R18）| 2026-09-07 |
| **讀得進玩家自己的原版進度了** | `save.ReadOriginal` 把 `DATA2.GRP` 裡的一個進度讀成一局。連帶修好 `ActiveFactions`：操縱方那一欄（諸侯 offset 0）**劇本檔裡還沒填**、存檔裡填好了，所以看得到 `0xFFFF` 就改用它。舊判準（君主是不是人／有沒有領地）在存檔上會漏掉兩種勢力——**自創君主的君主槽指向人物表的填充筆**，而輸掉的玩家一個郡都不剩 | 2026-09-07 |
| **存檔的另外三個項目全解了** | `BASEPRO.SVn`（256 B）＝ 年月、難度、六個選項、玉璽持有者，外加「這個月哪些郡還沒下令」的 43 格旗標與郡的處理順序；`BASEPRE.SVn`（384 B）＝ **自創君主的十二個字模**（四個名額 × 三個字，字碼是 Big5 `A141`–`A14C` 這十二個被挪用的全形標點）；`SAVENAME.SVP` ＝ 六筆 21 byte。位移全部從讀檔常式 `0x141ef`–`0x143b8` 讀出來（`docs/re/08`）| 2026-09-07 |
| **電腦買米整段對拍過了** | 四個月 136 次、六個 AI 等級全部走到、逐次相同（`docs/playtest/02`）。匯率的除數是 `[10,10,10,10,9,8]`——第六支寫成 `mov cx,3` ＋ `sar cl,ax`，那個 3 是位移量不是除數。買的量走浮點：`剩下的金 = clamp(金 − 缺口 ÷ 匯率, 0, 30000)`，倒推出花掉的金，所以金不夠時會自動花光而不透支 | 2026-09-07 |
| **原版連走十六個月** | 每個月倒一份三張表。物價每月四十到四十二個郡一起換、年齡在元月、人口在十月成長 15%（`docs/mechanics/70-ai` §2.2）| 2026-09-07 |
| **一個月的狀態轉移對拍差 0 個位元組** | 兩邊從同一份盤面各走同一個月：43 個郡、350 位人物、16 個勢力逐格相同，十張分派表的抽亂數次數全部 ±0。走到 0 的九個階段記在 `docs/playtest/02` 開頭 | 2026-09-08 |
| **M5 規則層收尾** | 九份機制文件的「還缺什麼」逐條清掉：弓箭的單次殺傷（自己的地形表 `DS:0x81c0`）、秋收的米那一半（洪水率只進米的算式）、秋收的保底分支、戰場糧草（三天扣一次、四個軍團都扣）、缺糧逃亡（每位將領的兵 `÷= RND(2)+2`）、軍團記錄 offset 14（兵士（百））、輸的定義改成絕嗣。`TuneArrowDamage` 與 `TuneHarvestRicePerLand` 因此退役 | 2026-09-09 |
| **戰役的逐日對拍完成（含交戰的傷亡）** | 把原版驅動進主戰場（玩家親征，盤面直接寫記憶體），讀它自己的部隊記錄：開打前三欄逐支相同；休息連走七天移動力逐日相同；走兩步逐步比移動花費；走到守軍旁邊之後電腦打過來，兩次交戰雙方的兵逐次相同。**56 個欄位全中**。修掉五個實作錯誤：移動力上限抄錯了公式（R37）、開新的一天是回填不是重設、休息 +2 之後夾在 15、交戰結算整條換成 `0x2a224`（R38）、浮點寬度要照 x87 的 64 位元尾數 | 2026-09-09 |
| **存讀檔兩個方向都對拍過了** | 載入：原版讀它自己的進度 1、remake 走 `save.ReadOriginal` 讀同一格，除兩個快照欄位外逐位元組相同。寫出：remake 走完一個月存進第 1 格，六個項目換進**原版目錄複製品**的 `DATA2.GRP`，原版讀回來 19,220 個位元組**完全相同** | 2026-09-09 |
| **兩版的規則差異解出來了** | 全部繞著難度：上限 10 → 20、係數表 11 格 → 21 格（`DS:0x5430`）。字串 400 條共有、只差 `請設定難度(1-10)` 與 `(1-20)` 那一條。碼段那一邊，戰役的勝負判定多兩條——難度 ≥ 11 跳過「守方統帥全滅 → 攻方勝」（`0x2296b`），另有不看難度的「總兵數為 0 者敗」（`0x229ba`）。全部 `L0`。remake 這邊是 `state.Edition` ＋ `battle.Rules`（`docs/spec/004`）| 2026-09-07 |
| **玉璽現世解出來了** | 春季判一次：玉璽未現世且 `RND(100) > 50` → 隨機一個活著的勢力拿到玉璽，**人望 += RND(30)+40**（`0x15cfd`／`0x15136`）。判準是出貨版裡留著的除錯字串 `Double Seal event` 與旁邊的 `玉璽 !`。**remake 原本只檢查玉璽、從來不會發**；劇本 001 開局就有人持有，所以缺口看不出來。玉璽的作用是人望，不是勝利條件（R19）| 2026-09-07 |
| **人物出頭解出來了** | 出頭年齡是**每個人自己的**（人物表 offset 26，原本在未解清單裡），不是全域常數；而且**牽絆對象有勢力就直接投奔他**（他的郡未滿 50 人），否則才回出身郡當在野。手冊「新將投效其親族朋友」是字面意思 | 2026-09-07 |
| **諸侯記錄 offset 8 是人望** | 判準是找出誰寫它：戰役勝方 +2、敗方 −2（`0x204b4`／`0x204e0`），春季隨機事件 `+RND(30)+40`。它每年決定部下忠誠的漲跌（`忠誠 += (人望 − 60) ÷ 2`，君主不算），**60 是分水嶺**——把「打勝仗」與「留得住人」接在一起 | 2026-09-07 |
| **訓練度與武裝度每年自然衰減** | 元月逐槽 `−RND(值 ÷ 10)`（`0x16006`／`0x16025`），與土地價值同一個形狀。**這是訓練與購置武器要一直做的原因**；remake 原本沒有，所以練到頂就永遠是精兵。全電腦對戰因此從 60 年統一延到 73 年 | 2026-09-07 |
| **進貢的件數解出來了** | 量的來源是**基數 ＝ 人才量 ÷ (RND(10)+80)**（人才量 ＝ Σ 民眾忠誠÷4 ＋ 土地價值÷2），領地數只是閘門；四種寶物**各**得 `min(RND(基數+1), RND(5)+8)`，再 `RND(4)` 挑一種多給一件（`0x1723c`）。**上限 8–12**——手冊只說「領地越多貢品越多」，照字面寫成正比會讓一統天下的勢力一年拿四十件。單郡勢力人才量 75 ÷ 80–89 ＝ 0，**一件都拿不到** | 2026-09-08 |
| **四種天災的損失幅度也解出來了** | 碼裡寫的是**保留率**：地震對人口／金／米各擲一次（剩 60–79／50–69／40–59%，人口下限五千）、水災（人口剩 70–79%、土地價值剩 60–79%、洪水率 ×1.20–1.39）、瘟疫（人口剩 40–59%）、蝗害（米剩 20–29%）。**沒有一種災害動兵力**——那是 remake 加的。⚠ 蝗害的土地價值是 ×0.80–1.69，平均往上，與直覺相反 | 2026-09-07 |
| **四季常式全部解完了** | 四種天災的機率都是 `L0`：地震純 `RND(3)`；水災只看洪水率；瘟疫要忠誠低**且**土地價值低；**蝗害要忠誠低但土地價值高**——與瘟疫方向相反。秋收 ＝ `(太守魅力 + 土地價值×4 + 忠誠×2) × 人口 ÷ 300`，上限 30000，**太守是誰會影響收入** | 2026-09-07 |
| **四季分派器與春季常式解出來了** | `es:[0x3f08]` 是**季節**（0–3），`0x15c1e` 拿它跳到四支常式。春季的第一件事是**土地價值每郡掉 `RND(土地價值 ÷ 10)`**——那是「土地開發要一直做」的原因；地震是純 `RND(3)`，隨機一個郡，什麼門檻都沒有 | 2026-09-07 |
| **天災與人口的算式解出來了** | 水災（`0x164c8`）只看洪水率、不看忠誠，各郡有不同的基礎值（`DS:0x679c`，43 格）；瘟疫（`0x167df`）每月只挑一個郡，看忠誠與土地價值兩道門檻——**說明書「天災多因人怨引起」講的是瘟疫**。人口成長的倍率跟著土地價值與忠誠走，上限 15%（R15）| 2026-09-07 |
| **十八張分派表全部解開了** | 相鄰兩張表差 `0x20`，所以**一張表八格不是六格**。144 個 far pointer 全解之後：第 6、7 格十八張全是空函式（AI 等級只有 0–5）；`0x5514` 六格全空；**出兵、賞賜物品、挖角、計略四張的第 0–2 格是空的**——等級 3 的閘門是四種行為共用的 | 2026-09-07 |
| **弓箭次數解出來了** | 部隊記錄 offset 20 ＝ 弓箭剩餘次數（選單印 `DS:0x80ae`「弓箭攻擊 次數:%d」）。初值是**兵數加權**的平均武裝度 ÷ 20（`0x272eb`），不是手冊算例看起來的算術平均（R14）；而且**會用完**——誘敵與圍攻的結算也消耗出手方的箭 | 2026-09-07 |
| **一騎討整支解完了** | 決勝的公式本來就是 `L0`；這次補上**落敗的處置**：`RND(7)`，只有 0 才死於刀下，其餘 6/7 被擒（敗者槽號寫進勝方的俘虜欄 `es:0x1732`）。回合用完雙方都有體能就是平手。`TuneCaptureOnDuel` 退役 | 2026-09-07 |
| **誘敵與圍攻解出來了** | 兩招都交給共同的交戰結算 `0x2a224`，倍率表 `DS:0x81a2` ＝ 80/100/150/200/250/300/350/400。誘敵**把攻守對調**（是引敵人來打你，不是手冊寫的「攻擊力下降」，見 R13）；圍攻掃目標的六個鄰格，與目標不同陣營的部隊各打一次、倍率 80。三個 `Tune*` 退役 | 2026-09-07 |
| **原版的統帥條件補上了** | 三十天之前分勝負比的是「軍力記錄的統帥」與「該方第一支部隊的第一位將領」（`0x24f8c`），不是全滅。兩版共通，remake 用 `Battle.CommanderAlive` 等價表達 | 2026-09-07 |
| **戰略層五種謀略全解** | 前置條件只查軍師、**不花錢**；候選郡的十一組條件；軍師會先預測而且謀略不足時預測被隨機化；偽書使疑 `忠誠 × (250 − RND(魅力÷2) − 魅力) ÷ 250`（**已對拍**）；驅虎吞狼／遠交近攻／聯合出兵三支的結局是呼叫戰鬥子系統，四個郡分別填什麼都讀出來了——那就是「助攻軍必須運用謀略才能得到」的出處（`docs/re/07`）| 2026-09-07 |
| **電腦諸侯只用偽書使疑** | 計略那張表的整條鏈在 `0xe68e` 一支裡，固定回 0，所以接在它後面的 `0xe7aa`（策反人民的五刀）永遠執行不到。**策反人民只有玩家下得了**（R22）| 2026-09-08 |
| **內政與商業的量解出來了** | **玩家與電腦走不同常式**：玩家開墾是 `max(謀略−50,0)÷12`（謀略不足就是加 0），電腦是 `(謀略−底)÷12` 非正時擲 `RND(2)`，而且底與防洪除數隨 AI 等級變。關寨費用 ＝ 物價 × 100、上限 5、要謀略 > 79。米糧買賣共用 `(100−物價)÷10`，買是「一金換 rate 米」賣是「rate 米換一金」| 2026-09-07 |
| **君主繼承與老死解出來了** | 繼承的候選是**整個勢力**不限郡、依魅力排序，人望 ← 四捨五入(繼承者魅力 × 原人望 ÷ 100)；找不到人就是「無人繼承」、勢力滅亡、玉璽釋出。老死看**壽命**（人物 offset 28）：`RND(3)+壽命 >= 年齡` 就完全不動體能，過了才扣 `(壽命−年齡)×25 − RND(50)`——**體能是壽命的緩衝** | 2026-09-07 |
| **記錄欄位又解出六格** | 州郡 offset 5 ＝ 州（14 個，原版寫「弁州」「充州」）、12 ＝ 郡縣自治型態、25 ＝ 關寨數（等於地圖上地形碼 6 的格數）、32 ＝ 主事者槽號、34 ＝ 戰場版面；人物 offset 27 ＝ 肖像編號、28 ＝ 壽命。**自治不是設定是委任**：非零的郡由電腦拿「值 − 1」當 AI 等級跑分派器，這也是「六個等級是六種性格不是六個強度」的第二個證據 | 2026-09-07 |
| **電腦諸侯的分派表解出十六張（共十八張）** | 六個等級 × 十八張 far pointer 表，無條件依序跑完。已讀的九張判斷式從碼讀出來不是行為推測：選出行動者、內政、訓練、指定太守、指定軍師、賞賜、尋訪、購置武器、登用。花費係數 `[1,1,1,1,0.9,0.75]`、登用的費用與加成、武裝度是百分比，全部有對應的單測（`docs/mechanics/70-ai`）| 2026-09-07 |

完成度的數字在 `VERIFICATION-MATRIX.md`（唯一來源）；文件放什麼在 `docs/INDEX.md`。

里程碑定義在 `CLAUDE.md` §10。M5／M6 的觀測管線已經打通——原版開得進遊戲、
過得了防拷、走得動月份，每個月的欄位變化收得下來，也和 remake 逐欄位比得起來。
`70-ai` 的十八張分派表讀出十六張，回合預算的公式與係數表都解出來了。目前在 **M2–M3 之間**：M0 完成、M2（容器格式）實質完成且**得到原版行為背書**（F31）、
M3（文字與字型）畫面已通。**M1（dosgolem 跑得動）達成**：原版走完
`AA.EXE` → `DATA0.GRP` → `DATA5.GRP` 的 chain-load，載入 `DATA1`／`DATA2`／`DATA3`
三組容器，畫出主選單並停在等待輸入。

---

## 2. 素材

`org_game/MANIFEST.md` 有來源、雜湊與取得方式。摘要：

| 版本 | 進入點 | 編譯器 |
|---|---|---|
| 原版 | `AA.EXE`（354,960 B）| Microsoft C 6.0 |
| 加強版 | `ASV.EXE`（300,494 B）| Microsoft C 6.0 |

`workplace/manual/珍098-三國演義/` 有 46 頁掃描（`000.jpg`–`045.jpg`，約 1080×1380）。

---

## 3. 已知事實

每條標推論等級（`CLAUDE.md` §8）與版本（`[base]`／`[plus]`／`[both]`）。

| # | 斷言 | 等級 | 版本 | 證據 |
|---|---|---|---|---|
| F1 | 兩支主程式都由 Microsoft C 6.0 編譯 | `L0` | `[both]` | 兩檔皆含字串 `MS Run-Time Library - Copyright (c) 1990, Microsoft Corp` |
| F2 | 六個 `.IDX` 與六個 `.NAM` 兩版**逐位元組相同** | `L0` | `[both]` | `org_game/` 下 SHA-256 比對 |
| F3 | `DATA1.GRP`／`DATA3.GRP`／`DATA4.GRP` 兩版相同 | `L0` | `[both]` | 同上 |
| F4 | `DATA0.GRP` 42,488 → 87,696；`DATA5.GRP` 145,378 → 287,590；`DATA2.GRP` 長度相同但內容不同 | `L0` | — | 同上 |
| F5 | 加強版獨有 `NAME001`–`NAME006.SHA`、`SV.COM`、`CHKLIST.CPS`；原版獨有 `10/20/D5.GRP`、`PARTNSAV.FIL`、三個 `.BAT` | `L0` | — | 同上 |
| F5b | **`NAME00n.SHA` 不是遊戲檔**：標頭 `WSHA V1B NewType`、內容是八字命名軟體的存檔（日期民國 87／91 年），而 `ASV.EXE`／`SV.COM` 連 `NAME00` 這個字串都沒有 | `L0` | `[plus]` | `docs/mechanics/90` §4.1 |
| F6 | 兩份 bundle 附的 `dosbox.conf` 皆 `machine=svga_s3`、`memsize=16`、`core=auto`、`cycles=auto` | `L0` | `[both]` | `.jsdos/dosbox.conf`，兩版只差 autoexec 末行的執行檔名 |
| F7 | 兩版啟動時的 DOS 服務輪廓**逐項相同**（`AH=35`×12、`25`×11、`44`×5、`30`×2、`4A`×2、`48`×1），停止位址只差 `0x21` | `L0` | `[both]` | dosgolem probe，`docs/re/00` |
| F8 | 兩支執行檔都在 `int 21h AH=08`（無回顯字元輸入）上空轉 | `L0` | `[both]` | 同上，佔全部呼叫 99.99%。**成因是 dosgolem 沒有阻塞模型，不是遊戲卡住**——見 `docs/re/00` 第二輪 |
| F9 | 原版的 `DATA5.GRP` 與 `10.GRP` **逐位元組相同**；`20.GRP`（142,804）與 `D5.GRP`（145,377）是另外兩個變體 | `L0` | `[base]` | SHA-256 比對 |
| F10 | `10.BAT`／`20.BAT` 的內容是刪掉 `DATA5.GRP` 再從 `10.GRP`／`20.GRP` 複製一份回去 | `L0` | `[base]` | 檔案內容 |
| F11 | `README.DOC`（兩版相同）說明增強版的變更，含難度擴充到 1–20、密碼只需輸入一次、以 `DATA5.GRP` 標示版本 | `L0` | — | `README.DOC` |
| F12 | `ChineseSys` 是遊戲**自己的子系統名**，與 `FileSystem`／`AdLib`／`Music`／`Sound`／`Icon`／`Picture` 同在一張表（`AA.EXE` offset `0x47227` 附近）；不是外部中文系統偵測 | `L0` | `[both]` | 字串上下文 |
| F13 | 手上這本說明書是**原版**的：無版號、無印刷日期，全書無「加強版」字樣；可用時間標記只有代序落款民國八十年四月與版權年 1991 | `L0` | `[base]` | `docs/reference/01-manual-00` |
| F14 | 遊戲有密碼保護，密碼由三個座標決定（首圖 × 橫圖 × 地支），版面推算共 12×12×12 ＝ 1728 組四位數 | `L1`（結構 `L0`、總數 `L2`）| `[base]` | 手冊 p.15–16、密碼表 p.44–45 |
| F15 | 手冊**全書沒有郡名對照表**，42 個郡一律只用編號稱呼 | `L0` | `[base]` | `docs/reference/01-manual-40` |
| F16 | 附錄三收 342 位人物（姓名＋字），程式驗算條目數為 342 | `L0` | `[base]` | 同上 |
| F17 | `.NAM` 每項 16 byte（8.3 檔名）、`.IDX` 每項 4 byte 為結束位移、`.GRP` 為本體；DATA1/2/3 末值 ＝ 檔長，DATA1 round-trip 通過 | `L0` | `[both]` | `docs/formats/01` |
| F18 | `DATA0`／`DATA4`／`DATA5` 的 `.GRP` 開頭是 `MZ`；`DATA4.GRP` 帶未抹除的 `LZ91`（LZEXE 0.91）簽章，重定位數 0 | `L0` | `[both]` | 同上 |
| F19 | **42 個郡名**已取得：`DATA2.GRP` offset 818,235 起、每 176 byte 一筆、名稱在該筆 offset 0（4 byte Big5 ＋ NUL）。兩版該區逐位元組相同 | `L0` | `[both]` | `docs/formats/02` |
| F20 | `DATA1`／`DATA5` 裝字模與調色盤（`ZHONG.PAT`／`YING.PAT`／`EGAFILL.PAL`），`DATA3` 裝 422 筆臉譜（`F###.FAC`），`DATA2` 裝圖片（`SCG*`／`ENDO*.IMG`）| `L0` | `[both]` | `.NAM` 直接讀出 |
| F21 | 啟動時有連續三個「輸入 1 或 2」的提示，回顯走 `int 21h AH=06`；98 個可打的鍵裡只有 `1`／`2` 被接受 | `L0` | `[base]` | `docs/re/00` 第三輪 |
| F22 | 答完提示後 `CS:IP` 走到 `06DD:1685`，跑約 790 萬道指令才再次阻塞（基準 53,778 道）；期間仍零開檔、零視訊寫入 | `L0` | `[base]` | 同上 |
| F23 | 劇本／存檔是三檔一組：`BASEMAS`＝16×72（諸侯）、`BASESTA`＝43×176（表頭＋42 郡）、`BASEGEN`＝350×30（人物）；六劇本 `.001`–`.006`、六存檔槽 `.SV1`–`.SV6` | `L0` | `[both]` | `docs/formats/03` |
| F24 | 人物姓名在筆內 offset 0，6 byte 空白補齊；350 槽中 346 個是漢字姓名（326 個兩字、20 個三字），末 4 槽是填充（連續 Big5 碼位 A141–A14C）| `L0` | `[both]` | 同上 |
| F25 | 六個劇本的人物名冊**完全相同**，劇本之間只差數值與所在地 | `L0` | `[base]` | `internal/state` 測試 |
| F26 | 啟動是**四題裝置選擇**：標題、`Music`(No/AdLib)、`Graphic`(Hercules/EGA)、`Disk`(Floppy/HardDisk)，只收 `1`–`2` | `L0` | `[base]` | 反組譯 `0x290bd` ＋ 字串，`docs/re/00` 第四輪 |
| F27 | `DATA0`／`4`／`5.GRP` 是 **overlay 模組**，遊戲用 `int 21h AH=4Bh AL=03` 載入 | `L1` | `[base]` | 同上 |
| F28 | dosgolem 的配置器不回收，是遊戲以離開碼 255 結束的原因；修好後 `AH=4Ah` 從 19,846 次降到 7 次 | `L1` | — | `dosgolem/docs/spec/009` |
| F29 | 原版把 Floppy 拼成 **`Floopy`**（原版自己的錯字，remake 照原樣保留）| `L0` | `[base]` | 對拍測試抓到 |
| F30 | 原版的主控台輸出是**緩衝**的：開檔那一刻只有三個回顯字元，提示文字更後面才沖出來 | `L0` | `[base]` | `internal/parity` |
| F31 | 原版讀容器的順序是 **`.IDX` → `.NAM` → `.GRP`**，與 `docs/formats/01` 解出的格式一致——格式規格因此有原版行為背書 | `L1` | `[base]` | dosgolem 開檔紀錄 |
| F32 | `DATA0.GRP` 是**自解壓縮**的 overlay：載到 `0110:0000` 後自己解壓再跑 MSC 啟動碼 | `L0` | `[base]` | 軌跡 ＋ `dosgolem/docs/spec/010` |
| F33 | 原版走過啟動後會開 `DATA0.GRP` ＋ `DATA1` 三件套；`TITFONT.IMG` 確實存在於 `DATA1`（第 28 項）、`DATA0`、`DATA4` | `L0` | `[base]` | probe |
| F34 | **`DATA5.GRP` 是主程式不是資料容器**：145,378 bytes 的 MZ 映像，由 `DATA0.GRP` 用 `AH=4Bh AL=03` chain-load 到 `0110:0000` | `L0` | `[base]` | overlay 紀錄 |
| F34b | `10.BAT`／`20.BAT` 做的是 `DEL DATA5.GRP` ＋ `COPY 10.GRP DATA5.GRP`。`10.GRP` 與出貨的 `DATA5.GRP` **逐位元組相同**；`20.GRP`（142,804）從第 3 個位元組起就不同，14 萬個位元組有差。兩者都是 MZ 且都經過壓縮，所以「差在哪」讀不出來 | `L0` | `[base]` | `cmp` |
| F34c | `D5.GRP`（145,377，比 `10.GRP` 少一個位元組）也是 MZ，用途未知 | `L0` | `[base]` | `cmp` |
| F35 | 開機鏈是 `AA.EXE` →（自解壓）→ `DATA0.GRP` → `DATA5.GRP`。每一層都是完整的 MSC 程式：自己縮 PSP 區塊、自己起 far heap、自己跑啟動碼 | `L0` | `[base]` | probe 軌跡 |
| F36 | 主程式把容器分成三組（`File #0`／`#1`／`#2`）＝ `DATA1`／`DATA2`／`DATA3`，每組開 `.GRP`＋`.IDX`＋`.NAM`。開法是 `fopen("rb")` → `fclose` → `fopen("r+b")`——**第二次要的是讀寫** | `L0` | `[base]` | `05C4:AA5A` 一帶 |
| F37 | 主程式的資料段有一張檔名表（`ds:9E14`）：`DATA1.GRP`／`DATA2.GRP`／`DATA3.GRP`／`DATA1.GRP`／`DATA2.GRP`／**`B:DATA3.GRP`**／三個 `.IDX`／三個 `.NAM`。第二組帶 `B:` 是雙磁碟機安裝的路徑 | `L0` | `[base]` | 記憶體 dump |
| F38 | 武將的欄位標籤在主程式字串表裡：姓名／職位／忠誠／年齡／体能／謀略／戰力／魅力／等級／兵士／訓練／武裝／兵種（13 項）。另有 `謀略 %3d 兵士數 %4d`／`戰力 %3d 訓練度 %3d`／`魅力 %3d 武裝度 %3d` 三行格式 | `L0` | `[base]` | 記憶體 dump `0x47100` 一帶 |
| F39 | 主選單六項：開始新遊戲／載入舊進度／使用楷書字／使用隸書字／音樂欣賞／回作業系統。**字體是遊戲內選項**，楷書與隸書兩套 | `L1` | `[base]` | 原版畫面 |
| F40 | 劇本六個：中平六年／興平二年／建安六年／建安十三年／建安二十年／黃初元年 | `L1` | `[base]` | 原版畫面 |
| F41 | 中平六年可選君主六位：劉備／曹操／孫堅／袁紹／袁術／董卓。畫面是中國地圖 ＋ 42 個編號郡 ＋ 直排年號「中平六年元月」 | `L1` | `[base]` | 原版畫面 |
| F46 | **劇本記錄的欄位版面已解**（`docs/spec/003`）。人物 15 欄、州郡 12 欄、諸侯 1 欄。方法是**讓程式自己說**：原版的 `printf` 呼叫端把「哪一個 byte」與「印出來的標籤」綁在同一道指令序列裡 | `L0` | `[both]` | 反組譯格式字串 |
| F50 | 人物的**身分**（`BASEGEN`+17）是規則不是分類：郡的「在野武將數」只數身分 8 的人（42/42 吻合，不加條件只對 26 個）；每個有主的郡剛好一位主事者（身分 0 君主或 2 太守，都不在時由 1 軍師代理，193/193）| `L0` | `[both]` | `internal/state` 測試 |
| F53 | **地圖圖形在 `BASESTA` offset 45–52**：最多 8 個鄰居、`0xFF` 補齊。完全對稱（95 條邊、零條單向）、全連通、十二個槽位完全相同——是地圖幾何不是劇本狀態 | `L0` | `[both]` | `internal/state` 測試 |
| F51 | 州郡的**人口與兵士存的是實際值 ÷ 100**（格式字串是 `人口 %5d00`／`兵士%4d00`）。照存的數字用會差兩個數量級而且不報錯 | `L0` | `[both]` | 反組譯 |
| F52 | 忠誠欄在在野者身上是 `0xFF` 哨兵（227/346）；有勢力者的值域 52–100 | `L0` | `[base]` | 資料 |
| F47 | 中平六年有 **14 個勢力**（諸侯表 16 槽，兩槽指向填充筆）。領地分佈與史實一致，劉備在齊郡（8），與原版狀態列「劉備 主公，請對(8)」相符 | `L1` | `[base]` | 資料 ＋ 原版畫面 |
| F48 | 346 位人物裡 108 位有勢力、238 位在野；**108 位的所在郡全部歸屬於自己的勢力，零例外**（兩版 × 六個劇本都成立）| `L0` | `[both]` | `internal/state` 測試 |
| F49 | 原版執行期把三張表**原封不動連續載進記憶體**（`MAS`→`STA`→`GEN`，首尾相接 19,220 bytes），所以磁碟版面就是執行期版面 | `L1` | `[base]` | 記憶體 dump |
| F43 | 遊戲主畫面的命令有十個：0 狀態／1 查看／2 軍事／3 兵士／4 內政／5 商業／6 人事／7 君主／8 謀略／9 其他 | `L1` | `[base]` | 原版畫面 |
| F44 | 中平六年可**選**的君主 16 位（提示列「第1位，請選擇(1-16)」），畫面一次顯示六個 | `L1` | `[base]` | 原版畫面 |
| F45 | 開新局要設難度，範圍 1–10 | `L1` | `[base]` | 原版畫面 |
| F54 | **主程式的介面字串表已抽出**（`docs/re/04`）：十類指令、九個子選單、主戰場的兩層指令、主畫面欄位標籤、人物檢視、寶物、費用與上限、資源檔名 | `L0` | `[base]` | `AA.EXE` 資料段 |
| F55 | 原版的**戰場計謀編號是「1 火攻 2 水洽 3 陷阱 4 誘敵 5 燒糧 6 圍攻」**（`0x46df3`），與手冊把誘敵排第 3、陷阱排第 4 不同。門檻與費用兩邊一致 | `L0` | `[base]` | 同上 |
| F56 | 主戰場有**兩層指令**：部隊層 `1.移動 2.對戰 3.快戰 4.死戰 5.弓箭 6.策略 7.查看 8.退兵 0.休息`（`0x46c02`）、選 2 之後的單位層 `1.行軍 2.單挑 3.攻擊 7.查看 0.休息`（`0x46fa0`）。方向鍵盤是 `4 5 6 / 1 2 3` | `L0` | `[base]` | 同上 |
| F57 | 出兵時**攻方自己決定攜帶多少金米**，畫面會算出「30日須耗用%d米」（`0x4f60c`）；戰場上看敵軍細節要 **10 金**（`0x46ccf`）| `L0` | `[base]` | 同上 |
| F58 | **每郡上限 50 位將軍**（`本郡已有50位將軍`、`本郡將軍已達50人`，另有除錯訊息 `War Gen <1 or >50`）。手冊沒寫這條 | `L0` | `[base]` | 同上 |
| F59 | 開機主選單有**六項**：開始新遊戲／載入舊進度／使用楷書字／使用隸書字／音樂欣賞／回作業系統（`0x474fc`–`0x47565`）。玩家人數 `0` 是電腦自動示範模式 | `L0` | `[base]` | 同上 |
| F60 | **自創君主是點數分配制**：年齡／體能／謀略／戰力／魅力／領地六項配點，且「您的領地必須為空白地」（`0x47941`–`0x479ec`）| `L0` | `[base]` | 同上 |
| F61 | 五首配樂有名字：思古／小徑／風雲／戰鼓／末路（`0x47bb9`–`0x47c0d`）| `L0` | `[base]` | 同上 |
| F62 | `.OKR` 的檔名由格式字串 `R%03d.OKR` 組出（`0x495f2`，出現三次，位在戰役程式碼區）。容器裡是 `R000`–`R483` 加 `R498`／`R499` 共 465 項。**465 不是 42 的倍數，所以不是一郡一張圖** | `L0`（名稱與位置）／`L1`（是戰役資料）| `[base]` | `docs/re/04` §13 |
| F63 | **點陣圖的版面已解**：表頭 `uint16 高`＋`uint16 寬`，本體四個完整位元平面（每平面 `(寬+7)/8 × 高`），最高位在左，平面依 I、R、G、B 存。613 張全部解得開 | `L0` | `[both]` | `docs/formats/07` |
| F64 | 平面與顏色的對應**只能拿原版畫面比對才分得出來**：二十四種排列在結構上完全一樣。dosgolem 跑到君主選擇畫面，`F000.FAC` 在 (488,56)、`F005.FAC` 在 (420,56) 逐像素 100% 相符 | `L0` | `[base]` | `cmd/san1imgcheck` |
| F65 | 肖像 306 張全部 64×80；主畫面的地圖是九條 640×36 的橫帶（`MAINMAP1`–`7`、`B`、`C`）| `L0` | `[both]` | 同上 |
| F42 | 主選單的鍵盤走**硬體**：程式自己裝 IRQ1 處理常式，從埠 `0x60` 讀掃描碼存到 `0000:0004`。它同時呼叫 `int 16h AH=01` 六十七萬次卻一次都沒讀走——**BIOS 有被呼叫不代表鍵從 BIOS 進去** | `L0` | `[base]` | 反組譯 ＋ `dosgolem/docs/spec/014` |

### 3.05 密碼表：唯一必須從執行檔取的東西

F14 的密碼表印在 p.44–45，**兩面都是防影印彩色網點，掃描完全不可判讀**。
版面結構讀得出來（12 個首圖 × 12 橫圖 × 12 地支），1728 組四位數本身讀不出來。

**它擋在對拍的路上**：載入舊進度進得了主畫面，但第一道帶 ＊ 的指令
用掉之後密碼關就跳出來（`docs/re/02` §3.3）。

**但它不擋東西，而且答錯沒有延後的代價。** `1111`、`0000`、`4029`
三個輸入的結果完全一致；用兩個不同答案各跑一段之後，`0xA0000` 以下
只差 60 個位元組，全部是輸入本身（16 位元的值兩份 ＋ 畫出來的字形）
——**答案沒有被存到第三個地方**，所以沒有可以拿來延後懲罰的狀態。
整局也驗過：兩個答案各連走十六個月，十八份盤面逐位元組相同。

所以密碼表**只能從執行檔或資料檔取**——這不是繞過保護，是這份資料
在手上的載體已經失效，而 remake 要重現這個畫面就得有它。
解出來之後放 `docs/formats/`，不進版控（原版資料）。

順帶一提：手冊描述的輸入流程是打四位數 → `Enter` → `Y` 確認。
`docs/re/00` 那個 `AH=08` 迴圈**有可能**是這一關，但目前沒有任何輸出，
所以還是 `L3`——一個會畫密碼畫面的程式不會零繪圖。

### 3.06 郡名已取得

F15 說手冊只有郡編號沒有郡名。郡名已從 `DATA2.GRP` 取出（F19，`docs/formats/02`），
42 個全部合法 Big5、順序由北到南。**沒有一個字是憑印象補的**——
這正是 `CLAUDE.md` §7 第 5 條要防的事。

### 3.1 容器格式已解

`.NAM` 每項 16 byte（DOS 8.3 檔名）、`.IDX` 每項 4 byte 是**結束位移**、
`.GRP` 是資料本體首尾相接。DATA1／2／3 的 `.IDX` 末值精準等於 `.GRP` 長度，
DATA1 的 round-trip 通過（217 項零逆序、無縫覆蓋）。細節與注意事項在
`docs/formats/01-grp-idx-nam.md`。

`DATA0`／`DATA4`／`DATA5` 的 `.GRP` 是 MZ 執行檔，不是資料本體（見 F17–F19）。

---

## 4. 已被推翻的斷言

推翻任何斷言時，把原斷言、當初的證據、推翻的證據一起搬到這裡，
正文改寫成現況——`rulebook/63`。

**`[HARD]` 改正文的範圍是「全文 grep」，不是「我正在看的那一節」。**
把訂正寫進檔案開頭的教訓段、或只改自己剛動過的段落，**單獨讀舊那一節的人
還是拿到錯的規則**。做法：從原斷言挑一個特徵詞（例如「機率不隨等級變」、
「es:[0x3f08] 就是季節」），`grep -rn` 整個 `docs/`，逐一看過再收工。
> 2026-09-08：R26（尋訪的三個常數隨等級變）訂正之後，`70-ai` §2.9 的正文
> 原封不動留著「機率不隨等級變——六個項目都推 10」，訂正只寫在檔案開頭的
> 教訓段。同一天在同一份檔案裡另外兩處也一樣：§2.8 的「忠誠上升多少還沒
> 解」（`20-personnel` §5.2 早有 186 次對拍）、§2.14 的「`es:[0x3f08]` 是不是
> 月份還是假說」（`docs/re/06` 早就寫明是月份）。**三處全在同一份文件**——
> 動過的那一份最容易漏，因為它看起來剛整理過。
> 事後把 R1–R30 的特徵詞全掃過一遍，其餘檔案都是乾淨的。

### R1（2026-09-06）：「`.IDX` 不是單純的位元組位移表」

**原斷言**：`DATA0.GRP` 長度從 42,488 漲到 87,696，而 `DATA0.IDX` 一個位元組都沒變，
所以 `.IDX` 不會是位元組位移表——否則長度變了它必然跟著變。當時列了三個候選
（定長槽／`.GRP` 自帶目錄／`.IDX` 索引的是 `.NAM`），**三個都錯**。

**推翻的證據**：`.IDX` 就是位元組位移表，而且是**結束位移**。DATA1／2／3 三個
獨立檔案的 `.IDX` 末值精準等於各自 `.GRP` 的長度（770,330／1,156,221／1,213,106），
DATA1 的 round-trip 零逆序、無縫覆蓋。

**當初錯在哪**：推論本身沒問題，錯的是**沒驗前提**。那個推論預設了
「`DATA0.GRP` 與 `DATA1.GRP` 是同一種東西」，而 `DATA0.GRP` 開頭是 `MZ`——
它是執行檔，那個槽根本沒有容器可索引。矛盾不在 `.IDX` 的語意，在我把
六個槽當成同一種格式。

**教訓（寫成規則，已進 `docs/formats/01` §5）**：一組看起來同構的檔案，
**先逐檔驗魔數再談共同格式**。命名一致（`DATA<n>.GRP`）不是同構的證據。


### R2（2026-09-06）：「`AH=08` 迴圈可能是密碼輸入」

**原斷言**：`README.DOC` 說密碼只需輸入一次，手冊 p.15 描述打四位數 → Enter → Y，
所以那個 `AH=08` 迴圈可能是密碼關。當時標了 `L3`。

**推翻的證據**：把 98 個可打的鍵一次餵進去，**只有 `1` 與 `2` 被接受並回顯**，
其餘 96 個被吃掉沒有反應；輸入 `2492` 只回顯其中的兩個 `2`。
回顯共三次，走 `int 21h AH=06`。那是**連續三個「1 或 2」的提示**，
不是四位數欄位。

**當初錯在哪**：手冊與 `README.DOC` 都確實講了密碼（那部分沒錯），
但我把「遊戲有密碼」接到「眼前這個迴圈就是密碼」，中間少了一步。
**兩份文件都成立，不代表它們講的是同一個畫面。**

**教訓**：文件說某個機制存在，只證明它存在，不證明它在你現在看的位置。
把 `L3` 標好、用一次可否證的實驗去驗，成本是一次 probe。


### R3（2026-09-06）：「人物表末尾 4 筆是空槽」

**原斷言**：`BASEGEN` 的 350 筆裡只有 346 筆解得出姓名，剩下 4 筆是空槽。

**推翻的證據**：那 4 筆**有內容**。姓名欄是連續的 Big5 碼位 `A141`–`A14C`
（全形標點），其餘 24 byte 也非零，而且四筆在 offset 6–13 完全相同。
是填充槽，不是空槽。

**當初錯在哪**：我的探測腳本用「全部字元落在漢字範圍」當過濾條件，
標點不合格就被算成解不出來，於是「解不出來」被我讀成「沒有內容」。
**過濾器有洞，落空的原因被我當成了結論**——這正是
`~/diagnosis-notes/docs/02-query-returned-empty` 講的四種形狀之一。

**怎麼抓到的**：Go 那邊的測試沒有那個過濾器，直接把四筆的內容印出來了。
兩條路徑用不同的判準讀同一份資料，不一致就會浮出來。

**教訓**：探測腳本的過濾條件要跟著結論一起講。
「346 筆有姓名」與「4 筆是空的」是兩個不同的斷言，前者成立不蘊含後者。

---

### R4（2026-09-06）：「原版從不釋放記憶體，`DATA5.GRP` 那一層是記憶體不夠」

**原斷言**：整趟 `int 21h AH=48h` 十三次、`AH=49h` **零次**，程式握著 373 KB
不放；它要 `0xFFFF` 段、拿到 122 KB 就收工，所以是數量問題。連帶把
`R6009` 與 `TITFONT.IMG` 也歸成同一面牆的三次撞擊。

**推翻的證據**：把 `0583:3068` 反組譯出來看，那個 `AH=48h BX=FFFF` 不是
配置請求，是 **MCB 鏈走訪的起點**——下一道是 `cmp al,7`（只在意「鏈壞了沒」），
接著從自己的 PSP 沿著鏈一格一格爬。原版釋放記憶體也走同一條鏈。
dosgolem 當時只在載入時放了一條寫死的兩格鏈，配置器完全沒有同步它，
所以原版找不到自己的區塊、也算錯可用總量。

把鏈發布出去之後（`dosgolem/docs/spec/012`），同一份輸入的 `AH=49h`
從 0 次變成 10 次，走到的指令數從 2.5 億變成 2.8 億，開過的檔從 6 個
變成 16 個。

**教訓**：**服務呼叫次數量到的是「程式做到了什麼」，不是「程式想做什麼」。**
「一次都沒呼叫」與「呼叫的前置條件不成立」在計數上長得一模一樣。
另外，一個診斷開關能讓症狀消失，不代表它指到了成因——
`DOSGOLEM_FREE_ON_OVERLAY` 做的事（強制標成自由）恰好與真正缺的機制
（讓程式自己找得到區塊去釋放）產生相同的可觀測結果，於是它同時
證實了症狀、掩蓋了成因。

---

### R5（2026-09-07）：「主畫面送什麼鍵都沒反應，卡的是程式自己的邏輯」

**原斷言**：六種送法的畫面差都落在對照組的量級（224–324 像素，對照組 224），
所以鍵雖然進得了遊戲自己的 IRQ1 常式，主畫面卻不動——下一步是反組譯
`DATA5.GRP` 的輸入處理。

**推翻的證據**：兩件事各自就足以推翻它。

- **鍵被收下了。** 按 `9` 之後的記憶體差分裡有 `0x000004 ＝ 0x0a`（掃描碼）
  與 `0x00b09c/0x00b09d ＝ 0x39 0x0a`——**字元 `'9'` 已經翻譯出來存好**。
  一個數字要跟著 `Enter` 才算輸入完（說明書 p.5／p.17），裸送數字只會
  停在輸入欄裡。
- **後面五種送法根本沒送出去。** `machine.Snapshot` 漏存了鍵盤佇列與
  `nextIRQ1`。`Restore` 把 `Steps` 倒回過去而 `nextIRQ1` 留在未來，
  `keyTick` 的「時間還沒到」從此永遠成立。六個變體裡只有第一個是真的在量。

**當初錯在哪**：兩個獨立的量測缺陷指向同一個結論，於是那個結論看起來
被兩邊佐證。判準訂在 200 像素、對照組量到 224——**對照組已經超過門檻**，
那時就該停下來重訂判準，而不是拿它去判其他變體。

**教訓（寫成規則，已進 `docs/re/02` §3.4）**：對照組落在判準的門檻上時，
這個判準當下就失效了，不要拿它去讀其他變體的數字。決定性執行器可以把
雜訊扣乾淨——同一個快照、同樣的指令數，雜訊逐像素重現。
另一條：**從快照展開多個變體之前，先確認快照真的把輸入那條路一起存了**；
沒存的話第一個變體正常、後面每一個都「沒反應」，看起來像那些送法不對。

---

### R6（2026-09-07）：「密碼是 `4029`」

**原斷言**：從「主畫面 → 密碼畫面」之間變動的 3,804 個位址裡挑出 571 個
四位數當候選，第一個候選 `4029` 就讓畫面整個換掉（差 51,783 像素）、
月份從建安二年八月走到九月，所以那是這一局的密碼。

**推翻的證據**：`1111` 與 `0000` 的結果一模一樣——畫面差 51,785 像素、
月份同樣走到九月、畫面內容逐項相同。**這一關擋不住任何東西。**

**當初錯在哪**：判準是「輸入之後畫面有沒有整個換掉」，而那個量在
「答對」與「答錯」上是同一個值。第一個候選就中本來就該起疑——
571 個候選裡第一個命中，先驗機率是 1/571，而「任何輸入都會過」
完全解釋得了它。**先驗機率低的結果要先當成判準有問題，不是當成運氣好。**

**教訓（寫成規則，已進 `docs/re/02` §3.3）**：一個「找出正確答案」的
搜尋，必須同時證明**錯的答案會被拒絕**。負對照與正對照一起跑，
成本是多試兩個值。這是 R5 的同一個形狀第二次出現：對照組與實驗組
量出同一個數字時，那個判準當下就失效了。

---

### R7（2026-09-07）：「季節是三月入春、九月入秋」

**原斷言**：`internal/game` 的 `Season()` 把 3–5 月當春、6–8 月當夏、
9–11 月當秋、12–2 月當冬，年齡在三月增長、秋收在九月、人口每個冬月成長。
當時標 `L2`（說明書寫了、remake 照做了、測試通過了）。

**推翻的證據**：原版主畫面左側直排同時寫年月與季節。三張畫面：
建安二年**八月秋**、建安三年**元月春**、建安三年**四月夏**。
分界照農曆——春 1–3、夏 4–6、秋 7–9、冬 10–12。連走十六個月的觀測
另外釘住兩個年度事件：全部 350 筆的**年齡只在元月**動，
二三十個郡的**人口只在十月**動，兩者都十二個月後再來一次。

**當初錯在哪**：說明書講的是「春夏秋冬各有什麼事件」，沒有給月份分界，
而**那個洞被西曆的常識填掉了**。填進去之後測試全綠——因為測試釘的是
remake 自己的對應表，兩邊是同一個假設。

**教訓**：`L2` 的意思是「remake 自洽」，不是「原版如此」。
一個沒有出處的數字被測試釘住之後看起來就像有出處的。
**填洞的時候要標明那是填的**，不然下一輪讀的人分不出哪些是量出來的。
另外：原版的畫面上常常同時擺著好幾個欄位（這裡是年、月、季節三個），
**一張畫面可以一次驗掉三件事**，比逐項去問便宜得多。

---

### R8（2026-09-07）：「物價跟著地力與洪水率走」

**原斷言**：受控實驗裡地力 10／50／100 對應物價 38／42／52、
洪水率 0／50／100 對應 52／42／42，而金與米怎麼改都不動——
所以物價有一項是郡狀態的函數，remake 的純亂數模型只做對一半。

**推翻的證據**：把地力每十格掃一次（11 個點）之後單調性整個消失：
0→36、10→34、20→40、30→38、40→36、50→31、60→36、70→34、80→40、
90→35、100→36。**民忠**也在跳（0→36、50→31、100→36），而民忠怎麼看
都不該進物價。

**當時給的機制（「那三欄餵災害判定、災害判定位移亂數流」，`L3`）
後來也被推翻**——見 R34：三欄是算式的直接輸入，進的是取餘數的除數。
所以 R8 推翻的「跟著地力單調上升」是對的，補上的解釋是錯的。

判準是**三個基準值等價的變體全部回到同一個值**（地力=100、洪水率=0、
民忠=100 都是該郡原本的值，三個都得到 36）——物價在同一個快照下是
可重現的。現在知道那是因為它們算出同一個除數。

**當初錯在哪**：三個點連成一條單調線，而三個點永遠連得成一條線。
`38/42/52` 看起來太像遞增了，於是我沒問「如果它是雜訊，會長什麼樣」。

**教訓**：**在單調性上下結論之前先把點加密。** 一個「改 X 之後 Y 動了」
的觀測要變成「Y 是 X 的函數」，最少要能排除「Y 對每個變體都在動」——
而那需要一個**不該有影響的變數**當對照（這裡是民忠），不是更多同方向的點。
另一條：受控實驗裡「改了盤面」會透過遊戲自己的亂數消耗傳播到不相干的
欄位，所以**變體之間的差異不等於那個欄位的因果**。

### R9（2026-09-07）：「每郡每月一道令也決定了 AI 的形狀」

**原斷言**：說明書 p.17 的「每郡每月一道命令」是策略層最硬的限制，
**也決定了電腦 AI 的形狀——每個郡挑一件事做，不是排一串**。

**當初的證據**：說明書那條規則本身，加上「玩家與電腦照同一套規則走」
這個沒有寫出來的預設。

**推翻的證據**：原版的指令分派器對**每一個電腦的郡**把十八張表全部
跑一遍（掛住其中九個分派點量到一個月 288 次，÷ 32 個相異的郡 ＝
每郡九次）
（`TestZZTableMeaning`／`TestZZDispatchScope`，`L1`）。時間軸歸屬顯示
同一個月裡同一個郡的地力、訓練度、身分、忠誠都動過，那不是「挑一件
事做」做得出來的。

**當初錯在哪**：把一條**玩家介面的限制**外推成**世界規則**。
說明書寫的是玩家怎麼操作，它沒有義務描述電腦怎麼行動；而
「每郡每月一次」讀起來像後者。

**教訓**：**說明書描述的是玩家看得到的那一面。** 拿它的限制去約束
AI、結算或內部狀態之前，要在原版的碼或行為裡另外找證據——
`CLAUDE.md` §4 把說明書排在第四位，正是為了這種外推。
這一條在 remake 裡不是文件問題而是**會讓 AI 完全動不了**：
`ApplyAll` 遇到 `ErrAlreadyMoved` 會整串中斷，所以電腦諸侯每個郡
只做得成第一道命令，而且不會有任何錯誤浮到畫面上。

### R10（2026-09-07）：「電腦諸侯的分派器有九張表」

**原斷言**：`0x00e8d2` 把等級夾在 0–5，用它索引**九張** far pointer 表，
一張表對應一種行為；`Coverage()` 的分母因此是 9。

**當初的證據**：從 `0xe926` 往下讀，八個 `lcall far [bx+表]` 間隔固定
`0x11`，第九個在 `0xe9fc`。讀到那裡就停了。

**推翻的證據**：拿 `ff 9f`（`lcall far [bx+disp16]`）的位元組樣式掃整個
碼段，分派器裡有 **18 個**，`0xe926` 到 `0xec1b`，之後 `lret`，
中間沒有任何分支。漏掉的九張是 `0x5574`、`0x5514`、`0x55f4`、`0x5654`、
`0x56d4`、`0x56f4`、`0x55b4`、`0x55d4`、`0x54f4`。

**當初錯在哪**：前八個間隔固定，第八個（`0xe99d`）與第九個（`0xe9fc`）
之間隔著一整段預算計算的碼。順著讀的人在那個「節奏斷掉」的地方
自然會認為清單結束了——而那一段正好長得像另一件事的開頭。

**教訓**：**要數「有幾個」的時候用樣式掃，不要順著讀。**
順著讀的終止條件是「看起來結束了」，那是觀感不是判準；
位元組樣式的終止條件是「掃完整個檔案」。
同一件事的另一面：`288 ÷ 32 ＝ 9` 這個量測不是獨立佐證——
那一輪只掛了九個分派點，**量到的是掛了幾個，不是有幾個**。
一個「量出來的數字」與「讀出來的數字」相符時，要先確認它們是不是
同一個來源。

### R11（2026-09-07）：「字串的 DS 位移 ＝ 線性位址 − 0x40000」

**原斷言**：`docs/re/04` 記的字串位址可以減掉 `0x40000` 得到執行期的
DS 位移，拿它去碼段裡找 `mov ax, imm16` 就能定位使用那條字串的碼。

**當初的證據**：一個命中。`0x4717f`（`守軍獲勝`）減 `0x40000` 得
`0x717f`，而碼段裡 `0x1b194` 正好有 `mov ax, 0x717f`。

**推翻的證據**：`docs/re/04` 的開頭寫著「**位移是檔案位移（映像內），
不是執行期 `seg:off`**」。而且直接去讀執行期記憶體的那幾個位址，
`0x46c02`、`0x47176`、`0x488cc` 上都不是字串，是別的資料。
那一個命中是巧合。

**當初錯在哪**：**答案就在我自己的文件裡**，而我沒有先看就從一個
樣本推了一條換算。一個命中在 16 位元的位址空間裡不是證據——
隨便一個 `mov ax, imm16` 都有 1/65536 的機率撞上，而碼段裡有上千個。

**教訓**：**用「單一命中」建立的對應關係，要先算它偶然發生的機率。**
低機率才算證據；`mov ax, imm16` 這種到處都是的樣式，一個命中什麼都
不能說明。另一條是老規則的再次應驗（`CLAUDE.md` §7 第 1 條）：
**動手推導之前先 grep 自己的 `docs/`**——那一份的第 21 行就寫著答案。


### R12（2026-09-07）：「戰術層是 overlay，要在戰鬥中取快照才讀得到」

**原斷言**：主戰場的選單字串在開機後的碼段 dump 裡找不到，
所以那一層是 overlay，要先把原版驅動進一場戰役才載得進來。

**當初的證據**：拿 `docs/re/04` 的 `0x46c02` 一帶去讀執行期記憶體，
讀到的不是字串。

**推翻的證據**：戰鬥的入口 `lcall 2020:0000`（線性 `0x20200`）
落在原本那份 `0x00b000`–`0x050000` 的 dump 裡面，而且帶著標準的
MSC 序幕 `push bp; mov bp,sp; mov ax,0x16; lcall 05c4:051c`，
底下 `0x2053c`、`0x21816`、`0x22444`、`0x22704` 等一整片是實碼。
整層一直都在映像裡。

**當初錯在哪**：那個「找不到」是 R11 的直接後果——用檔案位移去讀
執行期記憶體，本來就會落在別的東西上。**建立在已知錯誤的換算上的
否定結論，不是獨立的證據。**

**教訓**：一條斷言被推翻時，要一併清查**靠它推出來的其他斷言**。
R11 訂正的時候只改了換算本身，沒回頭看「因為找不到字串所以是
overlay」也是同一個前提生出來的。

### R13（2026-09-07）：「誘敵讓來犯敵軍的攻擊力暫時下降」

**原斷言**：誘敵成功之後，目標的攻擊力在幾天內下降（`Unit.Enraged`
＋ `TuneEnragedPenalty`／`TuneLureDays`）。

**當初的證據**：說明書 p.33。碼那一側只讀到「誘敵把事情交給
`0x2a224`，帶一個 8 或 9 的參數」，那一支還沒讀，所以先照手冊。

**推翻的證據**：`0x2b6aa` 播完動畫之後推參數叫 `0x2a224`
（`0x2b877`），而**推的順序把攻守對調**——先推目標、後推施法者，
所以落在「出手的那一方」的是目標。`0x2a224` 是共同的交戰結算，
碼裡沒有任何「暫時狀態」欄位。誘敵因此是**把敵人引過來打你一次**。

順帶解出來的：模式 8 與 9 都被 `0x2a2c9` 夾成 1，所以「施法者謀略
≥ 98 就加碼」在原版**完全沒有作用**（原版的 bug，remake 照它不修）。

**當初錯在哪**：手冊的位階低於執行檔（`CLAUDE.md` §4），而當時把
「還沒讀那一支」當成「可以先用手冊頂著」。頂著本身沒錯——錯在
`L2` 的標記沒有擋住 `Tune*` 常數被造出來，造出來之後就長得像結論。

**教訓**：**手冊與碼衝突時的暫代實作，要連「暫代」一起寫進程式碼的
註解，不是只寫在文件裡。** 一個叫 `TuneEnragedPenalty` 的常數在
`grep Tune` 的清單裡看起來與其他 remake 自選值沒有分別。

### R14（2026-09-07）：「弓箭次數用各單武裝度的**算術**平均」

**原斷言**：`Unit.Arrows` ＝ 各單武裝度的算術平均 ÷ 20，而且
**特地寫了一個測試防止有人改成加權平均**。

**當初的證據**：說明書 p.32 的算例
`(75 + 80 + 50 + 100) ÷ 4 = 76.25`，`76.25 ÷ 20 = 3.8125`，射三次。

**推翻的證據**：`0x2723e`–`0x272f4` 逐一走過十個將領槽，累加的是
`round(武裝度 × 兵士數 × 0.01)`，同時累加兵士數，最後
`round(S ÷ N × 100) ÷ 20`——**兵數加權**。手冊的算例沒有提兵數，
所以那一組數字在兩種算法下分不出來，看起來像算術平均而已。

**當初錯在哪**：拿一個**分辨不出兩種假設**的算例當成支持其中一種的
證據。算例只有在四個人兵數不同時才有鑑別力，而它沒說兵數。

**教訓**：**算例只有在能區分候選假設時才是證據。** 拿它排除另一種
算法之前，先問「如果是另一種，這個算例會不會給出不同的數字」。
答案是「不會」的時候，它一條資訊都沒提供——而當時據此寫下的
「防止有人改成加權平均」的測試，把錯誤鎖了起來。

### R15（2026-09-07）：「人口一年長固定 15%，與土地價值和忠誠無關」

**原斷言**：十月的人口成長是固定 15%、無條件捨去。

**當初的證據**：原版連走十六個月，兩個年度各驗一次，渤海
3,844→4,420、上黨 2,443→2,809、琅邪 2,251→2,588、下邳 2,102→2,417，
四個都剛好是 ×1.15 捨去；低於 15% 的郡都對得上同一個月的徵兵。
標的是 `L1`。

**推翻的證據**：`0x16ec2`–`0x16f16` 讀出來的是

```
人口 ← min(10000, 人口 × (土地價值 + 忠誠 ÷ 2 + 1000) ÷ 1000)
```

倍率上限 1150 ＝ 15%，**所以 15% 是天花板不是常數**。劇本 001 的倍率
只有 1019–1059（2–6%）。

**為什麼觀測會看起來像常數**：對拍走的是「載入舊進度」，而那份出貨
存檔的土地價值是 100、忠誠 99–100——36 個有主的郡裡 30 個倍率剛好
到頂（`TestZZSavedGameLandAndLoyalty`）。**每個郡都貼在上限**，
所以量到的是上限值。

**當初錯在哪**：在一個所有輸入都取到極值的局面上量一條公式，量到的
是那個極值，不是公式。而「四個郡、兩個年度都一致」讓它看起來像是
交叉驗證過了——**同一個局面重複量四次不是四個獨立樣本**。

**教訓**：**量公式之前先看輸入有沒有變異。** 輸入沒有變異的時候，
再多的樣本也分不出「常數」與「碰巧都取到同一個值的函數」。
這件事在「載入舊進度」這種現成局面上特別容易發生——出貨存檔常常是
內政做滿的狀態。要量係數就自己擺盤面
（使用者的指示：對拍直接設定記憶體，不要依賴 `RND()`）。

### R16（2026-09-07）：「`es:[0x3f08]` 是季節（0–3），四季常式每個月都跑」

**原斷言**：四季分派器讀的那一格是季節編號，remake 照 `Season()`
把十二個月分成四段，每個月跑對應的季節常式。

**當初的證據**：`0x15c29` 讀它、`0x15c2d` 跳到一張四個目標的跳躍表，
四個目標的內容正好是春夏秋冬四組事件。四個目標對四個季節，看起來
沒有別的可能。

**推翻的證據**：跳躍表本身（`0x15c48`）比的是 **1、4、7、10**：

```
cmp ax,10 je → 冬 ; ja → retf
dec al    je → 春  ; al == 1
sub al,3  je → 夏  ; al == 4
sub al,3  je → 秋  ; al == 7
retf                ; 其餘八個月什麼都不跑
```

那一格是**月份**，而四季常式**一年各只跑一次**。

**怎麼發現的**：想替蝗害加樣本，把那一格寫成 2（「秋」）之後四支常式
一支都沒跑——**寫進去卻什麼都沒發生**，比讀出來的值更早暴露問題。

**影響**：remake 原本每個月都跑季節常式，地震、水災、瘟疫、蝗害
一年各有十二次機會而不是一次（**十二倍**）。秋收與進貢的月份也不是
remake 挑得動的——它們就在秋季與冬季常式裡，所以是七月與十月，
原本挑的九月與十二月落在四支常式都不跑的月份上。

**教訓**：**跳躍表要讀判斷式本身，不要從目標的內容反推索引的語意。**
四個目標對四個季節是個太順的故事，順到不必去看那五行比較。

### R17（2026-09-07）：「買米的匯率是 `(100 − 物價) ÷ 10`」

**原斷言**：一金買到 `(100 − 物價) ÷ 10` 單位米，玩家與電腦共用。

**當初的證據**：電腦買米那一支（`0xc634`）的呼叫端讀物價、`sub 100`、
`neg`、`idiv 10`，然後把商數推進去。玩家那一條（`0x1b20e`）算的也是
`÷ 10`，兩邊一致，看起來沒有別的可能。

**推翻的證據**：實跑 106 次買米，**93 次對不上**。回頭看才發現
`0xc634` 有**六個呼叫端**（`0x0c7d1`、`0x0c805`、`0x0c839`、`0x0c86d`、
`0x0c8a1`、`0x0c8dd`），六支的除數是 `[10, 10, 10, 10, 9, 8]`。
當初只讀了第一個呼叫端。

**怎麼發現的**：對拍。同一個物價（44）在不同次買米得到 6 與 7 兩種匯率
——**一個應該是純函數的量給出兩個答案**，那就不是純函數。

**教訓**：**看到「六支同樣的碼」就要比對全部六支，不能只讀第一支。**
內政那張表（`docs/mechanics/70-ai` §2.5）早就是這個形狀，而且也踩過
一次「以為只有一個常數不同、其實有三個」。這一次是同一個坑的第三面：
連呼叫端都可能有六份。

**第二層**：改成六個除數之後，等級 4 的 14 次全對、等級 5 的 83 次仍
全錯，而且實測的匯率一律等於 `(100 − 物價) ÷ 8`。第六支的
`mov cx,3` 後面接的不是 `idiv` 而是 `sar cl,ax`（`0xc8d6`），
那個 3 是**位移量**，除數是 `2³ ＝ 8`。

> **規則**：`mov cx,N` 後面要看下一道指令才知道 N 是什麼。
> 接 `idiv` 時 N 是除數，接 `sar`／`shr` 時 N 是位移量、除數是 `2ᴺ`。
> 前後那兩對 `cwd` ／ `xor dx,ax` ／ `sub dx,ax` 是編譯器對負數的
> 捨入修正，出現它們就表示這是**除法被換成移位**，不是位元運算。

### R18（2026-09-07）：「州郡 offset 35 與有主／無主一致」

**原斷言**：offset 35 的 24／18 分群與「有主／無主」一致。

**當初的證據**：劇本 001 有 24 個有主的郡、18 個無主的郡，而 offset 35
剛好也是 24 個 11、18 個 12。

**推翻的證據**：逐郡對，不是逐群數對。劇本 001 裡 11 的那一群有 17 個
有主、7 個無主，12 的那一群有 7 個有主、11 個無主——**個數相同，
成員完全不同**。而且 offset 35 在六個劇本、十二個存檔槽、兩個版本裡
一個位元組都不差，有主／無主每一份都不一樣。

**它其實是什麼**：南北分群。11 是長江以北的 24 個郡，12 是以南的 18 個；
荊州與益州各自被切開，揚州與交州整個在南（`docs/spec/003` §3.8）。

**教訓**：**兩個分群的個數相同不表示分群相同。** 對照要逐項比，
不能比總數——尤其在小樣本裡，24／18 這種切分撞在一起並不罕見。
順帶一條：「沒有 `disp16` 形式的讀寫」這個結論本身要先做正對照，
拿一個**已知會被讀的**位移（這裡是 offset 34）跑同一支掃描，
確認掃得到，再說另一個掃不到代表沒有。

### R19（2026-09-07）：「統一天下要拿到玉璽」

**原斷言**：勝利條件是「擁有全部有主的郡」**而且**「手上有玉璽」。
佔滿地圖但沒有玉璽只回報「還要再等數月」。

**當初的證據**：說明書 p.37「在遊戲結束前一定要拿到玉璽，如果屆時玉璽
尚未到手，便須再多等候數月才能看到君臨天下的結局」。等級記的是 `L2`。

**推翻的證據**：統一判定整支讀出來了（`0x15852`）。它逐郡掃兩遍所屬，
全部有主的郡同屬一方就印「%s 一統天下」並結束——**除了郡的所屬與
君主的姓名以外沒有讀任何東西**，沒有碰寶庫。

**怎麼發現的**：從字串找。`DS:0x66d4` 是「%s 一統天下」，整份碼只有
`0x158ec` 一處引用，往回讀就是整支判定。

**教訓**：**`L2` 的意思是「說明書寫了、remake 照做了、測試通過了」，
不是「原版如此」。** 位階表（`CLAUDE.md` §4）把說明書排在執行檔之下
就是為了這種情形——1991 年的說明書與出貨版本不一定一致。
`L2` 的條目要當成待驗證項目排進工作表，不是當成已知。

順帶把 R18 的掃描教訓補齊：**找一個 16 位元立即數的引用時要把 opcode
一起錨住**（`68 lo hi` ＝ `push imm16`、`b8 lo hi` ＝ `mov ax,imm16`），
只搜那兩個位元組會撞進別的指令中間。這一次四個「引用」裡有三個是
`jl` 的 rel8 剛好等於高位元組。

### R20（2026-09-07）：「`WFLAGD*` 是攻方的旗，守方的標記不在容器裡」

**原斷言**：紮完寨的基準畫面上，`WFLAGD00`–`D03` 四張各有一處 100%
相符，判成主攻軍的四支部隊；而 `DATA1` 的 190 張與 `DATA3` 的 334 張
整張畫面掃過都沒有第五處 100%，於是記成「守軍的標記不是 `WFLAG`，
要往別處找」。當時另外記著「旗的編號是照隊伍還是照紮營先後分不出來」。

**推翻的證據**：三件事一起翻。

1. **`D` 是守、`A` 是攻。** 那四面綠旗的兵力牌是 2673／1500／2227／1500，
   合計 7900 ＝ 右側面板「周瑜軍　主守軍　四軍 4將　兵 7900」；畫面上
   另一個牌 3000 ＝ 左側「陳就軍　主攻軍　一軍 1將」，用的是 `A0`。
2. **守方的標記就在 `DATA1` 裡**，只是畫成**反白**——`WFLAGA00` 每格
   顏色取補數（`^15`）之後，與畫面上 (352,228) 那一塊 360 格逐格相同。
   呼叫端把反白旗標打開畫一次、關掉再畫一次（`0x275b9`–`0x275ec`），
   基準畫面剛好停在亮的那一格。
3. **旗的編號照隊伍不照紮營先後**：把二十四面旗放大讀字模，旗面上印的
   是**帥、先、左、右、後**，第六張是城門圖示。字本身就是答案。

**怎麼發現的**：先把二十四張旗排成一張放大圖去認旗面上的字（本來只是
想確認索引），字一讀出來，「編號照什麼」當場就沒了問題；接著發現守方
那塊的顏色與 `A0` 是位元補數關係，一驗 360 格全中。

**教訓**：**逐像素掃描只在顏色沒被動過時有效。** 掃不到不等於不存在——
反白、調色盤重映、半透明疊色都會讓原色比對全滅，而輸出看起來就是
乾淨的「一處都沒有」。掃描落空時要多試一次「顏色被變換過」的版本
（先試補數），再下「不在容器裡」的結論。
另外，**美術資產自己會說話**：旗上印著字、圖示畫著城門，放大看一眼
就省掉一輪反組譯。

### R21（2026-09-07）：「`WEATHER0-2.IMG` 依序是晴、刮風、下雨」

**原斷言**：`docs/re/04` 記著三張天氣圖示「對應戰場的三種天氣
（晴／刮風／下雨）」。當時只看到檔名剛好三個，順序是照說明書講到的
次序排的，沒有查證。

**推翻的證據**：原版的順序是**晴 0、雨 1、風 2**。天氣名表在
`DS:0x7892`，三筆每筆 5 byte：「 晴 」「 雨 」「 風 」。畫面左欄的圖示
編號是 `237 + 天氣 mod 3`（`0x21969`），而基準畫面上那一張是
`WEATHER1`，同一張畫面左欄第三個框裡的字正是「雨」——圖與字互相印證。

**影響**：`internal/battle` 的列舉照說明書排（`Clear`、`Windy`、`Rainy`），
規則層自成一套沒有問題，但**對回原版編號時要換算**
（`battle.Weather.OriginalIndex`）。畫面若拿列舉值直接去索引
`WEATHER%d.IMG`，下雨會畫成刮風——而兩張圖都看得懂，不會有人發現。

**教訓**：**「數量剛好對得上」不是順序的證據。** 三個檔名配三種天氣，
配對方式有六種；當時記下的那一種來自說明書的行文次序，不是資料。
資料裡本來就有一張名字表，查一次就定了。

### R22（2026-09-08）：「電腦諸侯的計略下的是策反人民的五刀」

**原斷言**：`docs/mechanics/30` 與 `70-ai` §2.13.7 記著電腦得手之後呼叫
`0x2d6e0`（民眾忠誠、洪水率、土地價值、米、金五刀），而且「`0x2d6e0`
的兩個呼叫端分別在策反人民與電腦的計略，所以那五刀確定掛在策反人民
底下」。當時的證據是 `0xe618` 那道 `call 0xe7aa`，以及 `0xe7aa` 裡
確實有一個 `lcall 0x2c21:0x14d0`（＝ `0x2d6e0`）。

**推翻的證據**：`0xe7aa` **執行不到**。它前面那一支 `0xe68e` 不是
「建候選表」的輔助常式，而是一整條計略——挑目標、選使者、判成敗、
下手全在裡面，尾巴是 `0xe7a3: sub ax,ax; lret`，**兩條出口都回 0**。
所以 `0xe614` 的 `or ax,ax` 之後那道 `je 0xe60c` 永遠成立，
`0xe618` 一次都執行不到。三個等級（`0xe4a2`／`0xe522`／`0xe5a2`）
寫法一模一樣。電腦真正呼叫的效果是 `0xe79e` 的 `0x2c21:0x0fea`
（＝ `0x2d1fa`，**偽書使疑**）。

`0xe7aa` 與 `0xe68e` 的前 `0x111` 個位元組逐位元組相同，唯一的差別
就是那個 far call 的位移——看起來是複製一份改掉效果，但接線只接了
第一份。

**影響**：`internal/ai/ai.go` 的 `plot` 原本回 `game.PlotIncite`，
改成 `game.PlotForgery`；`internal/parity/plot_oracle_test.go` 前四輪
掛在 `0x2d6e0`，那是電腦到不了的地方，所以「得手零次」不是觸發條件
沒滿足，是斷點掛錯。**策反人民只有玩家下得了。**

**教訓**：**「這支常式被誰呼叫」要連著呼叫端的回傳值一起看。**
當初查 xref 查到了 `0xe8ba`，也讀了 `0xe7aa` 的內容，唯獨沒問
「上一個 `call` 回什麼」——一支固定回 0 的常式，接在它後面的
`or ax,ax; je` 就是一道永遠關著的門。靜態 xref 只證明**位址被寫進
程式碼裡**，不證明那條路走得到。

### R23（2026-09-08）：「開場的三英圖不在容器裡」

**原斷言**：`docs/formats/01` §4.1 記著「原版開場第五格那張三英圖，拿
`DATA1`／`DATA3` 所有大圖去比（含色盤無關的比法）一個都不中，它應該
就是 `DATA0.NAM` 那 31 個找不到的名字之一，而資料不在容器裡」。

**推翻的證據**：它是 `DATA1` 的 `TITL0`–`TITL3`，四塊 160×400 **並排**
放在 x ＝ 0／160／320／480，縱向取第 0–349 列，與原版那一格的畫面
224,000 格逐格相同（`TestTitleArtLayoutMatchesTheOriginal`）。

先問原版自己要什麼：`TraceFiles` 把整段開機的讀取序列倒出來，
第 1.9 億條指令附近讀了 `TITL0`–`TITL3`，正好在第三格與第四格之間。

**影響**：`assets.TitleArt` 拼得出整張，`cmd/san1` 的開場變成
三英圖 → 開場詞 → 主選單。同一批讀取序列還一次解掉四個名字：
`TZUE`／`TZUE1` 是〈臨江仙〉的詞、`TITFONT` 是「三國演義」四個字、
`PRV0`–`PRV2` 是工作人員字幕、`CMARKL`／`CMARKR` 是智冠的商標畫面。

**教訓**：**「整張比」對切成幾塊的圖結構上無效。** 原斷言的「最高不到
70%」不是「找不到」，是**比法本身答不出這個問題**——四塊各佔四分之一，
單塊與整張的最高相似度天花板就是 25% 加背景色碰巧相同的部分。
落空之前要先問「這個比法能不能回答這個問題」。

另一半是**問錯對象**：開場十格的畫面裡只有第 5 格在呼叫載圖常式，
前五格一次都沒有——因為所有的圖都在開機那 5,000 萬條指令裡一次讀完，
之後只是從記憶體貼。**「畫出來的那一刻在讀什麼」與「這張圖從哪裡來」
不是同一個問題。**

### R24（2026-09-08）：「徵兵的稀釋就是加權平均」

**原斷言**：`10-strategy` §5 記著「新兵沒有訓練與武裝，加入會把全隊的
訓練度與武裝度拉低……訓練度那一半 remake 照同樣的形狀處理（加權平均，
新兵為 0）」，實作是 `(訓練 × 兵力) ÷ 新兵力` 與
`ArmsOf(Weapons(武裝, 兵力), 新兵力)`。

**推翻的證據**：597 次徵兵的對拍。原版是**兩層截斷**，而且還原成
「有訓練／有武器的人數」那一步乘的是 **float32 的 0.01**
（`fmuls DS:0xa5d0` ＝ `0.009999999776482582`）：

```
人數 ＝ trunc(舊值 × 舊兵力 × 0.01f)      ; 整除的時候少 1
新值 ＝ trunc(人數 × 100 ÷ 新兵力)
```

武裝度 100、兵 20 還原出來是 19 件武器不是 20 件。訓練度 80、兵 100
徵 100 人之後是 **39** 不是 40。

**買武器那一支（`0xc168`）乘的是 float64 的 `0.01`**（`DS:0xa5c8`），
整除不會少——兩條路的常數不同，`game.Weapons` 與 `game.WeaponsF32`
因此分成兩支。玩家的徵兵（`0x19b99`）與電腦走同一個 float32 常數。

**影響**：`game.Conscript` 改用 `DiluteAfterRecruit`；
`TestConscriptLowersTraining` 的期望值從 40／30 改成 39／29。

**教訓**：**浮點不是留容差的理由，是要把浮點也一起模型化的理由。**
第一版對拍留了 ±1 的容差，理由是「原版走浮點」——結果 597 次裡 76 項
超出容差，而**沒超出的那些也不代表對**：真正的算式與加權平均相差
一到兩格，容差正好蓋在那個尺度上。容差擋得住隨機雜訊，擋不住系統性
偏差，而 1991 年的定點流程裡沒有隨機雜訊。

### R25（2026-09-08）：「調整兵力的份額是整數的四捨五入」

**原斷言**：`game.Redistribute` 用 `(總兵力 × 上限 × 2 ÷ 總上限 ＋ 1) ÷ 2`
算份額，註解寫「原版 `0xc2c4` 在轉回整數之前加 `ds:[0xa5de]` ＝ 0.5」。
另外還有一段「把上限擋住的剩餘塞回還有空間的人」的迴圈。

**推翻的證據**：279 人的對拍裡有 4 個對不上，全是同一個形狀——
上限 2000、總兵 12565、總上限 28000，真值剛好 897.5，**原版給 897**。

原版先把「總兵力 ÷ 總上限」存成一個 **double**（0.44875 的最近 double
比真值小 1.8e-17），再用 80 位元的浮點乘回上限、加 0.5、截斷，
乘積落在 898 下面。用 float64 重算反而會錯：float64 把乘積捨入成剛好
897.5，加 0.5 就進位成 898。

「剩餘塞回」那段原版沒有——它寫完一輪就結束（`0xc4f4` 之後直接跳回
迴圈頭），註解自己也寫著「原版不做校正」。

**影響**：新增 `game.TroopShare`（用精確有理數重現那兩步），
`Redistribute` 改用它並拿掉塞回迴圈。

**教訓**：**要重現的是原版的運算順序與每一步的精度，不是最後那個數。**
「先捨入成 double、再用更高精度算下去」是 x87 程式的常見形狀；
拿 float64 一路算到底，會在剛好落在 .5 的案例上系統性地差一格，
而那種案例少到可以在小樣本裡完全不出現。

### R26（2026-09-08）：「尋訪人才的機率與門檻不隨等級變」

**原斷言**：`internal/ai/ai.go` 記著「尋訪人才：`RND(10) > 7`，也就是
20 %。**機率不隨等級變**（六個項目都推 10）」，`rules.go` 則把門檻寫成
兩個常數 `SearchIntelFloor = 30`／`SearchIntelSpread = 65`。
`game.Search` 甚至沒用那條公式，用的是 remake 自己的 `TuneSearchIntel`。

**推翻的證據**：六支分派常式（`0xcd20` 起，間隔 `0x3a`）形狀相同，
而**三個立即數全部隨等級變**：

| 等級 | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| `RND(10) >` | 7 | 7 | 7 | 6 | 5 | 4 |
| 寬度 | 65 | 65 | 65 | 45 | 20 | 20 |
| 底 | 30 | 30 | 30 | 30 | 30 | 15 |

33 次對拍逐次相同（`docs/playtest/02`）。等級 0 的門檻是 30–94，
等級 5 是 15–34——高等級的電腦幾乎每次尋訪都找得到人。

**影響**：新增 `game.SearchTierFor`，`ai.go` 與 `game.Search` 都改用它；
`TuneSearchIntel` 退場。

**教訓**：**「`RND(10)` 的 10 不變」不等於「機率不變」。** 變的是跟它比
的那個數，而那個數在另一道指令上。「同一支常式配不同常數」的分派表要
**六支都反組譯，把每一支的立即數列成一張表再比**——只讀第一支會同時
漏掉三張表裡的兩張半，而漏掉的部分在行為上看起來只是「高等級比較強」。

### R27（2026-09-08）：「出兵編隊挑中的那一批就是出征的部隊」

**原斷言**：`docs/mechanics/70-ai` §2.13.6 與 `ai.muster` 都寫著
「守軍清單洗牌之後從尾端往前**挑人**，累計兵力到 `100 × es:[0x2e62]`
為止；挑中的從清單裡拿掉」，並把 `es:[0x2e62]` 說成「出征的規模至少要
追上最強的敵鄰郡」。當初的證據是 `0xb2b4` 的累計迴圈——那一段確實在
從尾端往前走、填 `0xFFFF`、累計兵力。

**推翻的證據**：填 `0xFFFF` 的那一批是**留守**，清單截斷之後**留下來的
頭段才是出征的部隊**。原版接著只掃 `0..長度−1` 求和（`0xb3a8`），那個
和除以 100 才是 `es:[0x3c96]`；`es:[0x3c96]` 是拿去和目標郡的兵士比、
拿去按比例算帶走錢糧的那個數。三處旁證：50 人上限裁的是留下來的頭段，
而它比的是**目標郡**的現役將（`0xb7e1`）；`0xb706` 的迴圈用 `jg` 讓位置
0 永遠留著；原版關掉出兵的三個地方（`0xf0a9` 等）把目標設成 9999，配的
是 `es:[0x1600] = 0xFFFF` 這個作廢標記——「送全部人出去」不會配作廢
標記，「全部留在家裡」才會。61 次對拍逐次相同。

順帶兩件：`es:[0x2e62]` 的**單位是「百」**（`0xedd2` 有一道 `idiv 100`），
起始值是 5（`0xeccb`）；帶走的錢糧是 `金 ÷ 兵士（百） × 出征兵力（百）`
的**浮點**式子，原本寫成整數的 `金 ÷ 兵士 × 兵力`。

**影響**：`ai.muster` 改回傳留下來的頭段並加 `keepOne`（兩支的唯一差
別）；`ai.sortie` 改成編兩次隊；`sortieTarget` 改成「百」並補起始值 5；
新增 `game.SortieShare`／`game.SortieThreshold`。

**教訓**：**累計迴圈的語意由「誰去讀那個結果」決定，不由迴圈自己決定。**
「從尾端拿人累計到夠」這段碼兩種讀法都自洽，分不出來的時候不要挑順眼
的那個——往下找求和點、找上限裁在誰身上、找作廢路徑把哨兵值設成多少。
另一條：**清單在中途被洗過，進場拍的快照就不是出場那個排列**；拿進場
的前 N 筆去比會得到「多數相符、少數不符」，而那個形狀看起來像雜訊，
不像判準搞錯（91 次裡 80 次「相符」其實是兩邊都收成 0）。

### R28（2026-09-08）：「指定太守跳過的是君主所在的郡」

**原斷言**：`docs/mechanics/70-ai` §2.6 的虛擬碼只有「玩家控制的勢力 →
什麼都不做」，remake 這一邊 `ai.mostCharming` 與 `game.AppointGovernor`
都寫成「君主人在這個郡就不指太守」。當初的證據是 remake 自己的命令守則
擋下來之後 `ApplyAll` 會中斷，於是往「原版大概也不指」的方向補了條件。

**推翻的證據**：`0xd652` 判的是**州郡 offset 32（主事者）指到的那一位的
身分是不是 0**，而且用的是排序之前的舊值（`0xd6b3`）。主事者是 `0xFFFF`
時**不跳過**。君主在郡裡但主事者是別人時，原版照指不誤。

同一支常式另外兩件也沒記到：排序吃的名單是 `buildRoster(郡, 2)`，
**不比對勢力**；最後一步 `0xd74d` 把**州郡 offset 30（所屬）改成新太守的
勢力**——所以魅力最高的是站在郡裡的外人時，這個郡當場易主。

**影響**：§2.6 照碼重寫。實作還沒改——`ai.mostCharming`、
`game.AppointGovernor` 三處條件都要一起動，而單獨把閘門改成
「他已經是太守了嗎」量出來更差（身分差 14 → 18），所以要連著上游一起改
再量。列在 worklist。

**教訓**：**為了讓自己的命令層過關而補的條件，會偽裝成原版的規則。**
`mostCharming` 那道「君主在就不指」的註解寫的是「`game.AppointGovernor`
擋這一種」——理由是 remake 自己的守則，不是原版的行為，可是它躺在
`ai` 裡看起來就像還原出來的判斷。這種條件要在註解裡標明**是誰的規則**，
並且在機制文件裡留一條「原版怎麼做」的對照。

### R29（2026-09-08）：「電腦戰役的兩個戰力欄戰鬥期間不變」

**原斷言**：`es:[0x9ce]`／`es:[0x1608]` 是開場兵力，戰鬥期間不變，所以
`0x1f538` 每天扣的是「開場兵力的 1 %」。當初的證據是**全碼段搜尋
`fstpl [0x9ce]`／`fstps [0x9ce]` 只搜到 `0x1E908` 的四個累計迴圈**。

**推翻的證據**：那兩個值在 `0x1f683` 與 `0x1f691` 被 `movsw` **整塊複製**
更新（八個位元組、四道 `movsw`），不經過 x87 的儲存指令。搜尋條件只列了
浮點儲存，漏掉整塊複製這一種寫法。實際的規則是每天互相削減：
`新守方 = max(0, 守 − 攻 × 攻方品質)`、`新攻方 = max(0, 攻 − 守 × 守方品質)`，
兩個新值都用舊值算。

順帶兩件同一次讀出來的：折損軍團兵力**每三天才做一次**（`0x1f55d` 的
`天數 % 3`），扣的是**當下戰力**的 1 %。

**影響**：`docs/re/05` §7.1 的結算段整段改寫。

**教訓**：**「只有這幾處寫」的結論，取決於搜尋條件涵蓋幾種寫法。**
x86 寫記憶體的方式不只 `mov`／`fstp`——`movsw`／`stosw` 的整塊複製、
`push`/`pop` 到堆疊之外的段、`rep` 前綴都會被逐指令的樣式搜尋漏掉。
下「只有 N 處寫」這種結論之前，**先問「我列的樣式涵蓋了哪些寫法」**，
或者改用執行期的寫入監看（dosgolem 攔位址）取代靜態樣式搜尋。
與「查詢回空要先做正對照」是同一族：**過濾器有洞，回空看起來像不存在。**

### R30（2026-09-08）：「軍團記錄 offset 10 是將領人數、12 是部隊數」

**原斷言**：`docs/re/05` §3.3 的軍團記錄欄位表把 offset 10 標成「將領人數」、
offset 12 標成「部隊數」。支持它的靜態線索是建軍團那一支（`0x21087`）在
分配迴圈**之前**寫 +10、**之後**寫 +12——看起來像「先知道有幾位將領，
分完才知道編成幾支部隊」。

**推翻的證據**：兩格同時讀，19 次取樣得到 (10, 12) ＝ (1,1)、(2,2)、
(5,7)、(5,10)。**一個軍團最多五支部隊**（§5，五個部隊記錄），所以 7 與 10
不可能是部隊數；而 offset 10 從來沒超過 5。`0x1E908` 拿 offset 10 當 42 byte
部隊記錄的迴圈上界，這樣才說得通。

**影響**：§3.3 的兩列對調並標 `L1`；§7.1 的「平均品質」分母確定是部隊數。

**教訓**：**「寫入順序」推不出欄位語意。** 先寫的不一定是先知道的——
一支常式完全可以先填一個上限、迴圈跑完再回填實際用掉幾格。這一次的正解
來自**值域**（部隊數有上限 5，將領人數沒有），不是來自控制流。
欄位語意優先用「值域 ＋ 誰拿它當索引或上界」判，不要用寫入順序判。

### R31（2026-09-08）：「月度對拍裡 remake 的主事者停在人物表末端的填充區」

**原斷言**：州郡 offset 32 對不上的兩個郡，原版是槽 53／65（真的武將），
remake 是槽 336／342；人物表末尾 4 筆是填充（`docs/formats/03`），所以
remake 看起來是把主事者停在一個不該存在的槽上。

**推翻的證據**：把那四個槽印出來，336 ＝ 傅幹、342 ＝ 袁胤，兩位都有名字、
能力值、身分與所在郡都正常，而且都真的站在該郡裡。真正的填充筆長這樣：
`槽 349 名=︰…‥ 身分=12 勢力=255`。350 個槽裡 346 個是人，填充是
**346–349** 這四筆。

**影響**：成因改判為 `game.State.Governor()` 比對勢力導致重推
（`docs/playtest/02`「主事者為什麼會分岔」）。

**教訓**：**「末尾幾筆是填充」不等於「號碼大的是填充」。** 邊界是 346，
槽 336 離它還有十筆。判一筆是不是填充要**把那一筆印出來看**，
不要拿「號碼看起來很大」代替。同一個形狀在別處也成立：哨兵與填充都要
用內容認，不用位置認。

### R32（2026-09-08）：「訓練與武裝各 76 筆是批次套用造成的」

**原斷言**：`VERIFICATION-MATRIX` 把「訓練、武裝各 76 筆而原版只動 29 筆」
歸因於結構差異——原版的分派器逐郡即時執行、前一張的結果會改變後一張讀到
的盤面，而 remake 先把整個勢力的十八張表排完再一次套上去。

**推翻的證據**：把 remake 改成**發一道套一道**（`ai.Brain.Act`，
`planIn` 的 `emit` 當場 `o.Apply`）之後，這兩欄各變成 **77** 筆，
總數 439 → 444。**方向相反**，所以批次套用不是那 76 筆的成因。
成因是「哪幾個郡做了哪件事」的亂數分岔——兩邊各用自己的亂數，
一個郡的兵力先差一點，調整兵力就把整郡的訓練與武裝拉到不同的平均值。

**影響**：`ai.Brain.Act` 留著（原版的分派器確實是即時執行的，這一點是
`L0`，與量出來的位元組數無關）；`VERIFICATION-MATRIX` 那一列改寫。

**教訓**：**位元組總數在這個量級是被連鎖主導的，不是缺陷計數。**
判準要落在**離散欄位**——誰是主事者、誰的身分變了、誰移動了、哪個郡的
在職將變了——那幾欄一次只由一件事決定，改對了會整格消失。同一輪的四次
改動裡，離散欄位只在第一次動過，其餘三次全部落在連鎖欄位上。
用總數當判準會把「對的修法」判成回歸，也會把「什麼都沒修好」判成進步。

### R33（2026-09-08）：「月度對拍比的是同一個月」

**原斷言**：`TestZZMonthParity` 讓原版走一個月、remake 走同一個月，然後
比三張表——差多少就是規則還沒對齊多少。

**推翻的證據**：掛 `0x1746e`（每個郡的回合入口）數走到幾個郡，得到
**37**，不是 42。順序表讀出來是 43 格洗過的排列，**玩家的郡排在第 37 格**
——`0x17471` 走到玩家的郡就不跑分派器，接著進主命令提示等輸入，游標留在
那裡。所以那一段是「玩家回合 → 排在玩家前面的 37 個郡 → 又輪到玩家」，
排在後面的 `13 20 9 37 42` 五個郡完全沒動。而 remake 那一邊 42 個郡全跑。

排除過的解釋：旗標陣列在出發時是乾淨的（只有郡 0 是 0）；月份確實跑完
（清鍵盤續跑沒有新的郡）；那五個郡不是無主、不是自治、所屬重算後仍是
電腦。**都不是**——是視窗。

**影響**：對拍改成照同一個視窗（讀原版的順序表，找到玩家那一格，
remake 只跑前面的郡）。**436 → 385 個位元組**，訓練與武裝各由 77 降到
61、兵力 80 → 68、身分 4 → 2。先前所有的位元組數都偏高。

**教訓**：**「兩邊各走一個月」要先證明兩邊真的各走了一個月。**
量測的視窗本身是一個假設，而它不會自己報錯——多做的那五個郡看起來
只是「規則還沒對齊」。**判準要包含「這一輪各自做了幾件事」**，不能只
比結果；`t.Logf` 一行「走到 37 個郡」就足以在第一天發現。

### R34（2026-09-08）：「物價是純亂數，郡的狀態不進算式」

**原斷言**：受控實驗一次只改一個欄位，地力／洪水率／民忠三欄都讓物價
亂跳而沒有單調性，金與米完全不動它。結論是「物價是兩個 `RND(20)` 相加，
郡的狀態不是輸入；跳動來自那三欄餵災害判定、災害判定位移了亂數流」。

**推翻的證據**：開月常式 `0x17364` 的迴圈（`0x17396`–`0x173ea`，`L0`）：

```
cx  = RND(20)
基  = 民眾忠誠 − 洪水率÷2 + 土地價值
物價 = RND(基) ÷ 10 + cx + 30
```

三欄確實是輸入，只是進的是**除數**不是加數；除以 10 之後值域和
`RND(20)` 同一個量級，餘數對除數又不單調，所以掃單一欄位看到的就是
「亂跳」。金與米不動是因為它們根本不在算式裡，不需要繞到災害判定。
上限 30＋19＋19 ＝ 68 與量到的範圍相符。

**影響**：`repriceAll` 改成照這條算，物價那 42 筆位元組差消失；
`60-economy` §1.2 由 `L1`／`L3` 升為 `L0`，§6 的「物價怎麼算」結案。

**教訓**：**「改 X 之後 Y 亂跳」不能推出「X 不是 Y 的輸入」。**
X 進的若是取餘數的除數（或任何非單調的位置），單欄掃描看到的就是雜訊。
分辨方法是量**抽亂數的次數**：真的是「位移亂數流」的話次數會變，
這裡兩邊都是每郡兩次——次數沒變就不是位移，是同一次抽樣的參數變了。


### R35（2026-09-08）：「內政那張表擲一次骰、三個分支」

**原斷言**：`0x5534` 的六份常式是 `r = RND(K)`；`r == 0` 開墾、
`r == 1` 防洪、其餘不做。等級 5 的 `K` ＝ 2，所以「兩件事各半、從不閒著」。

**當初的證據**：讓原版跑四個月、58 次內政，把傳給寫回常式 `0xba02`
的量與 remake 的公式逐次比，沒有一次不同。

**推翻的證據**：六份常式逐條讀（`0xba9c`／`0xbb12`／`0xbb88`／`0xbbfe`／
`0xbc74`／`0xbcea`，`L0`）——**它擲兩次**：

```
r1 = RND(K)；r1 == 0 → 開墾，jne 跳過第二次
否則 r2 = RND(K)；r2 == 1 → 防洪，否則不做
```

機率因此是開墾 `1/K`、防洪 `(1−1/K)/K`。等級 5 是開墾 ½、防洪 ¼、
閒著 ¼——**不是各半，也不是從不閒著**。

寫回那一支還有一道（`0xba02`）：`量 > 0` 直接用，**只有非正時才擲
`RND(2)`**。remake 原本無條件擲，智力高的太守會多消耗一次。

**怎麼發現的**：月度對拍逐郡逐表拆帳。順序表第 0 格（郡 1）原版抽 25 次、
remake 21 次，拆開之後差在內政 2／1。兩邊在那之前的位置相同、`K` 也
相同（trace 印出 `K 2 擲 1`），所以只能是常式的形狀不同。

**教訓**：**「量對得上」不等於「形狀對得上」。** 那次 58 次的對拍比的是
傳進寫回常式的**數值**，看不到擲了幾次骰——第二次 `RND(K)` 在數值上
完全隱形。要抓形狀得數**抽樣次數**。


### R36（2026-09-09）：「戰術層的殺傷是 remake 拿部隊平均合成的」

**原斷言**：原版對每一位將領各算一個戰力值，remake 這一層算的是部隊，
所以拿加權平均代入，並把「兵種適性 ＋ 地形值」換算成相對「陸軍在平原」
的百分比（`battle.TerrainFactor`）。因此傷害公式、地形效應的幅度、
戰損公式三項列在 `docs/mechanics/40-military` §8 的未解清單裡。

**當初的證據**：那是把地形與兵種兩張表接進來時的第一版做法——
兩張表是量到的，怎麼折成一個部隊的乘數則是 remake 自選的。

**推翻的證據**：碼裡已經不是那樣了。`battle.strike` 逐一走隊伍裡的將領，
每個人各算一次 `LeaderPower(武, 武裝, 兵種, 地形, 攻守)` 再乘上他自己的
兵（`internal/battle/battle.go`），公式與常數 7／3／1000 由
`TestLeaderPower` 釘住，出處 `0x2e01a`／`0x2e13a`。`TerrainFactor`
**沒有任何呼叫者**，是那一版留下來的死碼，已刪。

剩下的合成只有移動力：remake 的一支部隊共用一個移動力池，所以拿兵數
加權的平均訓練度與武裝度代入（`Unit.MovePoints`）。

**怎麼發現的**：盤點 M6 出口條件「戰役的逐日對拍」還缺什麼，去讀未解
清單，發現同一份文件的 §3.1／§3.2 已經把公式寫成 `L0`，與 §8 互相矛盾。

**教訓**：**替換掉一個 remake 自選值的時候，要順手 grep 舊的那一個還有
沒有人用。** 沒有呼叫者的舊函式不會編譯失敗、不會測試失敗，只會帶著
一段描述舊做法的註解留在原地——而那段註解會被之後的人讀成現況。


### R37（2026-09-09）：「部隊一天的移動力是 (訓練 − 武裝 + 100)/10 + 1」

**原斷言**：移動力 ＝ `min(15, (訓練度 − 武裝度 + 100) ÷ 10 + 1)`，
上限 15 寫死在 `0x2e08f`；remake 的部隊拿兵數加權的平均訓練度與武裝度
代入。訓練度愈高愈遠、武裝度愈高愈近，與說明書 p.29–30 同向。

**當初的證據**：`0x2e07a` 的反組譯，`L0`。那一段沒有讀錯。

**推翻的證據**：那一支算的是**對戰子畫面裡單一將領**的值。部隊在
12×10 的戰場上一天能走多遠是另一支（`0x27114`–`0x271dc`），寫進部隊
記錄 offset 34：

```
每一位：值 ＝ ftol((訓練度 × 0.75 + 武裝度 ÷ 4) × 兵力 × 0.01 + 0.5)
上限   ＝ ftol(Σ值 ÷ Σ兵力 × 10 + 0.5) + 2，Σ兵力 ≤ 0 時是 2
```

**武裝度在這條裡是加分**（`0x2716c` 是 `fiadds`，係數 ¼）。四個常數是
資料段的 double：0.75、0.01、0.5、10.0。原版量到的三支部隊——訓 97／
武裝 97 是 12、訓 50／武裝 50 是 7、訓 80／武裝 80 是 10——舊公式一律
算出 11，新公式逐支相同（`TestBattleUnitsMatchTheOriginal`）。

**怎麼發現的**：把原版驅動進主戰場，讀它自己的部隊記錄，拿 remake 的
公式重算三欄。兵士數與綜合能力全中，移動力五支全錯而且**錯得一模一樣**
（remake 一律 11）——同一個值出現在每一支，代表輸入被抵消掉了，
那正是「訓練 − 武裝」在這批盤面上都等於 0。

**教訓**：**同一個名詞在原版裡可能有兩支常式。** 找到一支符合說明書
描述的公式就停手，會把另一支的責任也算到它頭上。判準是「這個值最後
寫進哪個欄位」——offset 34 的寫入端只有 `0x271d7` 與 `0x271f9`，
`0x2e07a` 的結果進的是 `es:[將領槽 × 2 + 0x548]`，兩者從來沒有交集。


### R38（2026-09-09）：「交戰的傷亡是逐將領的 `兵士數 × 戰力值 ÷ 100`」

**原斷言**：一次交戰的傷亡照 `0x30560`——雙方各自對每一位將領算
`殺傷 ＝ 人物.兵士數 × es:[0x548 + 槽×2] ÷ 100`，同時互扣。
remake 的 `battle.exchange` 照這條寫，戰力值走 `LeaderPower`
（地形表 `DS:0x85c2`／`DS:0x85e2`）。

**當初的證據**：`0x30560` 的反組譯，`L0`。那一段沒有讀錯。

**推翻的證據**：把原版驅動進主戰場、玩家那支走到守軍旁邊，
`0x30560` **一次都沒觸發**，而交戰結算 `0x2a224` 跑了。前者是對戰
子畫面（`2020:1616`）裡的，後者才是主戰場的「對戰」走的路。
`0x2a224` 看的是**部隊的**綜合能力（部隊記錄 offset 32）與另外兩張
地形表（`DS:0x8162` 攻／`DS:0x8182` 守）：

```
甲的殺傷 ＝ ftol(攻值[甲那格] × 甲.兵士數 × 甲.綜合能力 × 倍率[模式] × 1e-6)
乙的殺傷 ＝ ftol(守值[乙那格] × 乙.兵士數 × 乙.綜合能力 × 1e-4)
逐將領：新兵 ＝ max(0, ftol(兵 × (1 − 對方的殺傷 ÷ 自己的兵士數)) − 1)
```

**兩個坑**。`0x2a5c9` 那道 `dec` 少了會讓四項裡三項多 1。而算的寬度是
**x87 的 64 位元尾數**：`1500 × (1 − 585/1500)` 數學上是 915，
用 `float64` 會捨進成剛好 915（915 附近的間距 1.1e-13，誤差 2e-14
不到半格），用 64 位元尾數是 914.99999999999998——`ftol` 截尾差 1。

**怎麼發現的**：把 `0x30560` 掛上攔截點對拍交戰，一次都沒觸發，
而 `0x2a224` 觸發了三次。

**教訓**：**「這支常式算的是 X」不等於「遊戲裡的 X 走這支」。**
反組譯讀出一支語意正確的常式之後，還要問「玩家實際按下去的那條路
會不會走到它」——判準是攔截點有沒有被執行到，不是碼讀起來對不對。
這與 R37 是同一個形狀（同一個名詞兩支常式），只是這次差的不是公式
而是**整條路徑**。


## 5. 決策紀錄

| 日期 | 決策 | 誰 |
|---|---|---|
| 2026-09-06 | 兩版並列，`internal/rules` 共用，版本差異用旗標；**差異解出來之前不預先造旗標** | 使用者 |
| 2026-09-07 | **版本旗標定案為 `state.Edition`（`base`／`plus`），只切難度上限與出兵係數表**——差異解出來了，粒度就是量到的兩件事，不多造（`docs/spec/004`）| 分析結論 |
| 2026-09-06 | 定位是 remake ＋ 英日多語系；繁中是原文不是譯文 | 使用者 |
| 2026-09-06 | repo 名 `softworld_san1_remake`，private | 使用者 |
| 2026-09-06 | dosgolem 上游加 `apps/san1` ＋ `runtime/msc`；本機另 clone `dosgolem-san`，獨立分支 | 使用者 |
| 2026-09-06 | **人名與地名以遊戲資料為準**，手冊只當術語母本——手冊掃描轉錄有字形問題，資料是 `L0` | 分析結論 |
| 2026-09-06 | **AI 三個版本並列**：`base`（原版還原）、`plus`（加強版還原）、`enhanced`（remake 強化）。前兩個是還原、第三個是創作，驗收標準不同不可混用（`docs/design/01`）| 使用者 |
| 2026-09-06 | **反組譯結果整理成 markdown 進 repo**——專案的另一個目的是台灣電玩文化保存 | 使用者 |

---

## 6. 文件索引

| 目錄 | 內容 | 現況 |
|---|---|---|
| `docs/re/` | 程式碼在哪（位址、bytes、xref）| `00` DOS 服務普查、`01` IDA 管線、`02` 開機到遊戲設定、`03` 主程式碼地圖、`04` 主程式字串表、`05` 戰役層、`06` 四季事件、`07` 五種謀略 |
| `docs/formats/` | 資料長什麼樣 | `01` 容器、`02` 郡名、`03` 劇本表、`04` 資產目錄、`05` 存檔格式、`06` 配樂、`07` 點陣圖 |
| `docs/spec/` | `DRAFT`／`READY`／`CONFORMED`／`SUPERSEDED` | `001` 容器、`002` 劇本表、`003` 劇本欄位、`004` 版本與難度（皆 READY）|
| `fonts/` | 自由授權點陣字型 ＋ LICENSE | unifont 16×16、ascii6x10 |
| `docs/mechanics/` | 遊戲怎麼運作 | `00` 索引與狀態總表、`01` 開局局面、`10` 戰略、`20` 人物、`30` 謀略、`40` 軍事、`50` 事件、`60` 經濟、`70` AI、`80` 勝負、`90` 兩版差異 |
| `docs/playtest/` | 原版 vs remake 同狀態比較 | `01` 一個月的狀態轉移 |
| `docs/reference/` | 說明書整理、社群資料 | `01-manual-*`（5 份）、`02-web-*`（4 份）|
| `docs/design/` | remake 自己的設計決策 | `01` AI 三版本、`02` remake 自訂數值、`03` 戰役兩層 |
| `docs/release/` | 發行 | `01` 封包 |

---

## 7. 術語表

遊戲內術語一律以說明書繁中原文為準（`docs/reference/01-manual-40-glossary.md`）。
下面只放**專案內部**的詞。

| 詞 | 意思 |
|---|---|
| **base** | 原版，`AA.EXE` |
| **plus** | 加強版，`ASV.EXE` |
| **三件套** | `DATA<n>.GRP` ＋ `DATA<n>.IDX` ＋ `DATA<n>.NAM` 這組容器 |
| **對拍** | 用 dosgolem 在程序內跑原版，逐項比對 remake 的結果 |
| **槽位** | 原版版面上一段固定寬度的文字位置；多語系溢出問題出在這裡 |

---

## 8. Worklist

按 `CLAUDE.md` §10 的 M0 出口條件排：

- [ ] 兩版抽檔，每檔記 SHA-256，分開放 `workplace/orig/{base,plus}/`
- [ ] `tools/ida.sh` 包裝器（照 sangokushi 的形狀），對兩支 EXE 產 `.i64`
- [x] `tools/go.sh` 包裝器（離線 module cache 已備妥）
- [x] `dosgolem cmd/probe` 跑兩支 EXE，產未實作 DOS 服務清單 → `docs/re/00`
- [x] 在 `dosgolem-san` 實作 `int 21h AH=01/07/08/0B` ＋ `KeyWaits` 計數
- [x] **dosgolem 阻塞模型** → `dosgolem/docs/spec/008`（READY）＋ 實作，驗收全過
- [x] 阻塞模型做好後重跑 probe → 過了啟動選單（F21、F22）
- [x] 反組譯呼叫端 → 四題裝置選擇（F26）
- [x] 查那 790 萬道指令 → 是解壓階段的記憶體翻攪，成因是 dosgolem 配置器不回收（F28）
- [x] **在 dosgolem 實作 `int 21h AH=4Bh AL=03`（載入 overlay）** → `dosgolem/docs/spec/010`
- [ ] probe 要能看 `B0000`（Hercules）——目前只看 `A0000` 與 `B8000`，選 Hercules 時會得到假零
- [x] **對拍框架**：`internal/parity` ＋ go workspace ＋ `-tags oracle`，缺素材或缺 dosgolem 時 skip 不紅
- [x] **解 overlay 的 R6009** → 是 dosgolem 配置器的 bug：`AH=4Ah` 縮小 PSP 區塊時 arena 沒跟著往下長，少記 190 KB（`dosgolem/docs/spec/009` 附錄二）
- [x] 追 `TITFONT.IMG / ErrNo 888` → 根因是 dosgolem 沒把配置器狀態發布成 MCB 鏈（R4，`dosgolem/docs/spec/012`）
- [x] **原版跑到主選單** → 還缺兩塊：MCB 鏈（`spec/012`）與 handle 號碼重用（`spec/013`）
- [x] 從主選單按「1. 開始新遊戲」走進遊戲 → 選擇年代、君主選擇都走到了（`docs/re/02`）
- [x] 選定君主之後走進遊戲主畫面 → 十個命令的主畫面（F43）
- [x] 在遊戲主畫面 dump 記憶體定位執行期資料表 → 三張表連續在 0x399B0（F49）
- [x] 解出歸屬四欄並實作（F46–F48、`docs/spec/003`）
- [x] 屬性欄位版面 → 用原版自己的 `printf` 格式字串解出來（`docs/spec/003`）
- [x] **人物 offset 27 ＝ 肖像編號**（對上 `F000`–`F255.FAC` 的 256 張）、
      **offset 28 ＝ 壽命**（幾歲開始走下坡不是幾歲一定死，`0x15d5d`）
- [x] 人物 3／15／23 不是獨立欄位（姓名與相鄰 `u16` 的位元組），
      20 全是 `0xFF`、29 全是 0，兩者都沒有任何讀寫（`docs/spec/003` §2.9）
- [ ] 人物 offset 6 還沒解
- [x] 州郡的相鄰表 → offset 45–52（F53）
- [x] **州郡 offset 12 ＝ 郡縣自治型態**（0 正常、1 內政、2 軍事、3 自冶）：
      非零的郡由電腦拿「值 − 1」當 AI 等級跑分派器（`0x17550`）——
      這也是「六個 AI 等級是六種性格不是六個強度」的第二個證據
- [x] **州郡 offset 5 ＝ 州**（0–13，州名表 `DS:0x9b8c`；原版寫「弁州」
      「充州」）、**offset 34 ＝ 戰場版面**（9 ＝ 12×7、10 ＝ 8×10，
      與地圖形狀逐郡吻合）；offset 10／11 全是 `0xFF` 且沒有讀寫
- [x] **兵種與職位的編號對應是 `L0` 不是推的**：字串表在 `DS:0x5962`
      （七項）與 `DS:0x5934`（九項），用欄位值直接索引；兩欄都可以是
      `0xFF` 哨兵
- [x] **州郡記錄一百七十六個位元組盤點完了**（`docs/spec/003` §3.0）。
      「offset 84／86 是第二組金／米」是作廢的舊筆記——那兩格在戰場地圖
      （55–174）裡。offset 35 是南北分群（24 個 11、18 個 12，
      兩版都沒讀），36–44 是九個 120
- [x] `金%6d`／`米%6d` 是**戰役工作區**的（`0x22f41` 印四個軍團帶的
      錢糧，值在工作區 `0x1764` 起），與州郡記錄無關
- [ ] 諸侯剩下的 66 個 byte（offset 4 ＝ AI 等級、**offset 8 ＝ 人望**）。
      **offset 10–13、19–71 不是外交狀態**：五支謀略讀寫的都是州郡與人物
      的欄位，結盟不是存下來的關係而是當場打一仗（`docs/mechanics/30` §5）
- [ ] 把開機配方從「指令數觸發」換成「程式行為觸發」，才能當回歸測試
- [x] EGA 圖形控制器的寫入模式／Set-Reset／Bit Mask／latch（`dosgolem/docs/spec/011` §4）
- [ ] 主程式資料段的檔名表（F37）與武將欄位標籤（F38）→ 對照 `internal/state` 的 30 bytes/筆版面
- [x] `10.GRP`／`20.GRP`／`D5.GRP` 逐位元組 diff → **三份都是 MZ 主程式映像**，不是容器變體（F34b、F34c）。都經過壓縮，diff 讀不出語意
- [x] 從資料檔找 42 個郡名 → `docs/formats/02`
- [x] 從 `DATA2.GRP` 取人物表 → 346 位（**不是手冊說的 342**，差 4 位未解釋）
- [x] 對比 346 vs 342 → 差距被異體字與掃描誤讀蓋住，無法用字串比對解決；
      **由此定案：人名地名以資料為準（`L0`），手冊只當術語母本**（`docs/formats/03` §4）
- [x] **戰役的戰術層** → `internal/battle`（`docs/design/03`）；地形版面 remake 自產，
      機制照手冊。`.OKR` 未解不再擋這一步
- [ ] 解 `.OKR`（465 項，壓縮過）→ 反組譯 `AA.EXE` `0x495f2` 的三個
      引用點，看 `R%03d.OKR` 的 `%03d` 從哪裡來（`docs/re/04` §13）。
      解出來也只作參考，美術素材不重製；能拿到的是**版面尺寸與
      地形分佈**這類規則性資訊
- [x] **戰役的手動指揮介面** → `battle.Runner` ＋ `game.BeginAttack`／
      `FinishAttack` ＋ `ui.DrawBattle`；指令與方向鍵盤照原版（F56）
- [x] 逐日戰報畫面（`ui.BattleReport`；最近幾場掛在其他 → 戰役）
- [x] 郡地理誌（查看 → 5）顯示該郡的戰場地形，與真的打起來用的是同一張
- [x] **紮營讓玩家逐隊指定位置**（`battle.Camp`）
- [x] **出兵時自選攜帶的金米**，畫面算出「30 日須耗用 %d 米」
- [x] 徵兵／賞賜／運送改成輸入數字（原版本來就是問數字，先前用固定量）
- [x] **找到並解出配樂**（`docs/formats/06`）：`DATA1` 的 `MUS`／`MUSV`
      巢狀容器，五首曲子的事件數與表頭精準吻合
- [x] **解音色庫的 56 個位元組怎麼對到 AdLib 的參數**：兩個運算子各十三項，
      版面與 AdLib Visual Composer 的 `.BNK` 相同；拿原版自己填進 OPL2 的
      暫存器值回頭驗，25 段載入的二十三個參數逐項相同
- [x] **讓原版在 dosgolem 裡放得出音樂**：`cmd/probe -adlib` 讓偵測過關
- [x] **OPL2 合成**（`internal/music/opl2.go`）＋ 音高對拍
      （`TestNotesMatchTheOriginal`）
- [x] **素材匯出加 OGG**（`tools/assets.sh`：Go 合成 WAV → 容器裡的 ffmpeg 編碼）
- [x] **遊戲放得出配樂**：邊播邊合成，M 換下一首，「其他 → 音樂狀態」真的靜音
- [x] **解 `MUSV`**：時間差是一個位元組不是可變長度值，即時訊息可以插在
      事件中間——六首現在全部與表頭吻合
- [x] 「其他」底下的六個開關（音樂／音效／語音／戰役／年號／延時）
- [x] **解點陣圖並匯成 PNG**（`docs/formats/07`、`cmd/san1assets`）
- [x] **平面與顏色的對應拿 dosgolem 的畫面驗證**（`cmd/san1imgcheck`，100% 相符）
- [x] **多語系**：介面文字收進 JSON，繁中／英／日
- [x] `TITL0`–`3` 四張 160×400 **並排**（x ＝ 160i）取第 0–349 列（R23）
- [ ] `.MSK` 與 `8x8AND*` 遮罩怎麼與圖搭配
- [x] **人名與郡名的英日對照**：英文漢語拼音、日文新字體；字元層 426 個字，涵蓋率有測試
- [x] **遊戲內的訊息與錯誤字串也收進 i18n**：字串表從 174 個鍵長到 372 個

- [ ] 查驗年號：把原版在 dosgolem 裡推過一年，看它顯示中平七年還是初平元年
- [x] **反組譯指令分派器** → 六個等級 × **十八張**表（無條件依序跑完），
      等級在 `BASEMAS` offset 4（`docs/re/03` §1.4）
- [x] **前九張分派表各對應哪一種行為** → 時間軸歸屬量出來了（`docs/re/03` §1.4）
- [x] **諸侯 offset 14–18 是君主寶庫**（`L0`）：14 玉璽、15 兵書、16 寶刀、
      17 美女、18 駿馬。賞賜那四支拿 `es:0xf(%bx)`（＝15）起算的四格當閘門
      （`0xd9b4`：庫存 <= r 就不送），進貢寫回同樣的四格（`0x172f8`：
      `庫存 = min(100, 庫存 + 件數)`）
- [x] 拆開 `0x5594` 那一列 → 改用**位址**歸屬：寫回點 `0xc193`，一個月
      18 次，與碼讀出來的「走守軍清單、每一位寫回武裝度」相符
- [x] **前九張分派表辨識完畢**（`docs/re/03` §1.4）
- [x] `0x54d4` 排序清單的鍵 → `智 + 武 + 加權表[身分]`（`docs/re/03` §1.4）
- [x] `0e8d:0354` 不是成功率，是**扣錢**：金額 × 係數[等級]，係數表
      `[1,1,1,1,0.9,0.75]`，同時扣 AI 的本回合預算與郡的金
- [x] 把 `state.AICost` 接進各道命令的花費 → `game.Faction.ByComputer`
      ＋ `game.State.price`；買賣米糧沒接（`0xec24` 量到的是分派表底下
      那幾種行為，市場交易走哪條還沒看）
- [x] **電腦諸侯不受「每郡每月一道令」管**（說明書 p.17 那條只管玩家）：
      分派器每郡跑十八道。同一個旗標
      （`ByComputer`）；`Restore` 是拿 `NoFaction` 叫 `New` 的，讀檔之後
      要重算，否則玩家也跟著不受限
- [x] **AI 本回合的預算（`es:[0x3d16]`）怎麼算出來的**：十八張表裡有六張
      在呼叫前寫它，`預算 ＝ 係數表[12×(4×等級 + es:[0x3f08] mod 4) + 位移]
      × 郡的金 × ds:[0xa632]`（`docs/mechanics/70-ai` §2.14）
- [x] **讀出係數表 `DS:0x5714`（24×6 word）與常數 `ds:[0xa632]`**：
      常數是 `0.01`，所以**預算 ＝ 郡的金 × 係數%**
      （`docs/mechanics/70-ai` §2.14）
- [x] **另外九張表的形狀**：`0x5514` 八格全是空操作（拿了預算卻什麼
      都不做）；`0x54f4`／`0x56d4`／`0x56f4` 等級 0–2 不做；
      `0x56f4` 的機率是 `RND(10)/RND(8)/RND(5)`；`0x55f4` 一律 `RND(20)`
      （`docs/re/03` §1.4）
- [x] **`0x5574` 是徵兵**（`0xbeb8`）：徵到帶兵上限，受預算（郡的金的
      30–50 %）與「人口 − 3000」夾住；訓練度與武裝度按新兵力重算
- [x] **`0x55b4` 是調整兵力**（`0xc2c4`）：按帶兵上限攤平兵力，訓練度與
      武裝度拉到全郡加權平均；四捨五入，原版不做總數校正
- [x] **帶兵上限表 `es:[0x666e]`**：5000/3000/2500/2000/1500（職位 0–4，
      5–8 重複後四項）——與 `game.TroopCap` 逐格相同，那一條從 `L2` 升到 `L0`
- [x] **`0x55f4` 是開倉賑民**（`0xc8f6`）：民忠低於「底[等級] ＋ RND(20)」
      才做，撥整份預算，增幅上限是太守魅力 ÷ 2。**原版扣的是金不是米**
- [x] **`0x5654` 是賞賜金帛**（`0xd302`）：跳過君主，每人 min(預算,100)，
      忠誠 ＋= 效果 × 金 ÷ 100，花費照實際增幅反算
- [x] **`0x55d4` 是買入米糧**（`0xc634`）：**一金買到 `(100 − 物價) ÷ 10`
      單位米**，存糧目標 ＝ min(兵士（百）× (RND(10)+12), 30000)，
      缺口大就把金花光（下限 0）。不經過折扣常式，是市場交易
- [x] **`0x54f4` 是出兵／移防**（等級 3+）：四道門檻（兵士 ≥ 500、
      兵士 ≤ 金、米 ≥ 兵士×15、清單非空），編隊是**隨機洗牌後從尾端取**，
      目標 `RND(4)` 三選一（無主鄰郡 25 %／自己的鄰郡 25 %／第三份 50 %）
- [x] `0x54f4` 的第三份清單是**敵國的鄰郡**；出征兵力的目標
      `es:[0x2e62]` ＝ max(本郡最小非零兵力, 各敵鄰郡的兵士)；
      進攻前多一道 `難度係數[難度] × 出征兵力 >= 目標兵力`，
      係數表 `DS:0x5430`（難度 1–10 ＝ 1/0.9/1/0.8/0.8/0.7/0.7/0.6/0.6/0.5）
- [x] **`es:[0x30fe]` 是難度**（`請輸入難度(1-10)` 的輸入，`0x12313` 寫入）
- [x] `0x54f4` 的出發常式 `0xb706`：目標郡現役將 ＋ 出征人數 > 50 就裁隊伍；
      **部隊按兵力比例帶走郡裡的金與米**，目標郡的金米各自夾在 30000
- [x] `0x54f4` 的移動本身（`0x1938a`）：出征者的所在郡改成目標郡、
      錢糧兩邊加減、兩個郡各重整一次守將清單
- [x] **電腦諸侯的出兵不打仗**：每個郡的回合入口（`0x17471`）在跑分派器
      之前先**重算全部 43 個郡的所屬**（`0x1e394`）——先全部設成無主，
      再掃 350 筆人物，把每位有主武將所在的郡標成他的勢力。
      **同郡兩方時人物槽號大的說了算**。郡的歸屬是導出值不是狀態，
      remake 接上了（`game.RecomputeOwners`，換月時跑）
- [x] **玩家「發動戰役」的流程全讀出來了**（`0x188ea`）：**要君主本人
      坐鎮該郡**、先選從哪出兵再選打誰、目標只能是出兵郡的鄰郡裡
      「有主且不同勢力」的（`docs/re/03` §1.5）
- [x] **驅動原版進戰鬥的工具**：`TestZZBattleKeySweep` 開機一次存快照、
      每個候選按鍵還原重試（三分鐘 → 十秒）；`stageABattle` 直接寫
      記憶體擺盤面
- [x] **原版打得起來了**：`2 ⏎ 2 ⏎ 4 1 ⏎ 2 5 ⏎`，判準是 `0x18bb9`
      被執行到。卡了十六輪的「兩位數送不進去」是**選單那一位數沒送
      Enter**——欄寬滿了之後多打的數字被直接丟掉，於是後面的數字
      整段錯位（`docs/re/03` §1.5 的按鍵表）
- [x] **戰場地圖是原版的資料**：州郡記錄 offset 55–174，120 個位元組、
      12 欄 × 10 列，低四位地形高四位標記（`docs/re/05` §2、
      `docs/spec/003` §3.3）。地形碼九個全部對上，移動力表 `DS:0x7c42`
      與說明書 p.29 逐格相同。remake 換成 `battle.Load`，42 個郡
      round-trip 逐位元組相同
- [x] **原版進得了主戰場**：整編的按鍵序列解出來了（`docs/re/05` §5），
      畫面上的地形與解出來的地圖逐格對得上（顏色取樣，同 §2.3）
- [x] **將領戰力值與移動力**：`(戰力×7 + 3×武裝) × (兵種適性 + 地形值) ÷ 1000`
      與 `min(15, (訓練 − 武裝 + 100)/10 + 1)`，兵種 × 地形適性表在
      `DS:0x8602`（`docs/re/05` §3）。走一格只看地形，沒有兵種調整
- [x] **一次交戰的傷亡**（`0x30560`）：`殺傷 = 兵士數 × 戰力值 ÷ 100`，
      雙方同時算、不看對方防禦，兵打光的將領被俘。`0x548` 的索引是
      `(10 × 陣營 + 將領槽) × 2`，陣營只有兩格（0 守 1 攻）——與 §3.1
      的兩個寫入端接得起來。remake 已換成這一套，`TuneHit*` 整組退役
- [x] **計謀的費用與智力門檻**是量到的（`DS:0x7f62`／`DS:0x7f6e`），
      與說明書 p.32–34 逐格相同；軍團與部隊記錄的欄位也讀完
      （`docs/re/05` §3.3、§4）
- [x] **計謀還有一道說明書沒提的成功判定**：
      `RND(上限) + 目標謀略 < 施法者謀略`，上限
      火攻 10／水渰 8／陷阱 2／誘敵 2／燒糧 4／圍攻 6。
      remake 原本過了門檻就一定成功，已補上
- [x] **火攻與水淹的地形殺傷**是量到的（`DS:0x8200`／`DS:0x8224`）。
      原版的索引是 `地形碼 & 0x0D` 而表只有八格，樹林與沙漠讀到表外，
      被 0.9 的上限夾成九成——玩起來就是「樹林殺傷力最強」。
      謀略 98 起跳乘 1.6；打光的將領 80% 機率燒死
- [x] **陷阱與燒糧**：困住 `RND(5)+1` 天（手冊寫九日，碼裡不是）、
      燒糧留下五到七成；**六種計謀都會看領隊謀略有沒有到 98**，
      到了就加碼（火攻水淹 ×1.6、陷阱多困、燒糧只剩兩成）
- [x] **三十天的判定**：滿 30 天（不是 31）看**此刻**誰站在城池那一格，
      空著算守方。remake 原本用「進佔過城池」的黏著旗標，已改
- [x] **接不接受單挑**：預設拒絕，三道機會（戰力差 ±5 的窗、
      兵力兩倍、兵力五倍）。remake 原本一律應戰，已改
- [x] **單挑的勝負**：體能就是血條，`回合數 = RND((甲+乙)/2) + 甲/7 + 乙/7`，
      每回合 `max(0, RND(5) + 戰力差 − RND(5) − RND(6) − 4)`。
      期望值是「戰力差 − 6.5」，所以戰力相同就分不出勝負
- [x] **拒絕單挑掉的是拒絕方的兵**：`兵 ÷ 除數`，三條分支
      （謀士 2 %、打不過 4–5 %、旗鼓相當一成）
- [x] **對戰的子地圖那一層**定性完畢：獨立畫面、五個動作、欄位都量到
      （`docs/re/05` §10）。**remake 刻意不做**，收斂成一次部隊對部隊的
      交手；底下的公式全部照原版（登記在 `docs/design/03` §6）
- [ ] **戰術層剩下的**：
      對戰的子地圖那一層（地形 `es:[0x23c2]`、佔位 `es:[0x3b9c]`、
      行動力 `es:[0x3c9c]`）、
      誘敵的 `0x2a224`（看起來其實是一次攻擊，與手冊對不上）、
      圍攻的合擊、
      `0x21816`／`0x22704`／`0x21e94`／`0x22814`／`0x22bfc`／`0x22c94`／
      `23a6:0002`，以及 `0x30f48` 兵力損失（`損失 ＝ 兵力 ÷ (10 − RND(n))`）
- [x] `es:[0x16f2]` ＝ **軍師的謀略**，每個郡的回合開始時從諸侯記錄
      offset 6 重算（`0x175a7`）。所以 `0x18a90` 是「謀略 85 以上的軍師
      一定勸你別出兵」。諸侯 offset 6 ＝ 軍師的人物槽號也跟著解出來
- [ ] 原版怪癖：金／米加總後用 `jns` 判斷才夾 30000，所以真正的上限是 32767
- [x] **相鄰表是 10 格不是 8**（州郡 offset 45–54）：原版建候選清單掃 `k < 10`。
      劇本 001 第 9、10 格全是 `0xFF`，所以只讀 8 格在這份資料上結果一樣
- [x] **`0x56f4` 是計略（破壞）**：軍師要在場、機率 10／12.5／20 %、
      目標是**全圖**任何一個敵郡、使者是本郡魅力最高的人。
      成敗比 `max(軍師智, 君主智)`（人望 < 80、魅力 < 70 各扣分）
      對上對方的 `max(君主智, 軍師智)`。得手之後降民忠、升洪水率、
      降土地價值、燒米燒錢（`docs/mechanics/70-ai` §2.13.7）
- [x] 計略燒米燒錢的係數是 100；`game.Sabotage`／`game.PlotScore` 接上了，
      AI 那一面是 `internal/ai/ai.go` 的 `plot`
- [x] **`0x56d4` 是挖角**（`0xe0bc`／`0x1dc0a`）：君主要在本郡、機率
      30／60／80 %、直接扣 100 金。候選條件是**忠誠 < RND(15)+80**
      加上**牽絆閘門的鏡像**（Bond 指到的人不在他自己陣營）
- [x] **挖角的成敗判定讀出來了**（`0x1dc0a`）：門檻 ＝
      `(我方君主魅力×3 ＋ 我方人望×4) ÷ 6 ＋ 加成`，抵抗 ＝
      `忠誠 ＋ 戰力÷10 ＋ 謀略÷10` 再加三個條件式加項（對方人望、
      目標忠誠、**對方持玉璽 +1000 等於免疫**）；回傳的是成功後的忠誠
      `(100 − 舊忠誠)÷2 ＋ 我方人望÷2`，<= 0 就當失敗。
      `TuneHeadhuntBase` 退場（`docs/mechanics/20-personnel` §5.1）
- [x] **挖角對拍完成**（`docs/playtest/02`）：四個月 25 次判定，三個條件式
      加項各走到 25／2／23 次，觸發與否在全部 25 次上都與條件相符。
      忠誠有兩道相反的門（候選過濾要 < 94、加項要 >= 87），只有 87–93
      同時滿足——設 95 會被候選過濾先擋掉，看起來像加項不存在
- [x] **主事者分岔的成因是 `Governor()` 比對了勢力**（`internal/game/state.go`）。
  三份 dump 各讀一次 offset 32：出發 53／65、原版走完 53／65（**一整輪沒動**）、
  remake 走完 336／342——是 remake 自己改掉的，也就是候選解釋 (b)。
  - **郡 13 潁川**（所屬 4）的主事者是荀彧，**勢力 5**。`Governor()` 要求
    記著的那一位「還是這一方的人」，把他判失聯，`pickGovernor` 重挑袁胤
    （勢力 4、魅力 82 < 荀彧 90）並把身分由 3 升成 2。原版沒有這道檢查
    （`docs/re/07` §7：勢力相符是出貨劇本的性質，不是執行期的不變式）。
    拿掉之後這個郡對上；易主由另外兩個條件擋（被俘、陣亡、撤走都會落掉）。
  - **郡 1 遼東**是另一件事：原版的主事者許攸留在郡 1，remake 把他移防到
    郡 2（`所在` 1 → 2），主事者才輪到傅幹。**成因在出兵側，不在太守側。**
  - 槽 336／342 是傅幹與袁胤，**真的武將**；填充是 346–349
    （`槽 349 名=︰…‥ 身分=12 勢力=255`，R31）。
  量出來：`TestZZMonthParity` **442 → 439** 個位元組（兩次都是 `PASS`），
  `+32` 由 2 個郡剩 1 個。
- [x] 指定太守的名單：**不比對勢力**（28 次，其中一次名單裡有外勢力的人）；
  「身分 0–3」與「身分 1–3」在行為上等價——種了 5 位君主進「主事者是太守」
  的郡之後，排序當下一次都沒出現「君主在郡裡而且不是主事者」。
- [ ] **指定太守的三處條件實作過但量出來更差，已回退**（碼在 git 歷史裡，
  見 `d51dfa7` 之後那一輪）：
  - 跳過條件改成「主事者的身分 == 0」（`L0`，`0xd6b3`）
  - 名單不比對勢力 → 當時量到「5 個郡多寫主事者」（+32 從 2 個郡變成 7 個）。
    **成因不在名單**：`Governor()` 比對勢力，混編的郡每個月被重推一次，
    AI 再照重推的結果指一次。兩件事一起改之後是 439（見上一條），
    這一項已經進碼（`mostCharming` 不再過濾勢力）
  - `州郡 offset 30 ← 新主事者的勢力`（`0xd74d`）→ 讓潁川在同一批命令中途
    易主，後面的命令撞上「這個郡不是你的」，`TestZZMonthParity` 由綠轉紅。
    remake 的所屬本來就照 `0x1e394` 從人物表重算，這一步是多餘的。
  剩下兩項要重做時，記得原版的跳過條件讀的是**存著的 offset 32 原值**，
  而 remake 的 `Governor()` 是「讀了會改狀態」——存著的那位失聯時它會重新
  指派並寫回去。勢力條件拿掉之後失聯的門檻變窄了，但這個性質還在。
- [ ] **`cmd/san1turndiff -plan` 是 remake 那一半的快速迴圈**（2026-09-08）：
  吃 `internal/parity` 倒出來的 `.bin`，不用 oracle 就能重跑一個月並逐道印
  出電腦諸侯發的命令，幾秒一輪。「remake 為什麼多做了這件事」用它問，
  不要每次都等四分鐘的對拍；**它證明不了原版做了什麼**，比對還是要跑
  `TestZZMonthParity`。
- [ ] **`installGovernor` 要把新太守寫回州郡 offset 32**（原版 `0xd705`
  每次都寫）。現在不寫，所以指定太守之後 `Governor()` 還是回答舊太守，
  而他的身分剛被降成 3——原版保證那一格指到的人身分是 0、1 或 2。
  **補上之後量到 439 → 445**（`+32` 由 1 個郡變 6 個）：多出來的 5 個郡
  不是挑錯人（名單集合相等 28 次、`list[0]` 25 次逐次相同），是那幾個郡的
  守軍名單已經先分岔——原版這個月一個郡都沒改 offset 32，代表原版那邊
  最大魅力者就是現任。**擋在移防對拍後面**，一行：`installGovernor` 末尾
  `p.governor = t.Index`（`docs/playtest/02`）。
- [x] **指定太守／指定軍師吃的那份名單，順序解出來了**（`L1`，25 次逐次
  相同）。名單是**行動者那一張表（`0x54d4` → `0xf170`）就地排完留下的**，
  鍵是 `智 + 加權表[身分] + 武`；排序法是**交換排序**（內層一比到更大的
  就當場對調，`0xFFFF` 的洞跳過）。用穩定排序當模型時 25 次裡有 3 次差在
  相鄰一對，換成交換排序 0 次。
  **加權讓身分 2 排在身分 3 前面**，所以魅力並列時現任太守站在前面，原版
  常常什麼都不換；remake 原本照槽號掃，在 5 個並列的郡各換一次人。
  連帶三處照碼修正：`0xd715` 只升身分 3（挑中軍師時不動他的身分）、
  `Faction.Chief` 改讀諸侯 offset 6 的存值（重推會漏掉別的勢力的軍師）、
  `Tables()` 補寫諸侯 offset 6。
  **436 個位元組，身分差異 13 → 4 筆。**
- [x] **郡的回合改成全域的洗牌迴圈**（`internal/session`）。原版是一條走
  43 格的全域迴圈（`0x15758`，順序表每月洗五輪），不是「一個勢力跑完換
  下一個」——誰排在前面誰先花錢、先徵兵、先出兵。`Session.MonthOrder`／
  `MonthCursor` 對應原版的兩個存檔欄位（`docs/re/08` §2，M6 要用）。
  自治那一條併進同一個迴圈（`0x17550`：offset 12 減一當等級）。
  `enhanced` 補上 `PlanPrefecture`／`ActPrefecture`。
  ⚠ **`TestLogMentionsWar` 原本釘的是症狀**：它找「出兵攻」，而那是
  `AttackOrder.Describe`，只會在命令**被擋下**時隨錯誤訊息進紀錄。改成
  發一道套一道之後命令套得上去了，那一行就消失——判準改釘戰報。
- [x] **亂數解出來了**（`L1`）：`RND(n)` ＝ `0x10b0c`，取 `rand()` 對 n 的
  **idiv 餘數**；`rand()` 是 MSC 的 LCG `seed = seed*214013 + 2531011`，
  輸出 `(seed>>16) & 0x7fff`，**種子在活的 `DS:0xa3ae`**（`docs/re/03` §1.45）。
  核對 400 次逐次相同（`internal/parity/rand_oracle_test.go`）。
  remake 這一邊有 `game.MSCRand`。
  - ⚠ `and ah,0x7f` 遮掉第 31 位，**從輸出反推不出狀態**——要讀種子本身。
  - ⚠ 攔 `RND(n)` 內部那一次呼叫看不到全部的抽樣（別處直接呼叫 `rand()`）。
- [x] **`Roll` 接上同一條 LCG**（`game.SeedRand`／`RandDraws`，預設不開）。
  月度對拍讓兩邊從原版出發時的種子開始抽，並且比「各抽了幾次」——
  **這比位元組數好定位**：次數對不上表示某一支常式的分支或迴圈次數不同。
  量到 385 → **372**，而且**離散欄位全部歸零**（身分、所在、勢力、
  主事者、在職將都不再有差）。
- [ ] **亂數對齊的路線圖**（2026-09-08 量到）：原版一輪抽 1782 次、
  remake 1495 次。把原版的抽樣歸戶到十八張表（攔 `0xe926`–`0xec1b`）：

  | 表 | 次數 | remake |
  |---|---|---|
  | 賞賜物品 | 527 | 每郡 2–3 |
  | 挖角 | 235 | 每郡 1 |
  | 出兵 | 220 | 每郡數次 |
  | 賞賜金帛 | 196 | **0** |
  | 內政 46、尋訪 45 | 每郡約 1.4 | 每郡 1 ✓ |
  | 賑民 32、計略 32、買米 32 | 每郡 1 | 每郡 1 ✓ |
  | 行動者、指定軍師、指定太守、訓練、武器、徵兵、調整兵力、`0x5514` | 0 | 0 ✓ |
  | 郡回合之外（開月洗牌、每月結算、季節事件） | 414 | — |

  **四支就佔掉 1178 次**，正好對得上「每個郡少抽 20–40 次」。

  **兩邊都逐表歸戶之後**（remake 那一邊是 `ai.PrefecturePlanner.TraceDraws`）：

  | 表 | 原版 | remake | 差 |
  |---|---|---|---|
  | 賞賜金帛 | 196 | 196 | **±0** |
  | 內政 | 46 | 46 | **±0** |
  | 計略 | 32 | 32 | **±0** |
  | 挖角 | 235 | 229 | −6 |
  | 尋訪 | 45 | 43 | −2 |
  | 登用 | 3 | 1 | −2 |
  | 賑民 | 32 | 31 | −1 |
  | 買米 | 32 | 31 | −1 |
  | 賞賜物品 | 527 | 527 | **±0** |
  | 計略 | 32 | 33 | +1 |
  | 尋訪 | 45 | 48 | +3 |
  | 出兵 | 220 | 241 | +21 |
  | 挖角 | 235 | 452 | **+217** |

  （2026-09-08 的現況；總抽樣 2028／1782。上面那一欄是第一次歸戶時的
  數字，留著看形狀怎麼變的。）

  **開月的洗牌也要抽**（`0x1740a`，`game.ShuffleTurnOrder`）：5 輪 × 43 格
  ＝ 215 次，原版「郡回合之外」那 414 次裡有它。對拍不用它排出來的順序
  （照原版的順序表走），但**抽樣要抽**，否則接下來每一次都錯開。

  ⚠ **挖角的 +217 是症狀不是成因**（2026-09-08 量到）：原版只觸發一次
  （郡 24），remake 觸發兩次（郡 24、郡 27）。郡 27 兩邊**都是 30000 金**
  （本來就頂到上限），勢力 4 的 AI 等級是 5、相位 2 開放、預算 6000
  ——兩邊都過得了預算那道，差的是入口那次 `RND(10) > 1`（等級 5 的門檻，
  兩成機率擋掉）。**序列在那之前已經岔開了**，所以先收乾淨小的。

  賑民與買米也是「先擲再判」（原版每郡都抽一次，remake 在
  `budget <= 0`／`purse <= 0` 就回、抽都沒抽）——改完兩張都 ±0。
  **對拍也要照原版的順序表逐郡跑**：郡的回合是一條全域的洗牌迴圈，不是
  「一個勢力跑完換下一個」——即使每一張表抽的次數都對，消耗的**順序**
  不同，亂數序列一開始就岔開。改完之後總抽樣 2046 → **1823**（原版 1782）。

  挖角的三道門檻也照原版排：**君主在不在（`0xe414`）→ `RND(10) > 門檻`
  （`0xe427`）→ 本回合預算 >= 100（`0xe438`）**。擲骰夾在中間，所以預算
  不夠的那幾次**還是抽過了**；remake 原本先看預算，少抽一批。
  挖角 +217 → **+3**。

  **第一個岔開的地方量到了：還沒進任何郡回合。**
  逐郡比對亂數狀態（`game.RandSeed`，每個郡的回合開始前比一次），
  順序表第 0 格就對不上——所以差在**月底結算 ＋ 冬季事件 ＋ 開月洗牌**
  那一段：remake 422 次、原版 414 次，**多 8 次**。
  洗牌兩邊都是 215，所以差在結算：remake 207／原版 199。
  ⚠ **這是現在唯一該追的**——郡回合裡剩下的 ±3～±12 全是它的連鎖。
  歸戶完了（攔 `0x1581c`／`0x16e70`／`0x17371`／`0x1740a`）：

  | 段 | 原版 |
  |---|---|
  | 郡回合之外（月 9 的尾巴）| 38 |
  | 月底結算 | 0 |
  | **冬季事件**（人口成長 ＋ 進貢）| **77** |
  | 開月（物價）| 85 |
  | 開月洗牌 | 214（＋1 落在前一格）|

  物價兩邊都是**每郡 2 次**（`repriceAll`：43 × 2 ＝ 86，原版的 85 是攔截
  點的邊界差），所以那一段齊。洗牌 215 也齊。**剩下的 8 次在冬季事件**。
  進貢那一段每個勢力每種寶物抽 2–3 次（`v = RND(領地數+1)`、
  `cap = RND(5)+8`，`cap < v` 就再抽一次覆蓋，`0x171b0`–`0x17206`），
  四種就 8–12 次；13 個勢力算下來遠超過 77。
  外層迴圈確認是**逐勢力**（`0x17346`：`cmpw $0x10`，回跳 `0x1712a`，
  16 個諸侯槽），所以 **77 ÷ 13 ≈ 6 才是每個勢力的真實次數**
  ——「每種 2–3 次」是我從 `0x171b0` 起讀、切在指令中間得到的，不可信。
  再拆一層（攔 `0x1712a` 進貢的迴圈頭）：

  | 段 | 原版 | remake |
  |---|---|---|
  | 人口成長 | 8 | ~17 |
  | **進貢** | **69** | **~104** |
  | 物價（開月）| 85 | 86 |
  | 開月洗牌 | 214 | 215 |

  **對拍的種子改成從月底結算那一刻接上**：快照到月底之間是**玩家自己的
  回合**（原版按了「內政 → 休息」，量到抽 38 次），而 remake 的玩家什麼
  都不做——先前那 38 次剛好抵掉結算多抽的部分，讓總數看起來只差 8。

  進貢的碼從**跳躍目標** `0x1712a` 讀才對齊（先前從 `0x171b0` 起讀切在
  `lcall` 中間，解出自洽但錯的公式）：

  ```
  諸侯[i] 的 es:[0x24b2 + i*2] <= 0 → 跳過這個勢力      ; 0x17133
  RND(10)                                                ; 0x17144，每勢力一次
  領地數 = es:[0x2e36 + i*2]；<= 0 → 跳過                ; 0x17155
  每種寶物：v = RND(領地數)；c = RND(5)
            c < v → v = RND(5)（再抽一次覆蓋）
  ```

  兩個閘門的來源解出來了（`0x170b2`–`0x17123`，進貢之前先算）：

  ```
  清空 16 格 0x24b2[] 與 0x2e36[]
  for 郡 = 1..42：
      所屬 == 0xFF → 跳過
      0x24b2[所屬]++                                  ; 領地數
      0x2e36[所屬] += 民眾忠誠(offset26)/4 + 土地價值(offset27)/2  ; 「人才量」
  ```

  進貢抽的是 **`RND(人才量)`**，不是領地數。

  **進貢解完了**（`L0`＋`L1`）。漏斗實測：迴圈 16 → 過領地 13 → **過人才 6**，
  而 `0x2e36` 印出來是 `[0 75 75 225 150 969 449 75 225 150 0 75 75 75 10 0]`
  ——除以 `RND(10)+80` 之後剛好六個非零，與漏斗一致。

  ```
  基數 = 人才量 ÷ (RND(10) + 80)          ; 0x1714c–0x1715b
  基數 == 0 → 跳過這個勢力                ; 0x17162
  每種寶物：v = RND(基數 + 1)             ; 0x17167（inc ax 之後才 push）
            c = RND(5) + 8
            c < v → v = RND(5) + 8
            庫存 = min(100, 庫存 + v)
  ```

  **只有一個郡的勢力一件都拿不到**（人才量約 75 ÷ 80–89 ＝ 0）——不是
  「拿得少」。remake 原本用領地數當基數、還多發一件紅利，改寫之後
  總抽樣 2053 → 1941。
  **換月各段逐段量過了**（`game.State.TracePhases`）：

  | 段 | 原版 | remake |
  |---|---|---|
  | 物價 | 85（＋1 落在前一格）| **86 ✓** |
  | 人口成長 | 8 | 6 |
  | 進貢 | 69 | 61 |

  remake 的 61 ＝ 13 次 `RND(10)` ＋ 6 勢力 × 4 種 × 2 次，**一次重抽都
  沒有**。原版多的 8 次就是重抽（`c < v → v = RND(5) + 8`）。
  重抽的條件從 `0x17167`（跳躍目標，指令對齊）確認過了，remake 實作一致：
  `v = RND(基數+1)`、`c = RND(5)+8`、`c >= v` 才跳過。所以 8 次對 0 次是
  **取值的差**，不是規則錯——而取值會差，是因為更前面就岔開了。

  **物價那一段找到了**：原版的州郡表有 **43 筆**（第 0 筆是啞元），
  重抽物價的迴圈走完整張表；remake 的 `prefectures` 只有 42 筆，
  **少抽兩次**，而那是整條序列第一個岔開的地方。補上之後 86／86。
  （月度對拍的差異清單裡「郡 0：物價」一直都在，那就是它。）

  **冬季的民亂判定補上了**（`0x16f22`–`0x16f95`，`game.winterUnrest`）：
  抽一個郡 → 有主 → `民眾忠誠 < RND(20)+50` → `諸侯人望 < RND(20)+30`
  → 那個郡出事。**出事之後做什麼還沒解**（間接呼叫 `lcall *es:[0x20ea]`），
  這裡只還原判定與抽樣次數：2／2。

  **換月的順序解出來了**（`0x1581c`，`docs/re/06` §9.5）：
  重算所屬 → **開月 `0x17364`（物價 → 洗牌）** → 月份推進 →
  四季常式（人口成長 → 民亂 → 進貢）。**開月在四季之前**，
  所以那 86 ＋ 215 次排在人口成長前面。

  逐段的帳（原版，攔 `0x1581c`／`0x17371`／`0x1740a`／`0x16e70`／`0x1712a`）：

  | 段 | 原版 | remake |
  |---|---|---|
  | 月 9 的尾巴（玩家自己的回合）| 38 | — |
  | 月底結算入口 → 開月 | **0** | 0 |
  | 物價（開月的迴圈）| 84 | 84 ✓ |
  | 洗牌 | 215 | 215 ✓ |
  | 人口成長 | 6 | 6 ✓ |
  | 民亂判定 | 2 | 2 ✓ |
  | 進貢 | 68 | 68 ✓ |
  | 進貢之後（收尾）| 1 | **0** |

  「月底結算入口抽 0 次」是**接種子的關鍵**：兩邊在 `0x1581c` 那一刻
  可以直接對齊，開月與四季兩邊都會跑。先前接在 `0x16e70` 會讓 remake
  的開月落在錨點後面、原版的落在前面，憑空多 301 次。

  物價 84 而不是 86：`RND(n)` 在 n <= 0 時**回 0 而且不抽**，
  盤面上有兩筆的「民眾忠誠 − 洪水率÷2 + 土地價值」不是正數。
  原版的桶子印 85 ＋ 214 是攔截點的邊界差，總和一樣是 299。

  進貢那 68 次的組成（`docs/re/06` §9，攔 `0x1713e`／`0x17167` 與四段
  重抽的入口實數）：13 個過領地閘門的勢力各一次 `RND(10)`，6 個過基數
  閘門的勢力**各 9 次**（四種寶物各 2 次 ＋ `RND(4)` 挑一種多給一件），
  加 1 次重抽。**remake 少的就是那個 `RND(4)`**——當初改寫基數時把紅利
  一起拿掉了，而它是真的。補回去之後 68／68。

  ⚠ 先前記的「69」多算了一次：`進貢` 那個桶一路標到第一個郡才換，
  把**進貢常式收尾**（`0x1734f` 之後）的一次抽樣也算了進去。
  **13＋54＋2 與 13＋54＋1＋1 都湊得出 69**——分辨的方法是把四段重抽的
  入口（`0x17188`／`0x171be`／`0x171f4`／`0x1722a`）直接數一次。

  **還差的一次在進貢的收尾**：`0x1734f` 接著呼叫 `0x32fb8`
  ——郡 1..42 逐一 `0x32fe6`（讀 offset 30 決定顏色，`0xFF` 用 0x1F），
  再走間接的 `lcall *es:[0x20ea]`。**那個間接目標和冬季民亂的「效果」
  是同一個**（`docs/re/06` §3），看來是「重繪一個郡」的函式指標。
  它在這一輪抽了 1 次，remake 沒有對應的動作。

  ⚠ 這一段連續讀錯三次才對：兩次切在指令中間、一次把 `0x2e36` 當成領地數。
  **教訓：x86 變長指令從非進入點反組譯會給出自洽但錯的碼，而它不報錯。**
  最後是靠「把那兩張表從記憶體印出來」定案的。

  **2026-09-08 收工：月度對拍差 0 個位元組。** 43 個郡、350 位人物、
  16 個勢力逐格相同，十張分派表的抽亂數次數也全部 ±0
  （尋訪、登用、內政、賞賜物品、賑民、賞賜金帛、挖角、計略、買米、出兵）。
  總抽樣 1780／原版 1782（差的 2 次在郡回合之外的歸戶邊界）。
  起點是 **440 個位元組**。`TestZZMonthParity` 現在是硬閘門，
  差一個位元組就紅。逐步紀錄在 `docs/playtest/02` 開頭。

  **回歸也驗過了**（十二支對拍測試，各 0 項對不上）：
  內政 58、徵兵 **597**、尋訪 33、賑民 99、買米 136、賞賜物品 186、
  兵書挑人 31、行動者 40、指定軍師 69、換太守 28、名單形狀 25、編隊 61。

  ⚠ **整包 `-tags oracle` 一次跑不完**：每一支都要開機約 60 秒，
  41 個檔案的總時長遠超過 `go test` 的逾時，而且一次跑太多會被系統
  因記憶體不足砍掉（踩過兩次）。**三支一批**、`-v` 看個別結果。

  **徵兵那一塊也解掉了**（`docs/mechanics/70-ai` §2.13.1）：
  預算每一輪扣的是**花掉的金**（電腦有 0.75 折），不是人數，
  所以徵 323 人只花 242 金、預算 323 → 81 → 21 → 6 → 2 → 1。
  改完之後**位元組差 229 → 73**，訓練度與武裝度的差完全消失。

  **剩下的 73 個位元組**：兵力 36（每筆差 1–3 人，折扣換算的取整）、
  金 15、米 12、人口 4、智 4、魅 2。下一步是把徵兵那個取整對死——
  對拍測試裡攔 `0xbf63`（逐輪印空額／預算／金）與 `0xc025`（逐人配額）
  的鉤子都已經在，`SAN1_WATCH=<郡>` 打開。

  這一輪修掉的（每一條都有 `L0` 出處）：物價的公式、開月排在四季之前、
  月迴圈每格的 `RND(12)`、進貢的 `RND(4)` 紅利、內政的兩次擲骰、
  `0xba02` 只有量非正才擲、電腦尋訪不收錢、買米雙向、駿馬的門檻底 70、
  寶庫要寫回三張表、內政與尋訪用「智最高」那個人。

  **舊的逐段紀錄（2026-09-08 稍早）**：月底結算整段對齊（物價 84、洗牌 215、人口成長 6、
  民亂 2、進貢 68，逐項相同，寶庫逐格相同），月迴圈每格的 `RND(12)`
  （`0x15790`）補上之後**第一個岔開從順序表第 0 格移到第 1 格**。
  內政改成兩次擲骰（R35）之後郡 1 也只剩出兵差 1 次。
  總抽樣 2205 → 2021（原版 1782），逐表：登用／賑民／賞賜金帛／買米 ±0、
  計略 +1、尋訪 −3、內政 +3、出兵 +6、賞賜物品 +9、**挖角 +225**。
  挖角那 225 次是**多觸發兩次**（原版整輪只在郡 24 挖一次，每次掃描約
  222 抽），是前面那些小差的連鎖，不是規則錯——閘門順序與掃描量都對過。

  **下一個要追的**：順序表第 0 格（郡 1）的**出兵**，原版 2 次、remake 3 次。

  ⚠ 逐表比較有兩處**桶邊界**造成的固定偏差，不要當成差異：
  四種寶物的原版桶各含**下一種的 `RND(2)`**（標記點在常式入口 `0xd962`
  而呼叫端先擲），所以原版的「駿馬」比 remake 少一次、「賞賜物品」多一次。

  **先前 ±0 的五張：賞賜金帛、計略、賑民、買米、登用。**
  登用是 `recruitDifficulty` 在牽絆那兩條路上短路，兩次能力值的抽樣就沒
  發生——`0xce8c` 是難度先算、牽絆的例外抽完之後才蓋上去。
  剩下：內政 +2、尋訪 +3、賞賜物品 +9、出兵 +25、挖角 +217（症狀）。

  **「先擲再判」這一族到目前修過七支**：出兵、賞賜物品、尋訪、計略、
  賑民、買米、登用。判準都一樣——**做不做得成不影響抽不抽**。

  舊註（第一次歸戶時的判讀，留著看形狀）：**挖角是唯一剩下的大缺口**，而且形狀固定：一次觸發約 222 次
  （掃全部 350 人、每個合格候選抽一次），所以 +441 就是**多觸發兩次**。
  入口三道是「君主在這個郡」→ `RND(10) > 門檻` → `本回合預算 >= 100`
  （`0xe414`／`0xe427`／`0xe438`）。預算的分母與索引都改對了，剩下的
  差別要再量：**數 remake 這一邊觸發了幾次**，跟原版的一次比。

  **賞賜物品的入口抽樣也對上了**（每郡 8 次）：四支常式各一次
  `RND(2)+2`（呼叫端 `0xe047`），常式裡再一次 `RND(100)`（`0xd96e`），
  **門檻就是那個隨等級的常數 40／60／80**（`0xdf72`／`0xdfe2`／`0xe052`，
  `RND(100) > 門檻` 就回）。接著 `諸侯[所屬] 的庫存 <= r → 回`（`0xd9b4`），
  再掃名單挑人（迴圈裡每個都抽 `RND(20)`，`0xd9f2`）。
  remake 原本只抽一次、門檻寫死 40、而且順序相反。
  **還差的 271 次追到根了：remake 沒有進貢。**
  - 逐支歸戶：入口（呼叫端的 `RND(2)`）32 次、四支各約 125 次
    （`0xd962` 兵書／`0xdac0` 寶刀／`0xdc1e` 美女／`0xdd94` 駿馬），
    分派點之間那段預算計算 **0 次**——所以不是 (b)。
  - 兵書那一支的漏斗：**進入 32 → 過 `RND(100)` 25 → 過庫存 17**。
    庫存那道過得去，所以也不是 (a)。
  - 成因：**庫存在這個月裡長大**。同一個窗口裡勢力 5 的
    offset 15–18 由 `2/2/2/2` 變成 `6/3/2/10`（`parity-00` vs `parity-01`），
    而 remake 那四格**從頭到尾沒動過**——那正是對拍從一開始就有的
    「諸侯 +15(×3) +16(×3) +17(×5) +18(×4) 6 筆」。
    庫存卡在 1–2，`庫存 <= r`（r ＝ `RND(2)+2` ∈ {2,3}）每次成立，
    挑人的迴圈一次都跑不到。
  - **成因是相位，不是缺功能。** remake 的進貢本來就有（冬季，月 10），
    只是**跑在郡回合之後**。原版那個窗口的實際順序是：
    月 9 的尾巴（那 5 個郡的旗標早就清掉了）→ 月底結算 ＋ 冬季事件
    （**含進貢**）→ 開月洗牌 → **月 10 的前 37 個郡** → 又輪到玩家。
    所以郡回合看到的是**進貢之後**的庫存。對拍改成「先 `EndMonth()`
    再跑郡回合」之後：賞賜物品 256 → 422（原版 527）、內政 ±0、
    總抽樣 1703 → 2351。
    這也解釋了原版那一側的「人口(×56)」——冬季的人口成長本來就在窗口裡。
  - ⚠ **新的最大缺口是挖角**（原版 235／remake 1155）。原版每次觸發約抽
    17 次：入口 `RND(10) > 門檻`（`0xe427`，等級 5 的門檻是 1）、
    本回合預算 < 100 就回（`0xe438`）、挑目標走 `0xe0bc`
    ——**它先 `buildRoster(郡, 模式 2)` 把名單重建，還順手把
    州郡 offset 22（現役將數）寫成名單長度**，長度 >= 50 就放棄。
    `0xe0bc` 讀完了，結論反過來——**remake 的掃描是對的，錯的是觸發次數**：

    ```
    buildRoster(郡, 模式 2)；州郡 offset 22 ← 名單長度   ; 0xe0d5／0xe0f9
    名單長度 >= 50 → 回 0xFFFF                            ; 0xe102
    名單長度 = 0
    for i = 0 .. 349:                                     ; 0xe1c5 比的是 0x15e
        人物[i].勢力 == 0xFF → 跳過（在野）
        人物[i].勢力 == 郡的所屬 → 跳過（自己人）
        人物[i].身分 == 0 → 跳過（君主）
        r = RND(15) + 80                                  ; 0xe161
        人物[i].忠誠 >= r → 跳過                          ; 0xe17e
        收進名單                                          ; 0xe1b4
    ```

    **每個合格的候選都抽一次**，掃的就是全部 350 人——remake 同形。
    235 次 ≈ **一次觸發**（約 222 個候選）＋ 十來次入口擲骰；
    remake 的 1155 ≈ 觸發五次。差別在 `本回合預算 >= 100`（`0xe438`）
    那道：remake 的 `purse` 比原版的 `es:[0x3d16]` 寬。
    **本回合預算的模型改好了**（`0xe9a1`–`0xe9fc`）：

    ```
    索引 = 等級×4 + 月 mod 4
    es:[0x3d16] = 係數表[索引] × 州郡 offset 18（金）× 0.01
    ```

    分母是**當下的郡的金**，而且六張表之前各重算一次——不是一路扣下來
    的錢包。remake 原本拿剩餘錢包當分母，越後面的表越窮，而挖角那道
    `預算 >= 100` 反而過得太寬。另外 `HeadhuntBudget` 吃的是季節事件那個
    季，而分派器的索引是 **`月 mod 4`**（`0xe9a9` 的 `idiv 4`），兩者不
    對齊。兩件一起改之後 **挖角 1154 → 452**（原版 235）、
    總抽樣 2351 → **1649**（原版 1782）。
    ⚠ 預覽（`Plan`）讀不到真的支出，那一條只能用模型化的錢包頂著。
    ⚠ remake 挑到第一個就回，原版是**收滿整份名單再挑**——這一項還沒改。
    ⚠ 挖角還剩 +217 ≈ **多觸發一次**（452 ≈ 2×222，原版 235 ≈ 1×222）。

  **尋訪對上了**（45／47）：盤面上身分 9（在野未露面）的 15 個人
  **全部站在無主的郡**（20／33／34／40），所以 `game.Search` 的
  `hidden` 永遠是 nil、門檻那一次 `roll` 一次都沒跑到。
  **原版是先擲再找人**——那一次亂數無條件抽，這個郡有沒有人可找是後面
  才判的。把 `roll` 移到搜尋之前，32 → 47（原版 45）。
  又一次同樣的形狀：**「有沒有人可找」不影響抽不抽，只影響抽完之後做什麼**。

  **賞賜金帛已經對上**：`0xd5e6` 走名單、跳過君主，每一位都呼叫
  `0xd302`；`0xd302` 抽一次 `RND(加成/2)`，效果 ＝
  `r + 主事者魅力/3 + 加成`（`game.RewardEffect`），花掉的是「真的換到
  的那一段」，所以同一份預算撐得比「每人 100」久得多。remake 原本只發
  `預算/100` 道，改成貼著名單走。
  **出兵也對上了**（220／233）：入口只有「兵士（百）>= 5」一道
  （`0xb666`／`0xb696`／`0xb6c6` 三個等級同碼），金與米那兩道在
  `0xb47a` 裡、**排在編隊的洗牌後面**——所以被擋下來的那幾次原版還是
  抽過了。remake 原本先擋再編，少抽一整批。而且金看的是**郡的金**
  （州郡 offset 18），不是本回合預算。

  ⚠ **位元組數在對齊完成前是雜訊。** 兩邊共用同一條亂數流之後，只要
  某一支抽的**次數**不同，後面的抽樣全部錯開——所以位元組數不會隨著
  對齊單調下降（量到 372 → 383 → 396），而逐表次數會。
  **判準只看逐表次數**，位元組數等對齊完成再看。
- [ ] **亂數對齊**剩下的部分：
  remake 現在的 `State.roll` 是「年月 ＋ salt」的雜湊，與原版的序列無關。
  要對齊得做兩件事：(a) `Roll` 改抽同一條 LCG，開局從原版的種子接上；
  (b) **每一處消耗的次數與順序都要一樣**。(b) 才是主要工作——分派器的
  順序已經照原版排好了（`0xe926`–`0xec1b`），但每一支常式抽幾次要逐支
  對過（賞賜物品那次的教訓：判準是「消耗了幾次」，不是「結果對不對」）。
- [ ] **剩下的離散差異只有兩個成因**（2026-09-08）：
  1. **郡 1 的移防**——remake 把許攸從郡 1 調到郡 2（`RND(4)` 三選一擲到
     移防），原版這個月沒有人移動。連帶 `+32`／`+33`、在職將 ×2、
     身分 ×2。兩邊各用自己的亂數，這一條要等亂數對齊。
  2. **郡 13 的換軍師**——remake 把勢力 4 的軍師由徐庶（槽 146、勢力 5、
     智 95）換成荀彧（槽 65、勢力 5、智 97、身分 2、站在郡 13）。照
     `0xd7ae` 讀，門檻 95、荀彧 97、身分 2，原版**應該**也要換，可是它
     整個月一個身分都沒動。原版兩個月裡指定軍師走到 37 次只換 1 次。
     **分法**：在月度對拍裡掛 `0xd7ae`，記下每一次的郡、所屬、舊軍師、
     門檻與寫進諸侯 offset 6 的值，看郡 13 那一次到底發生什麼。
- [x] **電腦的回合改成發一道套一道**（`ai.Brain.Act`／`ActPrefecture`）。
  原版的分派器是逐郡、逐表即時執行的，第 n 張表看到的是前 n−1 張改過的
  盤面；`Plan` ＋ `ApplyAll` 是對月初的盤面把十八張表算完再一次套上。
  `Plan` 留著當預覽，`internal/session`、`internal/parity` 與
  `cmd/san1turndiff` 都改走 `Act`。`ErrDeclined` 比照 `ApplyAll` 不算違規。
  量到 439 → 444，**離散欄位一個都沒動**——R32：那 76 筆訓練／武裝不是
  批次套用造成的，判準改看離散欄位。
- [x] **登用人才不挑人**（`0xd0ae`，`L0`）：掃 0–349，所在郡相符且
  身分 ∈ {8, 10} 的每一位都呼叫一次 `0xce8c`，照槽號由小到大；擋住它的
  是 `0xce8c` 裡的每回合預算與 50 位上限。remake 原本只登用一位，已改成
  照席次與錢包發完（`ApplyAll` 會因為套不上去的命令中斷整輪，所以席次
  照成功算，寧可少發）。`TestZZMonthParity` 439 → 439：這個盤面身分 8
  只有 5 個人且分散在不同郡，兩種寫法多半同義，所以是中性的。
- [x] **身分 10 ＝ 剛失去勢力、留在原地**（`L0`、`state.StatusStranded`）：
  只有一種寫法（`0x20183`／`0x201c7`／`0x261f5`／`0x26239`），身分 ← 10、
  勢力 ← 0xFF、所在郡 ← 全域 `0x1bf8`。**讀的地方九處全部與 8 成對**，
  `0x0ff80` 是 `8 <= 身分 <= 12` 的範圍判定——原版沒有一處把 10 與 8 分開，
  唯一的不對稱是行動者加權表（10 是 400、8 與 9 是 0）。
  順帶：身分 12（已故）在對拍那個盤面上有 14 個人，**不是填充專用**。
- [x] **指定軍師（`0x5694` → `0xd7ae`）逐行讀完**（`docs/mechanics/70-ai` §2.7）。
  三件事 remake 原本不一樣，都改了：**君主不必在場**（整支常式沒有這個
  檢查，它從州郡 offset 30 取所屬直接改諸侯 offset 6）、**候選不比對勢力**
  （走 `es:[0x58c]`，跟指定太守同一份名單）、**前任回任時他若是所在郡的
  主事者就降成 2 而不是 3**（`0xd8dc` 比州郡 offset 32 的存值，維持
  「offset 32 指到的人身分是 0／1／2」這個不變式）。
  `TestZZMonthParity` 439 → 439：這個月沒發生換軍師，所以是中性的。
- [ ] **原版的電腦對電腦戰役怎麼結算？** 量到的：三個月裡電腦進攻 5 次，
  `0x20200`（戰役入口）5 次、`0x20253`（天數 ← 1）5 次，而
  `0x24f8c`（每天判一次的統帥條件）與 `0x250d4`（三十天期滿）**一次都沒跑**
  （`internal/parity/battleover_oracle_test.go` 的入口計數）。所以日循環是
  玩家在場才跑的。分岔點找到了（`0x20471`，見 `docs/re/05` §7.1）：四個郡
  有任何一個屬於玩家、或 `es:[0x2174] == 0` 就走戰術層 `0x2053c`，否則走
  `lcall 0x1e90:0x8` ＝ **線性 `0x1E908`**，那是電腦對電腦的自動結算。
  `0x1E908` 的前段讀完了（`docs/re/05` §7.1）：設四個軍力、清四個 double
  累計器、逐支部隊累計 `部隊[+32] × 0.01` 與 `部隊[+30]`，兩邊各自把前者
  除以部隊數（平均）、後者加 `0.0001` 存成除數。勝負寫在 **`0x1f538`**
  （`docs/re/05` §7.1）：逐日扣兵力、一方歸零就分勝負、三十天期滿直接判
  守軍獲勝。**整支讀完了**（`docs/re/05` §7.1）：每三天按當下戰力的 1 %
  消耗隨軍的米（糧盡即敗），每天在 `天數 <= RND(11) + 20` 的條件下互相削減戰力
  （`新守方 = max(0, 守 − 攻 × 攻方品質)`，攻方對稱），戰力歸零或三十天
  期滿就分勝負。四個工作量都對回 §5 的部隊欄位（offset 30 兵士數、
  offset 32 綜合能力）。軍團 offset 10／12 的標法量過了：**10 是部隊數、
  12 是將領人數**（`docs/re/05` §3.3 原本標反，R30）。
  已實作成 `battle.AutoResolveAI`，`game.fight` 照 `0x20471` 的分岔選路。
  量出來月度對拍 442 → 442、欄位計數一模一樣——這個月 remake 的 AI 一次
  進攻都沒發動，那條路沒被走到，所以**它不是兵力／訓練／武裝那三欄差異的
  成因**。
  傷亡的寫回也讀出來了（`0x1f6fe` 頭段算比例、`0x1f9fe` 逐人乘上去）：
  **存活比例 ＝ 結束時的戰力 ÷ (開場戰力 ＋ 0.0001)**，四個軍力都乘、
  贏的那一方也折損；原版接著把所在郡清成 0 再重新安置。已實作進
  `battle.AutoResolveAI`（安置沿用 `game.settle`）。
  呼叫鏈也接起來了：`0x1ecfc(主攻郡, 主守郡)` 就是這一仗（開頭算動畫位置、
  接著跑日迴圈呼叫 `0x1f538`），`0x1f6fe` 是打完的收尾。收尾除了傷亡還做
  兩件郡的善後：米夾在 0–30000，**民眾忠誠掉 `RND(10) + 1`**（四個郡各一次），
  這兩件 remake 還沒做。
  收尾那一段整段讀完了：**守方那一郡**的民眾忠誠 `−RND(10)−1`、土地價值
  `−RND(10)−1`（都夾 0）、洪水率 `+RND(7)+2`（夾 100）、物價 `+RND(18)+2`
  （夾 70），四個都打在同一個郡；另外把四個軍團的隨軍金與米收進那個郡，
  各夾在 30000。前四項已實作成 `game.ravageBattlefield`。
- [x] 錢糧收進戰場那個郡（`0x1f82f`／`0x1f8ae`）已接：`game.settle` 加了
  `autoAI` 分支，**不管誰贏都把四個軍團的錢糧收進守方那一郡**，各夾 30000。
  與玩家那條「補給跟著自己走」分開，兩條規則本來就不同。
- [ ] **把戰後收降接進安置流程**。判定本身讀完也實作了
  （`game.WarRecruitResistance`／`WarRecruited`，`docs/re/05` §7.1）：
  君主不收、郡裡現役將滿 50 不收，否則
  `勝方人望 >= max(謀略, 戰力) ＋ 牽絆同勢力時的 60 − RND(30)，再 ÷ 2`。
  分流也讀出來了（`docs/re/05` §7.1 的三條路表）：收得下來 → `0x1feba`
  收編；君主或郡的在野將滿 50 → `0x10050` 印訊息；其餘什麼都不做——
  而「什麼都不做」等於**留在戰場郡**（所在郡在 `0x1fd4f` 已經設過）。
  已實作成 `game.placeAfterAIBattle`（只走電腦對電腦那條）：所有生還者的
  所在郡都設成戰場郡，敗方逐一跑收降——收得下來就改勢力、降成一般武將、
  忠誠等於勝方人望（原本是軍師的話舊主的軍師槽清空），收不下來的維持原本
  的勢力留在那裡。⚠ 這是**量不到的改動**（月度對拍那一個月 AI 一次都沒
  進攻），靠的是 `0x1fb26` 的 `L0` 與一條單測。
  `0x1feba` 收編改的四個欄位也讀完了（忠誠 ← min(100, 勝方人望)、
  勢力 ← 勝方、**身分 ← 3 一般武將**、所在郡 ← 戰場郡；原本是軍師的話
  舊主的軍師槽清成 `0xFFFF`），忠誠那條實作成 `game.WarRecruitLoyalty`。
  ⚠ 接進流程要有「AI 真的打起來」的盤面才量得到，`battleover_oracle_test.go`
  那一套可以用。
- [ ] **其他從 `garrisonIndices` 建命令的行為也要各自確認名單條件。**
  調整兵力量過了（模式 2，不比對勢力）；訓練、徵兵、購置武器、出兵各自
  用哪個模式還沒逐支對回 `buildRoster` 的七個入口。
- [ ] **電腦諸侯「進攻」那條分支帶多少錢糧還沒解。** `0xb2b4` 算好的
  `es:[0x58a]`／`es:[0x20de]` 只有移防那兩條（`0xb50e`／`0xb568`）推給
  `0xb706`；打敵國那條（`0xb5c2`）直接 `lcall 0x2020:0x0`，四個參數裡
  沒有錢糧。戰役層自己有一份編隊（`0x236af` 也是 `100 × es:[0x2e62]`），
  要從那邊讀。remake 現在用 `game.HalfSupply()`（帶一半）頂著。
- [ ] **出兵的「兩次編隊」在 remake 是兩次 `muster`，但擲骰的鹽是自己編的。**
  原版兩次都真的重洗，序列不同不影響正確性，可是**這一點沒有對拍**——
  要驗得攔 `0xb706` 的清單，與 `0xb2b4` 那一次比對「兩隊不一定相同」。
- [ ] 挖角**成功那一側只有一個樣本**（成功後的忠誠）。要多幾個得把
      我方君主的魅力與人望都拉滿，讓門檻壓過抵抗
      （`0x5514` 八格全是空操作，不必讀）
- [x] **賑民真的扣兩次錢**（`0xc93a` 整份預算 ＋ `0xc9d6` 照實際增幅再一次）：
      99 次實跑每一次都收了，`70-ai` §2.13.3 從 `L3` 升 `L1`，
      `game.Relief` 也跟著收兩次
- [x] **賑民對拍完成**（`docs/playtest/02`）：三個月 99 次、六個等級全走到，
      逐次相同。同時定案兩件事：量不是 `max(…, 5)`（只有 `<= 0` 才換成 5）、
      `量 × 預算` 是 16 位元有號乘法會溢位，而忠誠只有上限沒有下限
- [x] **`es:[0x3f08]` 是季節**（0–3）：`0x15c1e` 拿它跳到春／夏／秋／冬
      四支常式（`docs/re/06`）。所以 AI 回合預算表的「四個相位」
      就是四季——**電腦諸侯的花錢比例隨季節換**
- [x] **解 `0x5594`（購置武器）**：`INT 34h`–`3Bh` ＝ ESC `D8`–`DF`
      （`3Ch` 是段前綴形式），照這個對照把浮點指令流換回來。
      **買到滿編**（缺口 ＝ 兵力 − 武器數），花費 ＝ 缺口 ÷ 100 截斷，
      超過本回合預算就買到錢用完；**武裝度是有武器的兵佔多少百分比**
      （`docs/re/03` §1.4、`docs/mechanics/70-ai` §2.13）
- [x] **分派表全部是 AI 決策**：分派器一個月跑 32 個相異的郡，玩家的郡
      一次都沒有（`TestZZDispatchScope`）
- [ ] AI 勢力共 35 個郡而只跑了 32 個——差的三個為什麼沒跑
- [x] **解 `0x5634`（登用人才）**：兩個等級參數是**費用與加成**
      （30/20/10/0 與 0/10/20/40）。說服力 ＝ (人望×3 ＋ 太守魅力)÷3 ＋ 加成，
      難度 ＝ 智÷(RND(4)+3) ＋ 武÷(RND(4)+3)，前面有一道**牽絆閘門**
      （人物 offset 14 指到的人效力於誰）。成功後忠誠 ＝ 人望÷2 ＋
      RND(人望÷2) ＋ 加成（`docs/mechanics/20-personnel` §4.1）
- [x] **人物表 offset 14 的用途**：登用的牽絆閘門（`docs/spec/003` §2.6）。
      是人物槽號這件事由 `0xce8c` 確定，代表什麼關係還沒定
- [ ] 原版登用收身分 8 **與 10**——10 是什麼、有好幾位候選時挑誰
- [x] **賞賜物品對拍完成**（`docs/playtest/02`）：186 次、四種寶物。
      說明書 p.24 的數字是**下界**，原版在上面再加 `RND(2)`；
      忠誠增幅是 `RND(30) + 新能力 ÷ 2`，**美女那一支是 `RND(50) + 50`**。
      收禮的是名單裡第一個「該能力 > RND(20)+60 或者是君主」的人
- [ ] 反組譯 `AA.EXE` `0x495f2` 的三個引用點 → `.OKR` 的索引怎麼算（F62）
- [x] **助攻軍／助守軍與出兵型計謀接起來**（說明書 p.26）：原版三支
      直接呼叫 `battle(攻方郡, 攻方援郡, 守方郡, 守方援郡)`（`0x20200`），
      驅虎吞狼我方不參戰、遠交近攻拿出使郡當助攻、聯合出兵四格全滿
      （`docs/re/07` §4、`game.Aid`／`launchCampaign`）
- [x] **五種謀略不花錢**（`L0`）：分派器與五支效果常式都沒碰州郡
      offset 18；原本 300/200/100/100/300 的 `L3` 猜測退場
- [x] **偽書使疑的公式**（`0x2d1fa`）：忠誠低於使者魅力的人
      `忠誠 × (250 − RND(魅力÷2) − 魅力) ÷ 250`
- [x] **軍師會先預測計謀成不成**（`0x2c777`）：`RND(5)+80 <` 軍師謀略
      才開口，`RND(18)+80 >` 軍師謀略則預測被隨機化
- [x] **州郡 offset 32 ＝ 主事者的人物槽號**（`docs/spec/003` §3.4）——
      推論版本被推翻兩次的那一格，原版直接存著
- [x] 聯合出兵前三個選單的候選條件（`0x2d95f`／`0x2d9f8`／`0x2dab0`）：
      我方的郡／出兵郡的敵方鄰郡／目標的我方鄰郡但不是出兵那一郡
- [x] **內政兩項對拍**（`docs/playtest/02`）：四個月 58 次逐次相同，
      六個 AI 等級全部走到，`70-ai` §2.5 升 `L1`
- [x] **老死對拍**（`docs/playtest/02`）：一年 21 次逐次相同，
      `20-personnel` §6 升 `L1`
- [x] **四季事件一年各只跑一次**（R16）：`es:[0x3f08]` 是月份不是季節，
      1 春 4 夏 7 秋 10 冬，其餘八個月什麼都不跑。remake 的天災頻率
      原本是原版的十二倍
- [x] **蝗害對拍過了**（`docs/playtest/02`）：把分派器讀到的月份改成七月，
      八個月都跑秋季常式，地力與米各 8 次全部落在算式的範圍內。
      段基底不必猜——掛在讀取指令上用執行期的 ES
- [x] **繼承的人望對拍**（`docs/playtest/02`）：三次逐次相同，
      而且跨在四捨五入的兩側；`80-victory` §2.5 升 `L1`
- [x] **電腦諸侯的計略對拍完成**（`docs/playtest/02`）：整條鏈是
      `0xe68e` 一支，得手之後呼叫的是 `0x2d1fa`（**偽書使疑**）不是
      `0x2d6e0`；`0xe7aa` 是死碼（R22）。六個月 132 次進入、13 次抽中、
      4 次得手、15 個人的忠誠逐次相同，`30-diplomacy` §4.2 升 `L1`
- [x] **選出行動者對拍完成**（`docs/playtest/02`）：兩個月 40 次，
      38 次郡裡有兩種以上身分（加權表分得開），逐次相同
- [x] **賞賜物品對拍完成**——見下方那一條
- [ ] `[HARD]` **對拍的判準要在 hook 的回呼裡當場算，不要事後回讀狀態。**
      三個地方各踩一次（指定軍師、選出行動者、挖角）：常式自己會改它
      剛讀過的欄位，或者同一輪後面的行為會改。事後回讀比的是**後來的**
      盤面，而錯誤訊息長得像「原版挑錯人」（`docs/playtest/02`）
- [x] **指定太守與指定軍師對拍完成**（`docs/playtest/02`）：28 ＋ 69 次。
      `0xf17:0x490` 的排序鍵是魅力；軍師挑的是**名單裡最後一個**超過門檻
      的人（門檻在迴圈裡不更新），不是謀略最高的
- [x] **尋訪人才對拍完成**（`docs/playtest/02`）：三個月 33 次、六個等級
      全走到。三個立即數全部隨等級變（R26），`game.SearchTierFor` 取代
      原本的兩個常數，`game.Search` 也改用原版的判定
- [x] **登用人才對拍完成**（`docs/playtest/02`）：四個月 128 次、六個等級
      全走到、成功 61 次，費用／加成表、難度、說服力、兩道牽絆閘門與
      成功之後的忠誠逐次相同
- [ ] `[HARD]` **呼叫端的返回位址一律逐支反組譯量，不要從第一支外推。**
      同一個坑踩過兩次（賞賜金帛、登用人才）：分派常式的間隔看起來固定，
      但推的立即數長度不同就會差一個位元組，而漏掉的等級在輸出裡
      看起來只是「剛好沒輪到」
- [x] **調整兵力對拍完成**（`docs/playtest/02`）：兩個月 40 郡次 279 人
      逐次相同。份額的捨入不是整數的四捨五入（R25），
      remake 多出來的「剩餘塞回」迴圈也拿掉了
- [x] **訓練兵士對拍完成**（`docs/playtest/02`）：兩個月 260 次、六個等級
      全走到，除數表與增幅式子逐次相同
- [x] **購置武器對拍完成**（`docs/playtest/02`）：三個月 505 次逐次相同，
      `70-ai` §2.13 升 `L1`。這一支的 `0.01` 是 float64，與徵兵那條稀釋的
      float32 不同（R24）
- [ ] 買武器有兩條**取樣不到**的路：原版把缺口加到還沒截斷的乘積上
      （兵力 < 100 時差 1）、而且沒有夾 100 的上限。改之前要先讀玩家
      那一支買武器的常式，確認數量有沒有被夾在缺口以內
- [x] **賞賜金帛對拍完成**（`docs/playtest/02`）：三個月 605 次、六個等級
      全部走到，加成表與效果／增幅／花費三條式子逐次相同，
      `70-ai` §2.13.4 升 `L1`
- [x] **徵兵對拍完成**（`docs/playtest/02`）：三個月 597 次，人口、
      訓練度、武裝度三欄逐次相同（無容差）。稀釋乘的是 float32 的 0.01
      所以整除時少一格（R24），`70-ai` §2.13.1 升 `L1`
- [x] **買米對拍完成**（`docs/playtest/02`）：四個月 136 次、六個 AI 等級
      全部走到、逐次相同，`60-economy` §1.3 升 `L1`。除數是
      `[10,10,10,10,9,8]`（R17），第六支寫成 `sar` 所以是 ÷8 不是 ÷3。
      完整算式也解了：`剩下的金 = clamp(金 − 缺口 ÷ 匯率, 0, 30000)`，
      買到的米 ＝ 花掉的金 × 匯率
- [x] **關寨的規則整組讀完了**：三道前置（上限 5、物價 × 100、謀略 > 79）
      加上位置的兩道門（`DS:0x7134` 的地形表、通道格不能蓋），
      寫入是 `(格 & 0xF6) | 6`。remake 的 `CanBuildFortOn` 照做
- [ ] 關寨的**對拍**：算式有碼與提示字串兩條獨立證據，但沒實跑過
      （要走完整的玩家選單，含選將與地圖游標）
- [ ] 五種謀略裡另外四支的**對拍**（偽書使疑已經對過，其餘四支要走
      完整的玩家選單才發得出來）
- [x] **內政兩項的量**（`0x1a6d2`／`0x1a93a`）：玩家是
      `max(謀略−50,0)÷12` 與 `謀略÷10`，**謀略不足就是加 0**；
      電腦走另一條，三個常數隨 AI 等級變（`0xba9c` 起六支，
      底 `[50,60,60,50,40,50]`、除數 `[10,15,15,14,12,10]`）——
      原本文件寫「只有一個常數不同」是錯的
- [x] 「徒手開墾效果減半」不是原版行為：金扣到 0 為止、效果照給
- [x] **君主繼承**（`0x14a40`–`0x14ce6`）：候選是整個勢力（不限郡）、
      依魅力排序、人望 ← 四捨五入(繼承者魅力 × 原人望 ÷ 100)、
      軍師接位就清空軍師欄；找不到人就是「無人繼承」、勢力滅亡、玉璽釋出
- [x] **人物身分 12 ＝ 已故**（`0x14a5d`），劇本 001 那一位的謎底
- [x] **諸侯 offset 0 ＝ 操縱方**：1 玩家、2 電腦、`0xFFFF` 已滅亡
- [x] **州郡 offset 25 ＝ 關寨數**，等於戰場地圖上地形碼 6 的格數
      （六個劇本 42 個郡逐一數過）；建築關寨的三道門也都讀出來了
      （上限 5、費用 ＝ 物價 × 100、謀略 > 79）
- [x] **賣米的匯率**：與買米同一條 `(100 − 物價) ÷ 10`，
      買是「一金換 rate 米」、賣是「rate 米換一金」；
      糧倉與財庫上限都是 30000
- [x] **存讀檔（其他 → 儲存，六個槽）** → `internal/save`（`docs/formats/05`）
- [x] **解 `BASEPRO.SVn`／`BASEPRE.SVn`／`SAVENAME.SVP`**（`docs/re/08`）：
      年月、難度、六個選項、玉璽、月內進度；字模是自創君主的姓名；
      名稱表六筆 21 byte。remake 已改寫成原版版面，年月與選項不再走 JSON
- [x] **讀原版存的進度**：`save.ReadOriginal`／`ListOriginal`，
      命令列是 `-orig-load N`。出貨的六個都讀得進來（第 2 個除外——
      它存的難度 15 超出原版係數表的 11 格，報錯不夾）
- [x] **存讀檔兩個方向都對拍過了**（`docs/playtest/02`）：載入方向
      `TestOriginalSaveLoadsIdentically`（連帶修好「所屬載入後要重算」，
      並釘住兵士／在職將是快照不是推導值）；寫出方向
      `TestRemakeSaveLoadsInOriginal`——把六個項目換進**原版目錄複製品**
      的 `DATA2.GRP` 再讓原版讀，三張表 19,220 個位元組完全相同
- [x] **戰役的日循環驅動起來了**（`docs/re/05` §7.0）：主戰場的命令提示
      讀的是**一個 ASCII**（`0x27aba` 的 `sub $0x30; cmp $8; ja` ＋
      `cs:0x13a` 九格跳表），不是數字欄位、不要送 Enter；下完命令接的是
      Y/N 確認（`0x1538c`）。所以休息一天就是 `0` 再 `Y`。
      送 Enter 答 Y/N 會讓天數停在 1，而畫面看起來只是「還在問同一個人」
- [x] **戰役裡真的打起來的那幾天對拍過了**：移動花費逐步相同，
      交戰的傷亡逐次相同（`docs/re/05` §3.6）。計謀那幾支還沒走到
- [x] **原版的讀檔入口是主選單第 2 項「載入舊進度」**，不是遊戲內選單
      ——所以 remake 的遊戲內選單沒有讀檔是對的（`docs/spec/005` §6）
- [x] **主選單的六個項目接上行為了**：`cmd/san1` 開機走開場詞 → 主選單 →
      選年代／選君主／選難度，另外三項是載入進度、音樂欣賞、離開。
      選單那幾層的版面是 remake 自排的（原版那一段在 `DATA0.GRP`，
      碼段 dump 涵蓋不到），內容是原版的資料
- [x] **主戰場接上原版素材了。** 基準畫面有了
      （`TestZZBattleKeySweep` 的候選 4 走到「畫戰場」，`SAN1_SHOTS` 存圖）。
      幾何也解了（`0x22745`–`0x22781`）：

          x = 欄 × 48 + 56
          y = 列 × 32 + 36        奇數欄再 +16
          地形碼（低四位）> 10 就不畫
          lcall 0x36a8:0x00f8(x, y, 地形碼, 0)

      **兩種版面都塞得下**：12×7 是 x ≤ 584、y ≤ 244；8×10 是 x ≤ 392、
      y ≤ 340。（先前記成「欄×32／列×48」是把兩個軸讀反了，那個讀法
      8×10 會算出 y=488 超出畫面。）
      **圖塊也解了**：`EICON.GRP` 不是一整塊圖，是**三十六筆 `.IMG`
      首尾相接**（表頭 4 ＋ 四個平面各 192 ＝ 772，27792 ÷ 772 ＝ 36），
      每張 48×32，表頭自己寫著高 32 寬 48。**圖塊編號就是地形碼**：
      拿基準畫面抽一格的四個平面去 grep，命中的記錄編號與該格地形碼
      相同（樹林 8 對 #8、平原 7 對 #7，四格四中）。
- [x] **其餘格子對不上的原因也解了**：基準畫面停在**紮寨**那一步，
      原版把可以下寨的格子照常畫、其餘蓋 `8x8AND0` 這張遮罩
      （偶數列的偶數欄留著，留四分之一），留下來的像素與圖塊
      **完全相同**。全中的四格就是可以下寨的那一小片
- [x] **整張戰場圖驗過了**：換成紮完寨之後的基準畫面，78 格裡 **73 格
      逐像素全中**，差的五格正是五支部隊站的位置
- [x] **部隊的標記是旗幟**（`DATA1` 的 `WFLAG*`）：四組 `D0`／`D1`／`A0`／
      `A1` ＝ 主守／助守／主攻／助攻，每組六張，0–4 的旗面上印
      **帥、先、左、右、後**（即中軍、先鋒、左軍、右軍、後軍），
      第六張是 16×15 的城門圖示。旗畫在**格子左上角加 (8, 0)**，
      兵力牌 40×17 在旗下 15、字用 `%5d`、字色 `DS:0x796a`
      （10、11、12、13）。天氣圖示 `WEATHER1` 在 (8,155)、框線 `FBRC0`
      在 (64,268)，也都 100%
- [x] **選到的部隊畫成反白**（每格顏色 `^15`）：基準畫面上陳就的中軍與
      `WFLAGA00` 取補數後 360 格逐格相同。五個標記全數 100%，
      沒有第六個——「守軍的標記找不到」是拿原色去掃的結果（R20）
- [x] **整張主戰場都接上素材了**：地形、部隊的旗與兵力牌、底紋、左欄
      四個小框、三個面板、肖像與肖像框。面板裡的字是 remake 自排
      （位置與字色照原版），字級與原版的 32×32／16×16 不同
- [x] **天氣的原版編號是晴 0、雨 1、風 2**（`DS:0x7892` 的名字表），
      不是 remake 列舉的順序——`Weather.OriginalIndex` 負責換算（R21）
- [x] **場地四周的階梯狀邊框接上了**：`0x21ee2`／`0x220f0` 的十六條線
      逐條抄，**邊框畫在圖塊之前**、格子以外的空隙露底紋不塗黑。
      整片場地 121560 格逐格相同，只挖掉五支部隊站的格子（紮過寨的
      格子連地形都變了）
- [x] **開場詞接上了**：`SANTL`(0,49)／`SANTR`(320,49) 各 94400 格全中，
      詞的版面（欄距 42、列距 24、淺青配黑影、「詞曰」低一列）量自
      寫字前後兩格相減。詞的字原版是點陣、容器裡沒有對應的 Big5 字串，
      remake 那一份照畫面謄（`L1`）
- [x] **開場的三英圖解了**：`DATA1` 的 `TITL0`–`TITL3`，四塊 160×400
      並排取第 0–349 列，224,000 格逐格相同（R23）。`TZUE`／`TZUE1` 是
      〈臨江仙〉的詞、`TITFONT` 是「三國演義」四個字、`PRV0`–`2` 是
      工作人員字幕、`CMARKL`／`CMARKR` 是智冠的商標畫面
- [x] **`es:0x5b02` ＝ 有沒有人在玩**：開局「請問有幾人玩(0-16)」答 0 就是
      `DS:0x5fcb`「電腦自動示範模式」，延時壓到 2、查看電腦戰役開啟、
      旗標設 0；有人玩就設 `0xFFFF`。**絕嗣檢查只在有人玩時做**
- [x] **`PARTNSAV.FIL` 不是遊戲檔**：開頭是 `CD 20` 加上
      `MIRROR 6.M`／`saved partitions`——MS-DOS 6.x 的分割表備份，
      `AA.EXE` 沒有引用。與加強版的 `NAME00n.SHA` 同一類，
      打包 bundle 時掃進來的；內容不轉錄
- [x] **遮罩解了**：`8x8AND0`–`3` 是四張 8×8 的 AND 遮罩，紮寨那一步蓋的
      是 `AND0`（偶數列偶數欄留著）——78 格裡 73 格與「圖塊 AND 遮罩」
      逐像素相同，照常畫的正好是可以下寨的四格
- [x] **州郡的填色解了**：`EGAFILL.PAL` ＝ 16 × 64，一個勢力一塊 8×8
      的圖樣（四塊純色、十二塊 2×2 網點），網點對齊畫面座標。
      拿原版剛載完第一個進度的畫面逐格比，35 個有主的郡裡 34 個全中
- [x] **地圖是「載完就畫」，之後不重畫**：載入後那五道提示（人數／君主／
      難度）改掉的東西不會反映到畫面上。潁川（存檔 5、記憶體 4）與
      南海（存檔無主、記憶體 14 ＝ 玩家自創的君主）兩格，畫面上畫的是
      **存檔的值**。拿執行期記憶體去對畫面顏色會在這兩格得到假的不符
      ——`save.ReadOriginal` 也會重算（`0x1e394`），所以填色的測試讀的是
      `state.LoadScenario(c2, "SV1")` 那一份
- [ ] **長沙的填色對不上**：原版畫圖樣 0（純淺紅），所屬寫的是 2
      （圖樣 2 是純淺綠），803 格逐格都不同。排除過玩家顏色、本據顏色、
      「只有一個郡的勢力」、太守與君主所屬不一致四種解釋。
      2026-09-08 再排除三種：**原版執行期記憶體裡長沙的所屬也是 2**
      （`TestLoadedPrefectureOwnersMatchTheSave`）；州郡記錄位移 0–54
      沒有任何一格滿足「34 個郡等於所屬、長沙等於 0」；諸侯記錄前 24 個
      位元組也沒有一格等於自己的槽號。**依序灌（後灌蓋先灌）也不是**——
      42 個郡灌進同一張圖，長沙那 803 格照樣全錯
      （`TestPrefectureFillsInOrderMatchTheOriginal`）
- [ ] `BASEPRO` 位移 `0xAC`（`DS:0x5b00`）整個碼段只有存與讀兩處引用，
      出貨的六個進度都是 0；看起來是死變數，但沒有第三條證據
- [ ] **自創君主的顯示名**：存檔裡的諸侯槽 14 指向人物表的填充筆 346
      （名字是全形標點「，、。」），但原版畫面上寫「新君主」。
      名字存在哪還沒解——可能在諸侯記錄未解的 70 個位元組裡，
      也可能原版對自創君主一律用固定字串（`AA.EXE` `0x47941` 附近有
      `新君主出現!!`）。
- [ ] 用 `unlzexe` 驗 `DATA4`／`DATA5.GRP` 的 LZEXE 假說（解壓後長度應為 810,396／457,424）
- [ ] 從執行檔／資料檔取 1728 組密碼表（§3.05）——**對拍不擋**
      （任何四位數都過得去）；整張表只剩考據價值
      掃過 `AA.EXE`：**沒有以 packed BCD 或 16 位元小端數字連續存放的 3,456 byte 表**
      （唯一夠長的候選 `0x424c4` 起 17,204 byte 是 BSS，99.98% 是零）。
      提示字串在 `0x507ec` `請輸入密碼(-%s-)`，配 `0x50833` `密碼輸入錯誤`
      與畫面 `SCG05.IMG`。剩下的可能：密碼是**算出來的**不是查表的，
      或表在打包過的 `DATA5.GRP` 裡。
- [ ] 複核手冊 p.42 洪水提升率的四個兩位數值（掃描字級極小）
- [x] 說明書整理成 `docs/reference/01-manual-*`（5 份，1,758 行）
- [ ] 社群資料整理成 `docs/reference/02-web-*`
- [x] **解 `.IDX` 索引什麼** → 結束位移，`docs/formats/01`
- [ ] `DATA2.GRP` 兩版逐位元組 diff，差異落點分佈
- [x] **兩版的執行期資料段對讀** → 規則層的差異只有難度（`docs/mechanics/90` §6、
      `docs/spec/004`）。做法是同時刻對讀：`TestZZDumpBaseAtBoot` 走
      `TestZZBootPlus` 那條完全相同的序列，先扣掉原版自己在兩個時刻之間
      改寫的 94 個位元組，剩下的才算版本差異
- [x] **加強版難度 11–20 那條規則** → `0x2296b` 的 `cmpw $0xb, es:0x310a` ＋ `jge`
      跳過「守方統帥全滅 → 攻方勝」整段；另有加強版獨有、不看難度的
      「總兵數為 0 者敗」（`0x229ba`）。`L0`，`docs/re/05` §8.2
- [ ] `DS:0x506a` 那張 4 byte 記錄的表與 `DS:0x50e8` 那個 word 的語意
      （兩版唯二剩下的差異，都沒有絕對定址的參考指到它們）
- [x] **誘敵與圍攻** → 都交給共同的交戰結算 `0x2a224`，倍率表 `DS:0x81a2`
      八格。誘敵推參數時攻守對調（R13），圍攻掃六鄰格、與目標不同陣營的
      各打一次（`docs/re/05` §4.2b–4.5）
- [x] **一騎討落敗的處置** → `0x31b04` 的 `RND(7)`：0 死、其餘被擒
      （`docs/re/05` §9.4）
- [x] **部隊記錄 offset 20** → 弓箭剩餘次數，初值 `0x272eb`
      ＝ 兵數加權的平均武裝度 ÷ 20（`docs/re/05` §4.2b）
- [x] **`0x29884`** → 弓箭的目標列舉：六方向各連走兩步，第一步之後
      重算奇偶列。「相間一格」因此從 `L2` 升到 `L0`

### 之後要回頭確認的

- `LICENSE` 的「灰色地帶」與「第三方素材」兩段目前是**照計畫填的**，
  發行前（M8）要用 `git ls-files` 對一次實際內容。

### R39（2026-09-09）：「玩家出局的判準是最後一個郡失守」

**原斷言**：`Session.PlayerAlive` 問 `Faction.Alive`——某個勢力的最後
一個郡被打下來就標記死亡，之後不再輪到他。

**當初的證據**：沒有原版證據。那是照「一般策略遊戲怎麼判」補上的。

**推翻的證據**：`0x15924`（`L0`）掃十六個諸侯槽，判準是
`offset 0 == 1`（玩家操縱）**而且** `offset 2 != 0xFFFF`（君主欄）。
整支常式沒有讀州郡表。**輸的定義是「沒有繼承人」不是「沒有領地」**，
所以君主還活著、領地被打光的玩家，原版照樣讓他繼續。

實跑對拍（`TestNoHeirEndsTheGame`）：同一個盤面不動君主欄時走
`0x1595b`（遊戲繼續），把玩家勢力的君主欄清成 `0xFFFF` 之後走
`0x15962`（印 `DS:0x97d4` 並結束）。

**配套**：沒有繼承人時要把君主欄清成哨兵（`events.go` 的陣亡處理寫
`f.Lord = -1`）。不清的話那一格會一直指著已故的那一位，判定永遠成立。

**教訓**：**「這個遊戲的輸贏怎麼算」不能照類型慣例補。** 沒有原版證據
的規則不要進 `internal/`，尤其是這種「看起來一定是這樣」的——
它不會有人質疑，因此也不會有人去查。

### R40（2026-09-09）：「水淹的目標必須在水上或岸邊」

**原斷言**：水淹的位置條件是「目標站在水上，或者六個鄰格裡有水」，
水包含淺水與深水。

**當初的證據**：說明書 p.33 的原文。`L2`。

**推翻的證據**：`0x2bfa2`（`L0`）掃的是**目標的六個鄰格**，
而且 `cmp al,3 / jne` **只認地形碼 3（淺水）**——深水（碼 4）不算，
目標自己站在水上也不算。remake 的 `nextToWater` 換成 `nextToShallow`，
單測改釘量到的規則。

**教訓**：說明書的位階低於執行檔（`CLAUDE.md` §4）。說明書那句話**涵蓋**
碼裡的行為，所以照它寫的實作跑起來不會錯得很明顯——**條件寫寬了不會
報錯，只會偶爾多允許一次**。這類差異只有把那道門的碼讀出來才看得到。

### R41（2026-09-09）：「圍攻每一支參戰部隊的倍率格是 0」

**原斷言**：圍攻對每一支貼著目標的部隊呼叫 `0x2a224(那一格的部隊, 目標, 0)`，
倍率固定是 `DS:0x81a2[0]` ＝ 80。remake 的 `SiegeStrike = 0` 照這條寫。

**當初的證據**：`0x2bc95` 的 `模式 = 0`。那一行沒有讀錯——**錯在把初值
當成傳進去的值**，漏了後面兩處 `incw -0xe(%bp)`。

**推翻的證據**：`0x2bdf8` 推的第五個參數是 `[bp-0xe]`，而它在兩處被加一：
`0x2bd04`（目標六個鄰格裡每一支與目標不同陣營的部隊）與 `0x2bd41`
（施法者的領隊謀略 ≥ 0x62 ＝ 98，`[bp+6]`／`[bp+8]` 是施法者）。
實跑（`TestSiegeWithTwoUnits`）量到兩支圍一支、施法者謀略 99 時是 **3**。

**教訓**：**一個區域變數的初值不是它的值。** 反組譯讀到 `x = 0` 之後，
要往下找完所有寫 `x` 的地方再說「傳進去的是 0」——尤其這種
「初值 0 ＋ 迴圈裡累加」的形狀，只看初值會得到一個完全自洽、
而且在單支部隊的情境下**剛好對**的結論（一支圍攻時模式是 1，不是 0，
但差別只有倍率 80 對 100，測不出來）。

### R42（2026-09-09）：「`DS:0x50e8` 是兩版的資料段差異之一」

**原斷言**：對齊的前綴裡兩版只有兩處不同，其中 `DS:0x50e8` 是
`0x0430`（原版）對 `0x0220`（加強版），候選解釋是語音延遲的預設值。

**當初的證據**：兩份執行期 dump 逐位元組比對，扣掉已知的執行期噪音。
比對沒有做錯——**錯在「扣掉執行期噪音」這一步漏了它**，
而它正是執行期噪音。

**推翻的證據**（`L1`，`TestWhoReadsTheEditionWords`）：
掛讀寫監看跑一個月，`0x50e8`–`0x50eb` 這個 32 位元格子被
**同一支程式庫常式** `0x3eb:0x31a` 讀（`0x034e`，53 次）也寫
（`0x0327`、`0x0333`，合計 212 次）。那一支只有一個呼叫端 `0x19653`，
在逐郡的月循環裡每郡跑一次。**程式自己每個月覆寫它二百多次**，
所以兩份 dump 的值不同只代表停在不同時刻。

順帶排除的：整支 27 條指令，把值乘十倍指令數一條都沒變（不是忙碌等待）；
整個月沒有任何指令讀 BIOS 計時器 `0040:006C`（不是靠時鐘比時間）。
`0x506a`–`0x5077` 同一輪既沒被讀也沒被寫，所以那一列是真的靜態差異。

**把碼段 dump 的窗往下補到 `0x400` 之後，機制整支讀得出來**（`L0`）：
`0x50e8`／`0x50ea` 是那一支自己的**暫存遠指標**，每次呼叫連寫兩次
（先 `0x220` 再 `0x430`，段都是 0），接著 `les bx,[0x50e8]` 取回來解參考
`0000:0430` 與 `0000:0436`，兩個條件都成立才印 `DS:0x50d8` 那個字串。
原版那份 dump 停在第二次寫之後、加強版那份停在第一次寫之後——
**同一支常式的兩個瞬間**，`0x0430` 與 `0x0220` 都是它自己寫進去的常數。

**教訓一**：**「扣掉執行期噪音」不能靠肉眼挑。** 判準是「程式跑的時候
會不會寫它」——那要監看，不是比對兩份 dump。兩份 dump 只告訴你兩個時刻
不一樣，告訴不了你為什麼。

**教訓二**：**掃不到讀取端時，先確認掃描範圍蓋得到。** 這兩處的讀寫端
在線性 `0x41ca` 起的程式庫段，而倒出來的碼段窗從 `0xb000` 起——
掃描結果是零筆，而零筆看起來與「沒有人讀」一模一樣。
（配套：dosgolem 這一輪加了 `WatchReadsAt`，寫入監看本來就有；
`TestZZDumpCode` 也跟著把 `0x400`–`0xb000` 一起倒出來。）
