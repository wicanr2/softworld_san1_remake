# 驗證矩陣

**完成度的唯一數字來源。** README 只連過來，不重複數字。

`CLAUDE.md` §8 的推論等級與版本標記在每一列上。

> **結構化的那一份是 `worklist.json`**（`rulebook/61`）：每一條掛一個跑得起來
> 的 `verify`，狀態由跑出來的結果決定，不由人手打勾。§8 是它的產物。
> 這一份其餘各節是**敘述層**——方法、坑、取捨這些跑不出來的東西。

⚠ 這一份記過「主選單 208,050 點逐點相同」很久，而那個數字建立在「畫面是
640×350」這個錯前提上（R48）。**數字沒錯，前提錯了**，而一張表分不出這兩件事
——那就是把狀態改成由 `verify` 決定的理由。

## 1. 里程碑

里程碑定義在 `CLAUDE.md` §10。

| # | 里程碑 | 狀態 | 卡在哪 |
|---|---|---|---|
| M0 | 環境 ＋ 素材 | **完成** | — |
| M1 | dosgolem 跑得動 | **完成** | 原版走完 chain-load、載三組容器、畫出主選單；`-adlib` 之後音樂也放得出來。主選單那一張**拿 DOSBox-X 交叉驗證過**：兩個獨立實作扣掉右下角那格動畫之後 **261,036 點**逐點相同（畫面是 640×408，`docs/spec/006`）（`tools/dosboxx.sh`、`docs/playtest/03`）|
| M2 | 容器格式 | **完成** | `.IDX` 是結束位移，round-trip 通過，而且**得到原版行為背書**（見 §3）|
| M3 | 文字與字型 | **完成** | Big5 字串抽出、CJK 畫布、字型涵蓋率有測試 |
| M4 | 靜態資料 | **完成** | 三張表對得回原版執行期記憶體，**逐位元組相同**（見 §3）|
| M5 | 規則層 | **完成**（AI 那一層已逐位元組對齊）| `70-ai` 十八張分派表全部解開（144 個 far pointer 逐一解析；判斷式從碼讀出），而且**十八張全部有實跑背書**；戰術層的規則幾乎整層是 `L0`（地圖、地形、移動、戰力、傷亡、六種計謀、三十天判定）。一騎討（含落敗的處置）、誘敵與圍攻都解出來了（`docs/re/05` §4.2b–4.5、§9.4）；水災、瘟疫與人口成長的算式也解出來了（`docs/re/06`）。全部 `L0`。`90-version-diff` 的規則層差異解出來了（難度上限、係數表、戰役勝負判定的兩條，全部 `L0`）。出口條件是「`10–80` 至少 `L1` ＋ 有單測」，九份都遠高於那條線。**`docs/mechanics/00-index` 的「還缺什麼」欄現在九章全部是「—」**：六種戰場計謀與五支戰略層謀略都在原版裡各走過一次、絕嗣結束正反兩面各推過一個月、指定軍師的名單順序量到 12 份全部按鍵遞減、人物位移 6 是姓名的結尾、兵力的單位是「人」；原本掛著的兩處資料段語意也解了——`DS:0x50e8` 是執行期暫存不是版本差異（`CONTEXT.md` R42），`DS:0x5040` 那張表是繪圖常式的遠指標表|
| M6 | 引擎可玩 | **完成** | 出口條件四段都對拍過了（§3）：開新遊戲的盤面逐位元組相同、**一個月的狀態轉移差 0 個位元組**、**戰役的逐日含移動花費與交戰的傷亡 56 個欄位全中**、**存讀檔兩個方向逐位元組相同**。兩個已知差異寫在 §3：剛載入時兵士／在職將是快照不是推導值；計謀那六支還沒在實跑的戰役裡對拍過 |
| M7 | 多語系 | **完成** | 介面、人名、郡名全覆蓋；槽位溢出只處理了地圖 |
| M8 | 發行 | **本機四包已交付；跨平台原生驗收與公開發布未完成** | `dist-all/v.1.0.0-20260923/` 由提交 `d74a735` 乾淨建置；Linux／Windows／macOS amd64／macOS arm64 四包均有 LICENSE、README、合法字型授權與固定清單，型別、壓縮完整性與 SHA-256 通過。Linux 包帶唯讀原版素材讓 base／plus 各持續啟動 25 秒；最終壓縮包解開後各啟動 8 秒。Windows／macOS 尚無原生啟動收據，現行四包未簽章、公證；本次未做雙次建置的逐位元組可重現驗證。無 Git tag 或公開 Release。收據見 `docs/release/01` 與 `dist-all/v.1.0.0-20260923/SHA256SUMS.json` |

## 2. 資料格式

| 格式 | 狀態 | 等級 | 版本 | 文件 |
|---|---|---|---|---|
| `.GRP`／`.IDX`／`.NAM` 容器 | 解出 | `L1` | `[both]` | `docs/formats/01` |
| 州郡名表 | 解出 | `L0` | `[base]` | `docs/formats/02` |
| 劇本三張表 | 部分 | `L0` | `[base]` | `docs/formats/03` |
| 存檔（六個項目全解，讀得進原版的進度）| 解出 | `L0`／`L1` | `[base]` | `docs/formats/05`、`docs/re/08` |
| 配樂（曲子）| 解出 | `L1` | `[base]` | `docs/formats/06` |
| 配樂（音色）| 解出 | `L1` | `[base]` | `docs/formats/06` §5、§7 |
| 點陣圖 `.IMG`／`.FAC` | 解出 | `L1` | `[base]` | `docs/formats/07` |
| `EGAFILL.PAL`／`HERCFILL.PAL` | 解出：1024 B ＝ 16 × 64，一個勢力一塊 8×8 的填色圖樣（四塊純色、十二塊 2×2 網點）| `L1` | `[base]` | `docs/formats/07` |
| `8x8AND*` 遮罩 | 解出：四張 8×8 的 AND 遮罩（1/4、1/2、1/8、1/2）；紮寨那一步蓋的是 `AND0`，78 格裡 73 格逐像素相同 | `L1` | `[base]` | `docs/formats/07` |
| `.MSK` | 解出：`ENDO4.MSK` 是 640×151 的單平面遮罩，配製作群畫面的 `REC10`（`TestDecodeMaskIsOnePlane`、`TestDrawCreditsRespectsTheMask`）| `L1` | `[base]` | `docs/formats/07`、`docs/spec/012` §2 |
| `TITL0`–`3` 怎麼組成標題 | 開場的三英圖：四塊 160×400 並排（x ＝ 160i）**取整整 400 列**，畫面 408 列、底下 8 列是貼圖前清成的黑，與原版逐格相同（先前記著「取第 0–349 列」，那是畫布設成 350 造成的，R48）| `L1` | `[base]` | `docs/spec/005`、`docs/spec/006` |
| `SANTL`／`SANTR`（開場詞的底圖）| 解出：各 320×295，畫在 (0,49)／(320,49)，與原版逐格相同 | `L1` | `[base]` | `docs/spec/005` |
| `EICON.GRP`（戰場地形圖塊）| 解出：36 筆 `.IMG` 首尾相接，每筆 772 B、48×32，**編號就是地形碼** | `L1` | `[base]` | `docs/spec/005` §8 |
| `.OKR`（**共 465 項** ＝ `DATA2` 418 ＋ `DATA3` 47）| 解出：PC 喇叭的語音片段，播放器整支解出，一句話由三段接起來（`TestSpeechClipsAreTheAssetBytes`）；哪一段是哪一句只證實兩則 | `L1` | `[both]` | `docs/re/09`、`docs/spec/008` |
| 戰場地圖（州郡 offset 55–174）| 解出 | `L0` | `[base]` | `docs/spec/003` §3.3、`docs/re/05` §2 |
| `NAME001`–`006.SHA`（加強版）| **不是遊戲檔**：bundle 裡混進來的第三方命名軟體存檔，`ASV.EXE`／`SV.COM` 都沒有引用 | `L0` | `[plus]` | `docs/mechanics/90` §4.1 |
| `PARTNSAV.FIL` | **不是遊戲檔**：`MIRROR 6.M`／`saved partitions` 簽章 ＝ MS-DOS 6.x 的分割表備份，`AA.EXE` 沒有引用 | `L0` | `[base]` | `docs/formats/01` §6 |

### 劇本三張表的欄位覆蓋率

數的是**位元組**，依據是 `docs/spec/003` 的欄位表（`u16` 算兩個）。

| 表 | 一筆幾 byte | 解出 | 比例 |
|---|---|---|---|
| `BASEMAS` 諸侯 | 72 | 15（其餘 57 個**兩版都無讀寫端**，`docs/re/11`）| 21%（無 consumer 的不算未解，覆蓋 100%）|
| `BASESTA` 州郡 | 176 | 148 | 84% |
| `BASEGEN` 人物 | 30 | 22 | 73% |

州郡那一跳是**戰場地圖**：offset 55–174 那 120 個位元組原本標「未解」，
解出來是 12 欄 × 10 列的地形圖（`docs/spec/003` §3.3）。
諸侯的 15 個是 offset 0／2／4／6／8 五個 `u16` 加 14–18 五種寶物；其餘 57 個
在兩版各 54／59 個月的實跑裡零存取、靜態也沒有以表段為 `ES` 的存取（Issue #6）。

州郡的 28 個裡有 8 個是相鄰表（offset 45–52），那是**地圖幾何不是劇本狀態**
——六個劇本 ＋ 六個存檔十二個槽位完全相同。

## 2.5 畫面接原版素材到哪裡

`docs/spec/005`。判準一律是**與原版的畫面逐像素比**，不是「看起來像」。

| 畫面 | 狀態 | 證據 |
|---|---|---|
| 設難度 | 接上 | 原版留在選君主那一頁：選中那一位印玩家序號（灰 7 黑邊兩倍寬，(x+16, y+73)）、提示框換「請設定難度(1-10)」。`TestZZDifficultyScreenMatchesTheOriginal` 框外逐像素相同、提示框兩邊有墨、序號外框重合（`docs/spec/005` §9.4，Issue #67）|
| 音樂欣賞 | 接上 | 原版不換畫面：直牌換「音樂欣賞」、按鈕換五個曲名加一條空白；選第 k 首是 `0x4fb:0x12a(k−1)` 然後回主選單。`TestZZMusicPickMatchesTheOriginal` 124 個字格墨相同、其餘 258,742 點逐格相同，反對照差 38 格，送「3」原版參數 2；DOSBox-X `rec16` 只差小飾框（`docs/spec/005` §6.6，Issue #70）|
| 主戰場的文字視窗（紮寨／命令／休息／退兵／方向／策略）| 接上 | 第三塊面板照原版訊息常式排字（22 格折行、6 列、黃 14 在青 3），字串照資料段；策略先問方向、休息與退兵要 Y/N。`TestZZBattleWindowsMatchTheOriginal` 紮寨與命令兩句逐字相同，五格 132 字格有墨逐格相同；六種計謀的清單那一格這一場走不到（`docs/spec/014` §7.1，Issue #75）|
| 賞賜物品的迴圈 | 接上 | 照 `0x1cfd6` 兩層迴圈問：那一郡 → 那一位 → 卡 → 物品 → 送完回到那一位；同一道命令裡同一位只收一次，空 Enter 回那一郡、再空 Enter 收掉，送出過一件才算下過令。玩家那一條（`0x1d118`）增幅是定值、忠誠看截斷前的能力，和電腦那四支不同。`TestZZGiftLoopMatchesTheOriginal` 逐問提示與三張表逐位元組相同（`docs/mechanics/20` §5.3，R79，Issue #79）|
| 輸入游標 | 接上 | 讀鍵等鍵時回呼槽 0（`0x10d7a`）在提示最後一個字後面合成 `CUR` 六格；主選單 A、主畫面 B、主戰場 C、開新局設定 D。`TestZZSaveScreenMatchesTheOriginal` 六格、`TestZZBattleWindowsMatchTheOriginal` 四格、`TestZZTwoPlayersMatchTheOriginal` 兩格逐像素相同；DOSBox-X `rec17`／`rec7` 是合法畫格（`docs/spec/014` §4.1，R80，Issue #78）|
| 挑一位將軍、數字輸入 | 接上 | `0x18024`：右側面板清藍＋`SIDEA`、表頭與欄名、一頁十二列、下面板「提示(1-筆數):」回顯、空白鍵換頁，十四道命令各用原版的（模式, 鍵）；`0x34ede`：欄寬取上限位數、空 Enter 取消、超出重問。`TestZZPickListMatchesTheOriginal` 名單順序與兩頁畫面相同、`TestZZNumberEntryMatchesTheOriginal` 徵兵提示與回顯相同；DOSBox-X `rec11` 面板相同（`docs/spec/014` §4.2–§4.3，R81，Issue #77）|
| 人物資料卡等鍵時的提示 | 接上 | 查看→檢視將軍卡片之後下面板「請按任一鍵」（`0x17cd8`）、賞賜物品「請按任一鍵\n查看物品表」，游標接在後面。`TestZZCardPromptMatchesTheOriginal` 兩格下面板字格有墨與游標逐像素相同（`docs/spec/005` §9.2，Issue #81）|
| 挑郡清單、調動軍隊 | 接上 | `0x1d4ec`：右側面板 `SIDED`、42 郡三欄，收得下的黃 14 其餘棕 6，提示接「(1-42):」；`0x18286` 多選清單（`SIDEE`、「*」、選滿不切換）；調動軍隊一次搬一份名單、沒有接手閘門（R82），金米提示接在前面幾行後面。`TestZZPrefPickMatchesTheOriginal`、`TestZZMovePickMatchesTheOriginal`（搬完三張表逐位元組相同）（`docs/spec/014` §4.4、`docs/mechanics/40` §1.1，Issue #80）|
| 計略的問法 | 接上 | 五種計略先用挑郡清單問一到三個郡、再挑使者，每一問收哪些郡照原版（`docs/mechanics/30` §2.1）；訊息常式寫滿一行立刻換行。`TestZZPlotPickMatchesTheOriginal` 偽書使疑兩問、遠交近攻三問畫面與字色逐郡相同；驅虎吞狼、策反人民、聯合出兵照碼寫未實拍（Issue #83）|
| 儲存進度 | 接上 | 右側面板清藍、外框 `SIDEC`、六筆名稱字 14 底 1；選槽之後組「n.＋姓名欄＋在＋郡名＋備註」。`TestZZSaveScreenMatchesTheOriginal` 面板名稱以外逐像素相同、120 格墨相同，寫檔前那一筆 21 byte 逐位元組相同；DOSBox-X `rec17` 只差游標（`docs/spec/005` §9.9，Issue #74）|
| 示範模式月底的鏡頭 | 接上 | `0x1e1fc`：從「主人不是被看那一郡的主人」的郡、所有有勢力的人物裡各擲一次換人，偶數月畫郡資料面板、奇數月畫人物卡。`TestZZDemoCameraMatchesTheOriginal` 原版 0 人局 1–4 月：換到的郡／人、排的那一格、之後的亂數狀態逐月相同，畫完那一刻的右側面板與 remake 同一狀態文字格墨相同、其餘逐像素相同（`docs/re/06` §9，R78，Issue #73）|
| 郡的資料面板 | 接上 | `0x33020` 對 37 個有主的郡直接呼叫，remake 同一盤面畫同一郡：文字格逐列比墨、其餘像素逐格相同（`TestZZStatusPanelMatchesTheOriginal`）；右欄的軍師一行（軍師在這一郡才畫）、現役將與兵士讀快照（兵士以百計）、主事者姓名欄從 520 起（`docs/spec/005` §2.1，Issue #76）|
| 自創君主的名字 | 接上 | 人物表的造字碼位 `A141`–`A14C` 解碼時換成出貨字模畫的「新君主」（`state.ShowCustomGlyphs`），寫回檔案碼位不變；「新君主出現」那一格名字的有墨直欄原版 39、remake 45，畫成「，、。」的反對照 10（`TestZZNewLordBornMatchesTheOriginal`，`docs/spec/013` R5，Issue #72）|
| 載入進度 | 接上 | 原版不換畫面：直牌換「載入進度」（字色 15）、六個按鈕是 `SAVENAME.SVP` 六筆原樣，造字碼位畫那一份進度的字模。`TestZZLoadPickMatchesTheOriginal` 124 個字格墨相同、其餘 255,603 點逐格相同，反對照差 36 格；DOSBox-X `rec16` 只差小飾框（`docs/spec/005` §6.5，Issue #69）|
| 選擇年代 | 接上 | 原版不換畫面：直牌換「選擇年代」（字色 15）、六個按鈕換原版字串（字 14、底 3、逐字排）。`TestZZScenarioPickMatchesTheOriginal` 124 個字格墨相同、其餘 257,757 點逐格相同，反對照差 16 格（`docs/spec/005` §6.4，Issue #66）|
| 主選單的六個項目 | 接上行為 | 片頭 → 主選單 → 選年代／幾人玩／選君主／設難度／載入／音樂欣賞／離開；選年代、幾人玩、選君主、設難度、載入、音樂欣賞照原版畫，字型兩項只說明一句（Issue #71）|
| 主選單 | 接上 | 五張圖全部就位：`MENU0A`(40,27)／`MENU0B`(320,27) 各 **100%**、`MENU1`(56,215) 95.3%、`MENU2` 六格（x ∈ {152,376}、y ∈ {215,267,320}）93–97%、`MENU3`(576,320) 91%——不足的都是字寫在上面。字的落點也照原版量（中文字距 24、直牌四個字橫向拉兩倍寬）：**扣掉字與小飾框那格動畫之後 245,170 點逐點相同**。基準畫面另拿 DOSBox-X 交叉驗證過，兩個實作逐點相同（`docs/playtest/03`）|
| 遊戲主畫面 | 接上 | 八張 `MAINMAP*` 的底圖：上方與**下方**花邊、最右直條各 **100%**，左直條 94.7%、地圖區 70.8%（差的是後來蓋上去的）；肖像 `F###.FAC` 在 (536,116) **100%**、肖像框 `FBRD` 四塊各 **100%**。右側兩塊面板的花邊外框是 `SIDE*` 拼件平鋪（`SIDEB`／`SIDED`），與原版 **12,544 點逐點相同**；面板上每一列的位置與字色也是量的（`docs/spec/005` §2.1）|
| 主戰場的地形 | 接上 | 78 格裡 **73 格逐像素全中**，差的五格是五支部隊站的位置 |
| 主戰場的上方花邊 | 接上 | 與主畫面共用 `MAINMAP1`，畫在 (0,0)（`0x2246a`／`0x2247c`）|
| 主戰場的部隊標記 | 接上 | 五個標記全數 **100%**：`WFLAGD00`–`D03` 原色、陳就的中軍是 `WFLAGA00` 取補數；旗在格子左上角加 (8,0)，兵力牌 40×17 在旗下 15 |
| 配樂 | 接上 | 從 `DATA1` 邊播邊合成（`docs/formats/06`）|
| 主戰場的底紋與四個小框 | 接上 | 底紋 `8x8PAT0`（黃／青棋盤）與左欄四個框（第四框 228–323）的邊框，與原版逐像素相同 |
| 主戰場的三個面板 | 接上 | 176×96；寬版面在 x 64／256／448、y 268，窄版面疊在 x 448、y 44／156／268；外框的黑白立體邊照 `0x22200` 逐條抄，上緣兩列 100% |
| 主戰場的肖像 | 接上 | 框 `FBRC0` 在面板左上角加 `DS:0x7b8c` 的位移（寬 (64,268)／(352,268)、窄 (448,44)／(544,156)）各 100%；肖像 64×80 再內縮 8，**攻方那張左右翻**（`F228` 翻過來 100%；窄版面守方 `F002`）|
| 主戰場漏畫的字 | 接上 | 軍力面板第六行 `米%6d`；左欄第四框是國字日數三格 32×16（位數表 `DS:0x78a8`）＋「日」＋ 時辰名（永遠卯）＋「時」＋ 時數 6，兩張基準的 60 個文字格格內有墨、格外沒有（`TestArtBattle*TextCells*`，Issue #57）|
| 主戰場的部隊面板與查看 | 接上 | 對戰子畫面把兩塊軍力面板換成那兩支部隊的面板（`0x320a6`：第 0 槽那一位、框 `FBRA`、君主／軍力／隊伍名與將數／兵），remake 在子畫面的每一句對白那一刻換（`Speech.Units`）；查看把第三塊換成那一位的肖像 (552,276)、框 `FBRB`、名字 x 512、六行資料（`0x284a2`），remake 在查看的覆蓋頁打開時畫第 0 槽那一位。兩支常式直接呼叫對拍，肖像、框、藍底逐像素相同、六行有無逐行相同（`TestZZUnitPanelsMatchTheOriginal`、`TestZZInspectPanelMatchesTheOriginal`，Issue #55）。指令面板的字改從 (448,268) 起、肖像框四片都畫（先前只畫上片）|
| 對戰子畫面 | 接上 | `0x2deb0` 的子地圖（版型逐格、地形碼 15 不畫）、將領標記（名字／攻守／兵／右緣直線）、每一時刻重畫的左欄時刻、輪到的人反白、第三塊面板的選單；玩家那一方在子畫面裡下令（`PlayerSkirmish`）、被叫陣問 Y/N。`TestZZSkirmishScreenMatchesTheOriginal`：標記以外的場地逐像素相同，標記的字格底色與墨、部隊面板、選單、左欄時刻都相同（`docs/spec/005` §8；Issue #56）|
| 主戰場的兩種版面 | 接上 | 州郡 offset 34 ＝ 9 走 12×7 的寬版面、其餘走 8×10 的窄版面（各 21 郡）。窄版面：上下階梯各 4 組（下緣是另一支 `0x21fe0`，y 324）、左緣黑線到 356、右緣白線 x 440–441 到 373、面板疊在右邊。基準 `orig-battle-narrow.png`（打夷郡 26）整片場地 **125,680 格逐格相同**，肖像、統帥名與五行資料 45 格落點相同；寬版面的右緣 x 632–633 一併接上（122,016 格）（`TestArtBattleNarrow*`，Issue #53）|
| 訊息框（肖像＋對白泡泡） | 接上 | 原版的訊息常式 `0x3273e` 直接呼叫四次（上格／下格 × 肖像左／右），肖像 64×80、白泡泡、四邊、尾巴、名字的黑底與 remake 的 `ui.DrawBubble` **逐像素相同**；對白框裡兩邊只有白與擲出來的那一色（`TestZZBubbleMatchesTheOriginal`，Issue #49／#51）。接上的呼叫端：元月與繼承六處（#49）、主畫面命令那一層 37 處——軍師勸諫 19 道命令、人事與計略的回話、宣戰（#51，`docs/spec/005` §9.6）；戰場、對戰子畫面與單挑 47 處——第三塊面板與單挑的兩塊側面板（#52，§9.7，`TestZZBattleSpeechMatchesTheOriginal` 三種框逐像素相同）；剩下的五組——新君主出現（`0x133f2`）、用計三道門（`0x28d48`）、戰役結束回郡（`0x256fa`）、郡地理誌（`0x18663`）、計略四種的成敗回話（#60，§9.5／§9.7／§9.8／§9.6）。不做：防拷密碼那一張的六處 |
| 新君主出現與郡地理誌兩張整頁 | 接上 | 自創君主收工那一格（提示框「新君主出現!!」、(424,66)–(615,161) 那一格泡泡、地圖上那一郡已換色）與查看 5 的郡地理誌（畫在顯示記憶體第二頁：底紋、花邊、地形圖、通道格的鄰郡號、第三塊面板那一句），與 remake 的 `DrawNewLordBorn`／`DrawArtAtlas` 字以外**逐像素相同**（`TestZZNewLordBornMatchesTheOriginal`、`TestZZAtlasMatchesTheOriginal`，Issue #60）|
| 畫肖像的九個呼叫端 | 盤點完 | `0xf7b4` 的九個呼叫端各對到畫面（`docs/spec/005` §9.1；`SAN1_FACES` 的普查跑玩家命令、盤面己、盤面丁、戰役鍵掃，573 次命中七處）：主畫面與主戰場面板接上、訊息框（#49／#51／#52／#60）、人物資料卡／尋訪／自創與選君主（#54／#59）、單挑與戰場查看（#55）全部接上 |
| 人物資料卡（查看→武將） | 接上 | 直接呼叫原版的 `0xf874`（要用真正的段 `0x0f17:0x0704`），盤面從原版記憶體解出、三位各走身分那一行的一個分支：灰底、`SIDEC` 外框、肖像與 `FBRC` 框**逐像素相同**，每一行字兩邊都有墨（`TestZZPersonCardMatchesTheOriginal`）；三種語言每一行都放得進它的槽（`TestCardFitsEveryLanguage`）。字模是 remake 的字庫；譯文的三處退路是 remake 差異（`docs/spec/005` §9.2，Issue #54）。五個呼叫端的時機（#59）：查看→3 是「檢視那位 → 卡 → 任意鍵 → 再問」的迴圈，看別人的郡先問 Y/N；賞賜物品先卡片、任意鍵、物品表、那一樣，送完道謝再一次卡；示範模式月底的鏡頭與調整兵力的除錯陷阱 remake 不做（`TestZZCardTimingsMatchTheOriginal` 逐鍵釘住原版的順序）|
| 尋訪找到人的肖像 | 接上 | 清成藍的面板上直接呼叫 `0xf7b4(488, 88, …)`，整塊面板（含留著的 `SIDEB` 外框）與 remake 的 `Bubble{FaceOnly}` **逐像素相同**（`TestZZSearchFaceMatchesTheOriginal`）；玩家尋訪之後排進 pending 的兩格（`TestPlayerSearchQueuesTheScreens`）。肖像是先畫在第二頁、再用拉幕 `0x32e40(432,80)` 拉進來的，remake 由 `ui.SearchPanel` 合成那一塊走同一段拉幕（`docs/spec/005` §9.3、`docs/spec/010` §1）|
| 場景圖拉幕（`SCG01`–`SCG31`） | 接上 | `0x32dfa` 的 48 個呼叫端逐一對到圖號與落點（`docs/spec/010` §1.1，`SCG11` 沒有呼叫端）；直接呼叫 `0x32dfa` 四個方向、三種落點每一步整張畫面逐像素相同（`TestZZSceneEffectMatchesTheOriginal`）。被擒處置的三張（`0x25cae`／`0x261a1`／`0x26383`）電腦捕獲時接上，玩家捕獲跟 #35 |
| 選君主（一頁六位） | 接上 | `bootToLordPick` 停在君主編號的數字輸入：地圖、`SIDEB`／`SIDEA` 外框、六張肖像、彩色空心框、勢力色塊**逐像素相同**，名字／編號／提示只比有沒有墨（`TestZZLordPickScreenMatchesTheOriginal`）；DOSBox-X 的同一格（`workplace/rec7` 第 10 步）只差游標的 55–59 個像素（`docs/spec/005` §9.4）|
| 新君主分配能力 | 接上 | 翻兩頁選 15、難度 5、停在 `0x13150`：地圖、外框、肖像與 `FBRD` 框**逐像素相同**，六行字與名字只比有沒有墨（`TestZZCustomLordScreenMatchesTheOriginal`）。原版開局把範本改寫成 18／80／50／50／50、100 點——規則層對齊另開 Issue（`docs/spec/005` §9.5、`docs/spec/013` §4）|
| 主戰場面板裡的字 | 接上 | 郡名 32×32 在 (8,52)／(8,84)、統帥名 32×32 直排（攻 144、守 320）、五行資料在攻 176／守 256 的 y 268＋16k，字色攻 12 守 10，全部從 `0x22c94`／`0x22fd4`／`0x2181d` 與 `DS:0x7b8c` 的表讀出；45 個文字格對基準畫面**格內有墨、格外那一圈沒有不屬於任何一格的墨**（`TestArtBattleTextCellsMatchTheOriginal`，Issue #48）。字模是 remake 的字庫 |
| 場地四周的階梯邊框 | 接上 | `0x21ee2`／`0x220f0` 的十六條線逐條抄；整片場地（含邊框與空隙）**121560 格逐格相同**，只挖掉五支部隊站的格子 |
| 城門圖示（每組旗的第六張）| 原版不畫 | `WFLAG?5.IMG` 是 16×15 的城門圖，載進槽 25／31／37／43 之後沒有呼叫端畫它——槽號的式子只有 `0x21c06` 一處而隊伍永遠 0–4；實跑一場含對戰子畫面，四個槽畫 0 次（`TestZZCityGateIconNeverDrawn`，`docs/re/05` §2.5）。remake 不畫 |
| 片頭 | 接上 | `internal/opening` 照原版 `DATA0` overlay 的兩頁畫面逐拍重做：商標 → 標題字 → 製作人員 → 海景船隊 22 格 → 逐列寫詞 → 調色盤淡出 → 頭像橫幅 920 步 → 三英圖捲入 80 步 → 等鍵 → 程式載入中。`TestZZOpeningMatchesTheOriginal` **1,308 拍每一拍顯示頁 640×408 逐格相同、調色盤相同**（詞那一層用 `TZUE`）；DOSBox-X 三張整張逐點相同（`TestOpeningMatchesDosboxX`）；remake 字庫的詞 62 個字框與 `TZUE` 比字格的墨（`docs/spec/005`「片頭」，Issue #61）|
| 主戰場誘敵的特效 | 接上 | 誘敵那一句之後在施法者那一格整塊貼 `EICON.GRP` 第 32、33、再 34／35 交替共二十二步，每步一聲 `speak`：盤面辛誘敵 6 次，每一步那一格 48×32 與圖塊逐格相同、速度相同（`TestZZUnitAIDayParity`）；加強版 `0x28a4f` 讀碼同形（`docs/spec/005` §8「誘敵的特效」，Issue #63）|
| 大地圖上的戰役 | 接上 | 電腦對電腦的戰役播 `0x1ecfc` 的旗隊動畫（`CVSC00`–`23`、兩頁、遮罩＋OR、每天 1/32）：`TestZZMarchAnimationMatchesTheOriginal` 兩場每一格整張 640×408 逐格相同、收尾還原相同、51 聲 `speak` 速度序列相同；往左與往上兩種方位只讀碼。`[base]`，加強版不播（`docs/spec/005`「大地圖上的戰役」，Issue #62）|
| 開場詞 | 接上 | `SANTL`(0,49)／`SANTR`(320,49) 兩張各 **94400 格全中**；詞的字框量自 `TZUE`（詩十欄 x 90＋42k、「詞曰」524、列距 24），淺青配黑影 |
| 開場的三英圖 | 接上 | `TITL0`–`3` 四塊並排成 640×400，畫面 408 列（底下 8 列是黑），**261,120 格全中**（`assets.TitleArt`）|
| 商標畫面 | 接上 | `CMARKL`／`CMARKR` 是 `AA.EXE` 開機的第一幕（y 64）：dosgolem（`TestZZTrademarkMatchesTheOriginal`）與 DOSBox-X（`TestTrademarkMatchesDosboxX`）兩個實作都與 `assets.TrademarkScreen` 整張逐點相同；remake 片頭第一幕放它（`docs/spec/005`「片頭」，Issue #34）|
| 州郡的填色 | 接上 | `EGAFILL.PAL` ＝ 16 個勢力各一塊 8×8 圖樣，執行期勢力槽對檔案區塊是 0／2 互換其餘同號；拿原版剛載完第一個進度的畫面逐格比，35 個有主的郡 **全中**、42 郡依序合成 0 像素差（`TestPrefectureFillsMatchTheOriginal`，Issue #12）|

**字型不接原版**：remake 自建字庫，不內嵌任何原版字模（`CLAUDE.md` §3.3）。

## 3. 對原版對拍過的（最高位階的證據）

`internal/parity`，走 `-tags oracle`。這些是**原版自己的行為**，
不是 remake 自洽。

| 對拍項 | 判準 | 結果 |
|---|---|---|
| 開機裝置選單 | 原版記憶體裡有沒有這些題目與選項 | 相符 |
| **玩家自己下的每一道命令** | 同一道送進原版的選單、也送進 remake 的 API，亂數用原版的種子對齊，比三張表 | **兩個盤面各十五道**。建安二年／南海（守將一位）十五道全部相同；劇本 1 曹操／郡 11（守將七位、鄰郡有敵）十五道全部相同。**出兵另立一支對拍**（`TestZZPlayerSortieDriven`，郡 11 打郡 4）：走完軍事 → 發動戰役 → 選郡 → 整編 → 攜帶錢糧 → 確認，原版動的 3 個位元組逐格相同。量到五件事：買米問的是金、玩家照數字付而電腦照效果反算、賞賜與賑民的係數兩條路不同、**出征的將領所在郡歸 0**（`0x20ce0`）、**戰場地圖沒被寫回存檔**（`Tables()` 漏了 offset 55–174，建築關寨改的那一格存不下來）——後兩件是這一輪抓出來的 remake 缺口（`docs/playtest/04`）|
| **dosgolem 與 DOSBox-X 走同一串按鍵** | 38 步（開局 → 選主君 → 出兵宣戰 → 軍團整編），逐步比 640×408 | 32 張列入判準：**20 張在 99.6% 以上（4 張逐點相同）**；2 張是防拷題目（95.4%，地支是抽的）；10 張落在一個 dosgolem 抽到、DOSBox 沒抽到的宣戰事件裡。另一輪獨立紀錄（33 步）對不上的是**不同的區間**，兩輪都自己收斂回來——差異來自兩邊的時鐘（`docs/playtest/03`）|
| 容器項目邊界 | 攔原版對 `DATA1.GRP` 的 seek，比它算出來的起點 | 逐項相符 |
| 三張表在記憶體的位置 | 拿劇本檔的位元組去搜 | 找到，且三張連續 |
| 三張表的內容 | 與劇本檔逐位元組比 | 19,220 個裡只有 43 個不同（州郡表的物價，原版**開月時**自己算的，`docs/mechanics/60-economy` §1.2）|
| 諸侯表的欄位 | 從碼與盤面兩邊定位；整張表掛讀寫監看走 54／59 個月（`TestZZMasterRecordAccessCensus`／`…Plus`）| 72 個位元組 15 個有語意；**其餘 57 個兩版都零存取**，靜態補掃也沒有（`docs/re/11`）。offset 70 有 2 次讀取，是州郡 −1 的越界不是欄位 |
| 玉璽只有一個持有者 | 六個劇本 × 十六個槽 | 每個劇本剛好一人一個 |
| 盤面寫得進去 | 寫進去再讀回來 | 19,220 個位元組完全相同 |
| 點陣圖的平面→顏色 | 與原版畫面逐像素比 | `F000.FAC`／`F005.FAC` **100% 相符** |
| 配樂的音高 | 原版播「思古」送出的 F-number 與 block | 沒滑音的五個聲部**逐次相同**，有滑音的四個差 ≤ 1 |
| 音色參數 | 原版填進 OPL2 的暫存器值 | 58 段載入裡 25 段的 23 個參數**逐項相同**（六件不同音色）|

| 原版進得了遊戲主畫面 | 從存檔載入 | 通了（`docs/re/02` §3.3）|
| 主畫面收得下命令 | 扣掉游標雜訊之後的畫面差 | `9` ＋ `Enter` 開子選單 1,912 像素，對照組 **0** |
| 防拷密碼過得去 | 輸入之後月份走不走得下去 | 過得去，而且**任何四位數都一樣**（`docs/re/02` §3.3）|
| 月份推得下去 | 「內政 → 休息 → `Y`」之後的日期 | 建安二年八月 → 九月 |

| **存檔載入** | 原版載入它自己的進度 1，remake 走 `save.ReadOriginal` 讀同一格，三張表逐位元組比 | **除兩個快照欄位之外完全相同**（19,220 個位元組；兵士與在職將在存檔裡是寫檔當下的快照，原版載入時不重算，六個郡與重算結果不同）|
| **戰役的部隊** | 把原版驅動進主戰場（盤面直接寫記憶體、玩家親征），讀它自己的部隊記錄，拿 remake 的公式重算 | 兵士數、綜合能力、**一天的移動力上限**三欄 15 個位元組**逐支相同**。移動力因此改掉了：原本抄的是對戰子畫面那一支（`TestBattleUnitsMatchTheOriginal`，2026-09-09）|
| **戰役的計謀** | 玩家那支走到守軍旁邊，`6` → 方向 → 陷阱 | 隨軍的金 5000 → 4900（費用表 `DS:0x7f62` 的 100）、守軍被困 6 天（智 99 的範圍 1..11）。順帶解出**計謀只能對相鄰的六格用**——原版先問方向，`TuneStratagemRange` 因此退役 |
| **戰役的逐日** | 全部休息連走七天逐日比移動力；再走兩步逐步比移動花費；走到守軍旁邊之後電腦打過來，逐次比交戰的傷亡 | **56 個欄位全中**（逐日 50 ＋ 移動花費 2 ＋ 兩次交戰各兩邊的兵 4）。據此修掉三個實作錯誤：開新的一天是回填不是重設；休息 +2 之後**夾在 15**；交戰結算整條換成原版的（`0x2a224`）|
| **存檔寫出** | remake 走完一個月存進第 1 格，把六個項目換進**原版目錄複製品**的 `DATA2.GRP`，讓原版讀那一格 | **19,220 個位元組完全相同**，而且載入流程的六個檢查點都是 0（換進去的內容與原本那一格差 2／168／255 個位元組，所以「相同」不是沒換到。`TestRemakeSaveLoadsInOriginal`，2026-09-09）|
| **對戰子畫面** | 盤面丁把玩家的兵壓到二千四、戰力 1，郡裡四位守將戰力也壓成 1、各不超過五千兵，第 24 天左軍擲到 `RND(16)==0` 進對戰；`SAN1_SKIRMISH=1` 把子畫面裡每一位每一步（位置、餘步、體能、走哪條路、擲了什麼）倒出來，remake 的 `Skirmish` 從同一個種子走同一場 | **整場相同**：攻方一位從 `(0,3)` 每時刻走兩格到 `(10,3)`，第 11 時起每時刻攻擊；玩家那一位每時刻休息；`SAN1_NORESYNC=1` 下 120 條鏈的種子、狀態、決策逐條相同（`TestZZUnitAIDayParity` 盤面丁、`TestSkirmishMatchesTheTracedFight`，2026-09-16）|
| **戰役的勝負** | 玩家親征打到分出勝負（守軍壓薄、貼身每天對戰），在**判定發生的那一刻**讀原版的軍團統帥與各軍第一支部隊的排頭，套 remake 的 `checkOver` 規則算勝方 | **一致**：守方統帥 96 在排頭、攻方統帥 331 不在，兩邊都算出 `0`（主守軍勝）。期間統帥條件 `0x24f8c` 跑 105 次、三十天判定 `0x250d4` 跑 5 次——電腦對電腦那條路兩支都是 0 次（`TestBattleFinishesWithPlayer`，2026-09-09）。三十天期滿的**時點**另量到：第 30 天的部隊動完才判（`0x23c3b` 先判再加天數），盤面丁跑滿三十天第 30 天三支守軍照樣快戰，remake 原本在 29→30 就判、第 30 天全休息，改在 `EndDay` 加天數之前判（Issue #36，2026-09-16）|
| **一個月的狀態轉移** | 兩邊從同一份盤面各走同一個視窗，逐欄位比 | **差 0 個位元組**（2026-09-11，新起點）：43 個郡、350 位人物、16 個勢力逐格相同，逐表與逐郡的抽亂數次數全部 ±0。2026-09-10 換 base 後起點漂移（R49）從 339 重新校準到 0，走勢與解出的規則在 §8 該列與 `docs/playtest/02`（`TestZZMonthParity`）|
| **兩版的月度對拍推到四季（十二個視窗）** | `SAN1_MONTH=n` 把視窗推到 n 月底（跨進 n+1 月），`SAN1_PLAGUE=1` 把一個郡擺成必淹必瘟；原版與加強版各六個視窗（預設、3、3＋瘟疫、6、9、12），三張表整月逐位元組比 | **十二個視窗各差 0 個位元組**（2026-09-16，Issue #44）：四季常式兩版逐段同形，加強版獨有的兩條（元月忠誠漂移的難度門、挖角的 `RND(1010)` 難度門）與兩版共用碼修的一批（元月順序與對白／特效的擲、地震震兵、蝗害不看所屬且土地價值不夾、挖角挑忠誠最低者＋等級加成＋三條寫回路、武器預算扣折扣價、移防看目標郡餘裕、出兵看存的兵士、買米的 8087 精度）在 `docs/mechanics/50` §1.1、`70` §2.13.8、`90` §6.6b；每支 ≈ 35 秒 |
| **加強版一個月的狀態轉移（含兩場電腦戰役）** | 從月底結算那一刻的三張表與亂數種子接手，remake 照原版的順序表跑 42 個郡到再輪到玩家，逐欄位比 | **差 0 個位元組**（2026-09-16，Issue #29）：四十二格的抽亂數次數逐格相同，兩場電腦對電腦的戰役（整編、每日結算、安置、補位、分兵）與戰後的郡回合都在視窗裡。走勢 324 → 271 → 194 → 112 → 91 → 87 → 51 → 48 → 42 → 31 → 28 → 23 → 17 → 4 → 1 → **0**（`docs/playtest/02` §7、`TestZZMonthParityPlus`）|
| **統一年份的分布** | 原版示範模式自己打完一局（種子寫進 `DS:0xa3ae`），remake 用 `ai.ModeBase` ＋ 原版 LCG；各自記統一年月、統一者、逐年存活勢力數，**只報分布** | 原版跑完的兩局 284／285 年由曹爽統一（另兩局 40 分鐘逾時：65 年剩 9、90 年剩 3）；remake 五局 266／267 年因君主老死「統一」（三局統一者沒有君主）、三局 339 年仍剩 2–3 個。形狀不同，來源是兩條 remake 缺口（`docs/playtest/05`）|
| 年度人口成長 | 同上，只看人口欄 | **四十三筆逐格相同**（人口存的是「百」，成長的零頭寫回去就沒了）|

**走到 0 的逐步紀錄在 `docs/playtest/02` 開頭**（起點 440，九個階段各解出
哪一條、位址是什麼）。那一段也記了三個反覆出現的形狀：玩家一條電腦一條、
原版的捨入誤差是行為的一部分、交換排序不穩定所以名單順序有意義。

**回歸用十二支對拍測試守著**（各 0 項對不上）：內政 58、徵兵 597、尋訪 33、
賑民 99、買米 136、賞賜物品 186、兵書挑人 31、行動者 40、指定軍師 69、
換太守 28、名單形狀 25、編隊 61。那十二支是**分派表**那一層的；
`internal/parity` 現在總共 81 支測試，還包含存讀檔兩個方向、戰役的逐日
與計謀、事件、蝗害、君主繼承、開機畫面與容器格式。

⚠ **整包 `-tags oracle` 一次跑不完**：每一支都要開機約 60 秒，總時長超過
`go test` 的逾時，而且一次跑太多會被系統因記憶體不足砍掉。**三支一批**、
`-v` 才看得到個別結果。

**電腦對電腦的戰役**照 `0x20471` 的分岔實作了（`battle.AutoResolveAI`，
含傷亡寫回與四條單測），實跑背書在 `TestZZBattleOver…`（Issue #7）：
固定種子、逐將領的戰後兵力與勝方逐筆相同。

**戰役的逐日對拍**：日循環只有玩家在場才跑（電腦對電腦不進戰術層），
所以要玩家親征——盤面直接寫記憶體、按鍵序列驅動進主戰場
（`docs/re/05` §7）。第一段比**開打前**：原版整編出來的每一支部隊，
remake 的公式算不算得出同一個兵士數、綜合能力與移動力上限
（`TestBattleUnitsMatchTheOriginal`）。第二段比**每一天每一支電腦部隊
的決策**（`TestZZUnitAIDayParity`，Issue #22）：五張盤面 192 次決策的
選項、目標、落點逐次相同。第三段比**決策之後那一段的骰序**（Issue #24，
`docs/re/05` §12.2–12.4）：交戰結算、弓箭、計謀、退兵、被擒處置、投敵、
天候各擲幾次、依什麼順序，remake 的 `internal/battle` 改成同序同數，
`SAN1_NORESYNC=1` 只拍第一天的盤面、骰從原版的種子接（玩家按鍵讓原版
重新播種的那幾點也照原版的值接），甲乙丙戊四張盤面一整場（38／52／29／
59 條鏈）的種子、部隊狀態與決策逐條相同。第四段是**對戰子畫面**
（Issue #30，`docs/re/05` §10）：選項 7 進的那一層（版型、起點、每一
時刻的判斷式與六處骰、尋路、攻擊、俘虜、結束）照 `AA.EXE` 實作
（`internal/battle/skirmish.go`），盤面丁 120 條鏈含第 24 天一整場對戰
逐條相同；加強版的這一層也對回 `ASV.EXE` 了（`docs/re/05` §10.9，Issue #37）。
**加強版是同一支本體換一組路標**
（`TestZZUnitAIDayParityPlus`，Issue #28，`docs/re/05` §12.5）：開新局選
曹操、只留君主親征，七張盤面（甲、乙難度 15、丙、丁、戊、己、庚）九支都
定案過，每一次決策與 `SAN1_NORESYNC=1` 整場逐條相同——九支是同一條鏈，門與門檻改了幾處
（移動多兩道門而且目標圍著自己的帥隊、退兵少一道門、死戰 0.4、對戰與
快戰看目標兵力比、模式用 `難度 mod 11`），後果常式只差交戰的兩張地形表、
弓箭尺度 5e-5、綜合能力的加法順序（`docs/mechanics/90` §6.7）。

**存檔的寫出方向怎麼驗的**：`internal/save` 只讀不寫原版容器（那是玩家
自己的 `DATA2.GRP`，寫壞沒有第二份），所以對拍是把整份原版目錄複製到暫存
目錄，在**複製品**上就地換掉那一格的六個項目——六個項目的長度由原版的版面
決定，remake 寫出來的長度不一樣就代表版面寫錯了，所以長度不符直接判失敗而
不是重建容器。原版目錄本身唯讀掛載，一個位元組都沒動到。

**六種計謀**：門檻、費用與殺傷都是 `L0`，單測逐條釘住；實跑裡電腦對
玩家六招都成功過——陷阱與燒糧在盤面戊，火攻、水淹、誘敵、圍攻在盤面辛
（守將謀略 99、天候直寫、旁邊一格淺水、守方金九千；22 次全成），
`SAN1_NORESYNC=1` 下每一條鏈進來時被打的那幾支逐將領的兵、將領數與落點
都與原版相同（Issue #40）。火攻／水淹把將領打光之後的除名與燒死那兩條
路實跑還沒走到，仍是 `L0`。

**交戰的傷亡收斂了**（`docs/re/05` §3.6）：整條算式、三張表與逐將領的
套用迴圈都讀出來了，殘差是兩件事——`0x2a5c9` 那道 `dec`，以及
**x87 的 80 位元尾數**。`1500 × (1 − 585/1500)` 數學上是 915，
用 `float64` 會捨進成剛好 915，用 64 位元尾數是 914.99999999999998，
`ftol` 截尾差 1。remake 這一層因此改用 `math/big` 指定 64 位元尾數
（`battle.MeleeDamage`／`MeleeRatio`／`MeleeSurvivors`）；
`battle.exchange` 就是這一支，讀的綜合能力是每天重算的那一格
（`docs/re/05` §12.3）。

**兵士與在職將是快照不是推導值**（`L1`，`TestOriginalSaveLoadsIdentically`）：
原版存檔裡的 offset 16／22 是寫檔當下的值，載入時原封不動；remake 一律從
人物表推導。走完一個月兩邊會一致（月度對拍 0 個位元組），差別只在剛載入
那一刻。要完全對齊得把它們改成「由改動它們的常式寫」，那是一次不小的
重構——目前的取捨記在這裡。

## 4. 規則層

| 機制 | 文件 | 最高等級 | 實作 | 測試 |
|---|---|---|---|---|
| 回合流程與十類指令 | `mechanics/10` | `L0`（**內政兩項的量**玩家與電腦各一條、**建築關寨**三道門、**商業買賣**的匯率）／`L2`（其餘）| `internal/game` | 有 |
| 人物與人事 | `mechanics/20` | `L1`（**老死看壽命**，21 次逐次相同；**徵兵的稀釋**，597 次逐次相同）／`L0`（欄位、**訓練公式**、肖像編號）／`L1`（徵兵 1:1 扣人口、年齡在元月）／`L2`（其餘）| `internal/game` | 有 |
| 謀略（戰略層五種）| `mechanics/30` | `L1`（**偽書使疑的公式**，15 次逐次相同）／`L0`（前置條件、候選郡、成敗判定、軍師預測、**不花錢**、策反人民的五刀、三支發動戰役的四個郡）| `internal/game/plot.go`、`plotcampaign.go` | 有 |
| 軍事與主戰場 | `mechanics/40` | `L0`（**戰場地圖**、地形碼、移動花費、地形攻守值、兵種適性、移動力、將領戰力值、交戰傷亡、發動戰役的流程、軍師勸諫、**六種計謀的費用／門檻／成功判定**、火攻水淹的地形殺傷、陷阱、燒糧、**電腦對電腦不進戰術層**：分岔條件、糧草消耗、戰力互削、勝負與傷亡寫回、**單挑的接受判定／拒絕損兵／回合與勝負**）／`L1`（電腦對電腦那條路真的沒跑日循環，5 場全數；**六種計謀成功的效果**：陷阱、燒糧在盤面戊，火攻、水淹、誘敵、圍攻在盤面辛，被打的部隊逐將領的兵逐鏈相同；**對戰子畫面兩版**：原版盤面丁整場、盤面己兩場單挑，加強版七張盤面含一場單挑，骰逐格相同）／`L2`（部隊層的合成、弓箭與圍攻的幅度）| `internal/battle` | 有 |
| 玩家在場的戰後結算 | `spec/020`、`mechanics/40` | `L0`／`[both]`（敗軍強制退兵、勝方安置、錢糧與打殘公式、退場君主分贓）；`L1`／`[base]`（原版正常玩家戰役的三支收尾呼叫、戰場郡欄位與四類寶物正向收據）；`L1`／`[both]`（正常玩家戰役結算入口三表相同，戰後整張人物／州郡表各 0 差）。諸侯表與戰術全程仍是限制，規格維持 `READY` | `internal/game/tactical.go`、`TestBattleFinishesWithPlayer`、`TestZZPlayerSettlementTables`／`Plus` | 部分 |
| 四季事件 | `mechanics/50` | `L1`／`[both]`（**四季常式一年各跑一次**、元月的順序與每一則對白／特效的擲、老死與繼承、出頭、**地震、水災與瘟疫連郡裡的人物一起動**、秋收、人望調整、進貢、人口成長：`SAN1_MONTH=n` 把月度對拍的視窗推到每一季，兩版十二個視窗 0 位元組；加強版元月漂移多一道難度門）／`L0`（四種天災的機率與幅度、蝗害的兩條損失與不夾的土地價值、洪水率的年度變動、`DS:0x679c` 的 43 格基礎表、玉璽）| `internal/game/events.go` | 有 |
| 經濟 | `mechanics/60` | `L1`（**電腦買米的六個匯率**，136 次逐次相同；物價每月重抽 30–68）／`L0`（買賣的上下限、關寨造價、買米的浮點夾限鏈）／`L2`（其餘）| `internal/game` | 有 |
| **電腦 AI** | `mechanics/70` | **十八張表全部有實跑背書**（`docs/playtest/02`）；**骨架 `L0`**（六個等級 × **十八張**分派表，無條件依序跑完）／**十八張表都讀過了**（判斷式從碼讀出，不是行為推測）；**內政與買米兩張表六個等級全部對拍過**（`docs/playtest/02`，58 ＋ 136 次逐次相同）；**計略那張表也對拍過**（15 次逐次相同，電腦五種計謀裡只用偽書使疑）；**徵兵那張表也對拍過**（597 次，人口／訓練度／武裝度三欄逐次相同）；**賞賜金帛那張表也對拍過**（605 次、六個等級全走到）；**開倉賑民那張表也對拍過**（99 次、六個等級，含 16 位元溢位與第二次扣錢）；**購置武器那張表也對拍過**（505 次逐次相同）；**訓練兵士那張表也對拍過**（260 次、六個等級）；**調整兵力那張表也對拍過**（40 郡次 279 人）；**登用人才那張表也對拍過**（128 次、六個等級，含兩道牽絆閘門）；**尋訪人才那張表也對拍過**（33 次、六個等級，三個常數都隨等級變）；**選出行動者也對拍過**（40 次；排序鍵是 `謀略 ＋ 戰力 ＋ 加權表[身分]`）；**賞賜物品也對拍過**（186 次、四種寶物；說明書的數字是下界，原版再加 `RND(2)`；**挑誰**另外對拍 23 次，排序鍵是「該能力 ＋ 加權表[身分]」）；**指定太守與指定軍師也對拍過**（28 ＋ 69 次；軍師挑的是名單裡**最後一個**超過門檻的人，不是最高的）；**挖角的成敗判定也對拍過**（`0x1dc0a`，25 次判定、三個條件式加項各走到 25／2／23 次；成功那一側只有一個樣本）；**出兵／移防也對拍過**（61 次編隊、58 次帶走的錢糧、20 次進攻門檻；留守目標的三步式子 61 次全中）；`0x5514` 六個等級全是空操作 | `ai.base` 十七種都發（`0x5514` 是空操作，正確的做法就是不發），`Coverage()` 回 18/18 | 有 |
| 勝負判定 | `mechanics/80` | `L1`（**繼承的人望折損**，3 次逐次相同）／`L0`（**統一條件**、**玩家絕嗣**、繼承的候選、排序、身分 12、玉璽現世、絕嗣時寶庫玉璽釋出而已現世旗標不倒退）| `internal/game/events.go` | 有 |
| **兩版差異** | `mechanics/90` | `L0`（`DATA2` 已排除）| — | `cmd/san1diff` |

⚠ **`L2` 的意思是「說明書寫了、remake 照做了、測試通過了」**——
那證明的是 remake 自洽，不是原版如此。要升到 `L1` 得對拍。

remake 自己選的係數（不是原版數字）集中在兩個檔案，
對照表在 `docs/design/02-remake-owned-values.md`：

```
grep -rn Tune internal/game internal/battle
```

## 5. 程式碼

數法：`find . -name '*.go' -not -path './workplace/*'`，
測試是 `*_test.go`、其餘算程式；測試函式數 `grep -h '^func Test'`。

| 項目 | 數字 |
|---|---|
| 套件（有 `.go` 的目錄）| 25 |
| 程式行數（不含測試）| 24,719 |
| 測試行數 | 27,490（其中 `internal/parity` 的對拍 14,037）|
| 測試函式 | 457（其中 `internal/parity` 的對拍 81）|
| 介面字串鍵 | 372 × 3 語系 |
| 人名／地名的字對照 | 426 個字（拼音）＋ 35 個新字體 |
| 文件 | 49 份 markdown |
| 發行包 | 4 個平台（win/linux/darwin ×2）|

## 6. 多語系

| 項目 | 狀態 |
|---|---|
| 選單與欄位標籤 | 完成（繁中／英／日）|
| 提示與錯誤訊息 | 完成 |
| 事件紀錄與戰報 | 完成 |
| 年號 | 完成（英文用羅馬拼音）|
| 人名（346）| 完成：英文漢語拼音、日文新字體 |
| 郡名（42）| 完成：同上 |
| 槽位溢出處理 | 地圖格寬依最長郡名自動調整；其餘未逐項驗 |

## 7. 怎麼更新這一份

**結構化的欄位改 `worklist.json`，然後重跑 render**：

```sh
tools/worklist.py verify --status open blocked   # 先看未完成項還成不成立
tools/worklist.py render                         # 產生 §8，貼回這一份
```

§1–§6 是敘述層，跑不出來的東西寫在那裡（方法、坑、取捨），
改動影響到哪一段就**同一個 commit 一起改**。數字用指令重數，不要憑印象：

```sh
grep -rc 'func Test' --include=*_test.go internal/ cmd/ | awk -F: '{s+=$2} END{print s}'
find internal cmd -name '*.go' ! -name '*_test.go' | xargs wc -l | tail -1
python3 -c "import json;print(len(json.load(open('internal/i18n/lang/en.json'))))"
```

## 8. 清單

<!-- worklist:begin —— 這一段由 tools/worklist.py render 產生，不要手改。
     要改內容改 worklist.json，然後重跑 render。 -->

### 對拍

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 主戰場基準畫面的取法文件記錯了 | 未完成 | L1 | base | 指令重數 | 成因查出來了：`TestZZBattleKeySweep` 帶 `SAN1_BATTLEKEY` 時 `cands` 只剩一個候選，所以迴圈只跑一輪、只產 `sweep-01.png`——而 `docs/spec/005` 記的取法說它產一整批，`battlefield_test.go` 的註解也說 `orig-battle.png` 由它產。實際上 `orig-battle.png` 是人工從其中一張改名來的。**`orig-battle.png` 已經換成 408 的那張**（場地區的比對在 y<350，換之後三支測試照樣全綠）；還沒解決的是 `sweep-04.png`（紮寨那一步，`TestCampMaskMatchesTheOriginal` 用）——它要另一組按鍵，那組還沒重跑。（`docs/spec/005`、`docs/spec/006`） |
| 逐步對拍的參照畫面是舊尺寸，要重錄 | 未完成 | L1 | base | 指令重數 | 畫面高度 2026-09-10 從 350 改成 408（R48），`workplace/rec7/frames` 那一批是 640×350 錄的。測試現在會明講並 skip，不會安靜地給一個偏低的百分比。重錄跑 `tools/dosboxx-record.sh`。 對拍那一支（`TestZZDosgolemMatchesDosbox`）因此會 skip；**skip 不是綠**，所以這一條留在未完成清單裡直到重錄完成。（`docs/playtest/03`、`docs/spec/006`） |
| 玩家自己下的每一道命令 | 未完成 | L1 | base | `TestPlayerCommandsMatchTheOriginal`、`TestPlayerCommandsAsCaoCao` | 2026-09-11：十四個指令裡**十三個通過**，只剩「休息」。盤面層面的差異已經解決——先前「原版沒動過卻不同」的郡 3／29／36 是兵士欄被當成導出值造成的（見 `state.troops`），改成存值之後「整份盤面兩邊差 0」。  剩下的「休息」是**取樣窗口**問題，不是規則：休息自己不動盤面，而原版一下完就轉移控制權，電腦那個郡的十八張表在同一段裡跑完。寫入監看指名了寫入者——`0x0bdf2`（內政）、`0x0c193`（購置武器）、`0x0c4ab`／`0x0c4b3`／`0x0c4f9`（調整兵力），全是分派表的常式。  ⚠ 試過把停點收緊到下一個郡的回合入口（`0x1746e`），**不能這樣做**：那一格會跑兩趟量測，而旗標是子測試層級、hook 又跨子測試累積，第二趟會立刻停下——量出來的「差 0」是空的。要改得先處理旗標的生命週期。  另一個選項是承認跨郡的一致性是 `TestZZMonthParity` 的工作（它現在差 0），讓這一支只比玩家自己的郡。（`docs/playtest/04`） |
| 兵士與在職將是快照不是推導值（已知差異） | 未完成 | L1 | base | present：internal/parity/loadsave_oracle_test.go 的「在存檔裡是快照」 | 原版存檔的 offset 16／22 是寫檔當下的值，載入時原封不動；remake 一律從人物表推導。走完一個月兩邊會一致（月度對拍 0 個位元組），差別只在**剛載入那一刻**。要完全對齊得把它們改成「由改動它們的常式寫」，那是一次不小的重構。 訊號在對拍測試裡（`include_tests`）：同上，缺的是對齊不是實作。（`docs/formats/05`） |
| 統一年份的分布還沒對拍 | 未完成 | L2 | base | **要人判** | 全電腦對戰約五十年分出勝負，但那是 remake 自己跑的；原版跑同樣的劇本會在哪一年統一沒有比過。（`docs/mechanics/80`） |
| 電腦對電腦的戰役在對拍裡走到了 | 完成 | L1 | both | `TestZZMonthParityPlus`、`TestAIvsAIBattleMatchesOriginal` | 2026-09-16（Issue #29）：加強版的月度對拍整月差 0 位元組，視窗裡有兩場電腦對電腦的戰役（郡 15 → 13、郡 37 → 30）——入口的整編、每日結算、吃糧、傷亡、戰損、安置、主事者補位、戰後的郡回合全部逐格相同；原版那條由 `TestAIvsAIBattleMatchesOriginal` 固定骰序對過。解出的規則在 `docs/re/05` §5.1／§7.1，推翻紀錄 `CONTEXT.md` R63–R66。（`docs/re/05`、`docs/mechanics/40`、`docs/spec/017`、`docs/playtest/02`） |
| 防拷密碼盤問畫面（月內迴圈那支動畫） | 完成 | L1 | base | `TestZZMonthAnimationFrames` | `lcall 03EB:0000` 之前被記成「畫面或音樂，remake 用不到」。逐格存圖之後看出來是**防拷密碼的盤問**：兩圈各 160 步，每步一次 `speak(0, 10)`，結束提示 `請輸入密碼(-%s-)`。remake 不實作防拷，但 `RND(12)` 要照抽。（`docs/re/10`） |
| 分派表：選出行動者與名單順序 | 完成 | L1 | base | `TestActorPickMatchesTheOriginal`、`TestChiefRosterOrder`、`TestGovernorSortListShape` | 40 次；排序鍵是 `謀略 ＋ 戰力 ＋ 加權表[身分]`。交換排序不穩定，所以名單順序有意義。（`docs/re/03`） |
| 分派表：內政 | 完成 | L1 | base | `TestAffairsMatchTheOriginal` | 58 次逐次相同、六個 AI 等級全部走到。玩家與電腦走不同常式。（`docs/mechanics/70-ai`） |
| 分派表：購置武器與訓練兵士 | 完成 | L1 | base | `TestArmsPurchaseMatchesTheOriginal`、`TestTrainingMatchesTheOriginal` | 購置武器 505 次逐次相同；訓練兵士 260 次、六個等級。武裝度是百分比。（`docs/mechanics/70-ai`） |
| 分派表：徵兵 | 完成 | L1 | base | `TestConscriptionMatchesTheOriginal` | 597 次，人口／訓練度／武裝度三欄逐次相同。稀釋是**兩層截斷**不是加權平均（R24）。（`docs/mechanics/20`） |
| 分派表：登用、尋訪、挖角、指定太守與軍師 | 完成 | L1 | base | `TestRecruitMatchesTheOriginal`、`TestSearchMatchesTheOriginal`、`TestHeadhuntMatchesTheOriginal`、`TestAppointGovernorMatchesTheOriginal`、`TestAppointChiefMatchesTheOriginal` | 登用 128 次（含兩道牽絆閘門）、尋訪 33 次（三個常數都隨等級變，R26）、挖角 25 次判定、太守 28 ＋ 軍師 69 次（**軍師挑的是名單裡最後一個**超過門檻的）。（`docs/mechanics/70-ai`） |
| 分派表：調整兵力與出兵移防 | 完成 | L1 | base | `TestRedistributeMatchesTheOriginal`、`TestRedistributeCountsForeignGenerals` | 調整兵力 40 郡次 279 人；編隊 61 次、帶走的錢糧 58 次、進攻門檻 20 次。（`docs/mechanics/70-ai`） |
| 分派表：開倉賑民 | 完成 | L1 | base | `TestReliefMatchesTheOriginal` | 99 次、六個等級，含 16 位元溢位與第二次扣錢。（`docs/mechanics/70-ai`） |
| 分派表：賞賜金帛與物品 | 完成 | L1 | base | `TestRewardGoldMatchesTheOriginal`、`TestTreasureGiftMatchesTheOriginal`、`TestTreasurePickMatchesTheOriginal` | 賞賜金帛 605 次、六個等級全走到；賞賜物品 186 次、四種寶物；挑誰另外 23 次。（`docs/mechanics/70-ai`） |
| 分派表：買米 | 完成 | L1 | base | `TestRicePurchaseMatchesTheOriginal` | 136 次逐次相同。匯率除數 `[10,10,10,10,9,8]`——第六支的 3 是位移量不是除數。（`docs/playtest/02`） |
| 戰役的逐日：移動力、移動花費、交戰傷亡 | 完成 | L1 | base | `TestZZBattleDaySweep` | **56 個欄位全中**（逐日 50 ＋ 移動花費 2 ＋ 兩次交戰各兩邊的兵 4）。修掉五個實作錯誤，見 `CONTEXT.md` R37／R38。（`docs/playtest/02`、`docs/re/05`） |
| 戰役的勝負判定 | 完成 | L1 | base | `TestBattleFinishesWithPlayer`、`TestBattleOutcomeMatchesTheOriginal` | 在判定發生的那一刻讀原版的軍團統帥與各軍排頭，套 `checkOver` 算勝方——一致。（`docs/mechanics/40`） |
| 戰場六種計謀在實跑的戰役裡走過 | 完成 | L1 | base | `TestBattlePlotsRunLive`、`TestSiegeWithTwoUnits`、`TestZZUnitAIDayParity` | 陷阱那一支量到：隨軍的金 5000 → 4900（費用表 `DS:0x7f62`）、守軍被困 6 天。順帶解出計謀只能對相鄰六格用。2026-09-16 補齊成功的效果：`TestZZUnitAIDayParity` 盤面辛（電腦用計 22 次全成：火攻 6、水淹 3、誘敵 6、圍攻 7）不重拍下每一條鏈進來時被打的那幾支逐將領的兵、將領數與落點都與原版相同（Issue #40）；打光之後的除名與燒死實跑還沒走到。（`docs/re/05`） |
| 戰役開打前的部隊三欄 | 完成 | L1 | base | `TestBattleUnitsMatchTheOriginal` | 兵士數、綜合能力、一天的移動力上限三欄 15 個位元組逐支相同。（`docs/re/05`） |
| 開機裝置選單與防拷密碼 | 完成 | L0 | base | `TestBootPromptsMatchOriginal`、`TestBootAcceptsOnlyOneAndTwo`、`TestPasswordAnswerDoesNotMatter`、`TestZZPassword` | 四題裝置選擇；防拷密碼**任何四位數都過得去**，兩個答案各走十六個月、十八份盤面逐位元組相同。（`docs/re/02`） |
| 容器項目邊界與原版算的一致 | 完成 | L1 | base | `TestContainerOffsetsMatchOriginal`、`TestContainerHeaderReadsAreComplete` | 攔原版對 `DATA1.GRP` 的 seek，比它算出來的起點——逐項相符。（`docs/formats/01`） |
| 兩版差異只繞著難度 | 完成 | L0 | both | `TestWhoTouchesTheEditionTable`、`TestWhoReadsTheEditionWords` | 上限 10 → 20、係數表 11 → 21 格（`DS:0x5430`）；字串 400 條只差一條；戰役勝負判定多兩條。（`docs/spec/004`、`docs/mechanics/90`） |
| 四季事件、蝗害、君主繼承、絕嗣 | 完成 | L1 | both | `TestEventsMatchTheOriginal`、`TestLocustMatchesTheOriginal`、`TestSuccessionMatchesTheOriginal`、`TestNoHeirEndsTheGame`、`TestZZMonthParity$`、`TestZZMonthParityPlus$` | 十二個月的四季事件逐項對上（2026-09-11）：四季常式各跑 1 次、老死判定 17 次、蝗害改地力 1 次，**0 次對不上**。  ⚠ 先前紅是**迴圈寫死十二輪**：四季常式一年各只跑一次（月 1／4／7／10），而 `bootToGame` 停在月中——第一輪按鍵是把當下那個月走完、不是推進一個月，十二輪因此只推了約十個月，從 9 月出發剛好差在秋季（量到春 1 夏 1 冬 1 **秋 0**）。改成「跑到四季都輪過為止」（實測 14 輪），不必跟著起點漂移改數字。  訊息也拆成兩層：秋季沒走到 → `Fatal`「結果不算數」；秋季走到了但蝗害為 0 → `Error` 並指出擺盤兩道閘門都最有利。原本一句話涵蓋兩種病因。  2026-09-16：`SAN1_MONTH=n` 把視窗推到任一個月；四月那一趟量到水災與瘟疫連郡裡的人物一起動（兵、體能），洪水率年度變動加整個舊值、`DS:0x679c` 要數滿 43 格（Issue #39、#41），淹 3 郡與淹 1 郡＋瘟疫 1 郡兩趟 0 位元組。  2026-09-16（Issue #44）：兩版各六個視窗（預設、3、3＋瘟疫、6、9、12）三張表整月 0 位元組——四季常式兩版逐段同形；加強版元月忠誠漂移多一道 `(難度−1) mod 10 + 90` 的門、挖角多一道 `RND(1010)` 難度門；兩版共用碼修了元月順序與對白／特效的擲、地震震兵、蝗害不看所屬且土地價值不夾、挖角挑忠誠最低者＋等級加成＋三條寫回路、武器預算扣折扣價、移防看目標郡餘裕、出兵看存的兵士、買米的 8087 精度（`docs/mechanics/50` §1.1、`70` §2.13.8、`90` §6.6b）。（`docs/re/06`、`docs/mechanics/50`） |
| 建築關寨的三道門與格子編碼 | 完成 | L0 | base | `TestFortMatchesTheOriginal`、`TestZZFortPromptsAsCaoCao` | `DS:0x7134[地形碼] != 0`（山丘、平原、樹林）、`(格 & 0xF0) >= 0xA0` 擋通道格、Y/N 確認；寫入是 `(格 & 0xF6) \| 6`。（`docs/mechanics/10`） |
| 主選單小飾框是六格動畫 | 完成 | L1 | base | `TestZZMenuOrnamentFrames` | 停在主選單不送鍵、每 200 萬道指令抓一次那 12×17 的一格，抓 200 次：**六種畫格**，一輪約 1,400 萬道指令。先前只知道「連拍兩張會不同」。畫格的來源還沒解（`anim-menu-ornament`）。（`docs/re/10`） |
| 一個月的狀態轉移差 0 個位元組 | 完成 | L1 | base | `TestZZMonthParity` | **差 0 個位元組**（2026-09-11）：43 個郡、350 位人物、16 個勢力逐格相同，逐表與逐郡的抽亂數次數全部 ±0。  2026-09-10 換 base 後起點漂移（R49），在新起點上重新校準。走勢：339 →（換對拍起點與固定種子）145 → 97 → 76 → 92 → 88 → 80 → 50 → 17 → 5 → 3 → **0**。位元組數不是單調的——修對一條規則會讓下游浮出更多不一致，過程中看的是「第一個岔開在第幾格」與逐表的抽樣次數。  走到 0 解出的規則記在 `docs/mechanics/70-ai` §2.5b／§2.6／§2.13.1、`docs/spec/007`，推翻紀錄在 `CONTEXT.md` R50／R51。（`docs/playtest/02`） |
| 開新遊戲的盤面 | 完成 | L1 | base | `TestZZNewGameBoardIsCaoCao`、`TestPlantedBoardReadsBack`、`TestLoadedPrefectureOwnersMatchTheSave` | **開新遊戲是一等驗收路徑**，從存檔載入看不到缺口（`CLAUDE.md` §7 第 12 條）。（`docs/re/02`） |
| 加強版跑得起來 | 完成 | L1 | plus | `TestZZBootPlus` | `ASV.EXE` 載進 dosgolem、開 253 個檔、畫出標題。卡點是 `DATA0.GRP` 的 `e_cblp = 0xAA90` 只有低九位有意義。（`docs/mechanics/90`） |
| 原版的亂數是 MSC 的 LCG | 完成 | L0 | both | `TestRandIsTheMSCLCG` | 核對 400 次逐次相同。**公式通用、進入點與狀態變數位址不通用**。（`docs/playtest/02`） |
| 存檔載入：原版與 remake 讀同一格逐位元組相同 | 完成 | L1 | base | `TestOriginalSaveLoadsIdentically` | 19,220 個位元組，**除兩個快照欄位之外完全相同**（兵士與在職將在存檔裡是寫檔當下的快照，原版載入時不重算）。（`docs/formats/05`、`docs/re/08`） |
| 存檔寫出：原版讀得回 remake 存的那一格 | 完成 | L1 | base | `TestRemakeSaveLoadsInOriginal` | remake 走完一個月存進第 1 格，六個項目換進**原版目錄複製品**的 `DATA2.GRP`，原版讀回來 **19,220 個位元組完全相同**，載入流程六個檢查點都是 0。（`docs/formats/05`） |
| 出兵：整編、攜帶錢糧、確認 | 完成 | L1 | base | `TestZZPlayerSortieDriven`、`TestSortieMustersMatchTheOriginal` | 郡 11 打郡 4，走完軍事 → 發動戰役 → 選郡 → 整編 → 攜帶錢糧 → 確認，原版動的 3 個位元組逐格相同。（`docs/playtest/04`） |
| PC 喇叭語音 | 完成 | L1 | base | `TestSpeechDrivesTheSpeaker` | `docs/re/09` |
| 戰略層五種謀略在實跑裡走過 | 完成 | L1 | base | `TestStratagemsRunLive`、`TestTigerWolfRunsLive`、`TestSabotageRunsLive`、`TestAIPlotMatchesTheOriginal` | 策反人民的五刀（`0x2d6e0`）**逐條對上**（2026-09-11）：民忠 −`RND(魅/10)`、洪水 +`RND(魅/5)` 夾 100、地力 −`RND(魅/12)`、米 −`米×100/(RND(魅)+300)`、金 −`金×100/(RND(魅)+500)`。實測魅力 43：擲值 3／5／1／11／17 → 90→87、10→15、90→89、20000→13570、9000→7260，五條全中。  ⚠ 先前紅是**測試量錯時間點**，不是規則缺口：`before` 在常式入口讀、`after` 卻等送完所有鍵才讀，中間整段玩家流程都還在跑，於是量到「民眾忠誠 90 → 100」這種 `0x2d70c`（是減）根本不會做的事。改成入口與 `0x2d7ef`（五刀寫完的第一道指令）各讀一次就通了，並且把五道 `RND` 的實際擲值也記下來，不必再窮舉。（`docs/re/07`、`docs/mechanics/30`） |
| 三張表在記憶體裡連續且位置找得到 | 完成 | L0 | base | `TestScenarioTablesLiveInMemory` | 拿劇本檔的位元組去搜，找到且三張連續（0x399B0）。（`docs/spec/003`） |
| 四種拉幕逐步量到的畫面 | 完成 | L1 | base | `TestZZTransitionFrames` | 直接呼叫 `0x32e40`（`oracle.Call`）＋ 把 `RND(4)` stub 成指定值，四條路一次跑完，每一步存畫面。**三個量一起看才指得出模型**：與前一步的差異範圍、那一塊的顏色直方圖、與最後一步的差異點數。  只問「像不像起點／終點」會得到「兩邊都不像」——那句話排除得掉一個模型，卻指不出下一個（`CONTEXT.md` R55）。（`docs/re/10`、`docs/spec/010`） |
| 部隊層 AI 的決策鏈 | 完成 | L2 | base | `TestUnitAIDecisionChain`、`TestUnitAIRangedAndPlotOptions`、`TestWhoMovesTheUnits` | 決策鏈與遠攻／計謀選項的分支（R43：不在對戰子地圖那一側）。（`docs/re/05`） |
| 戰場徵兵 | 完成 | L1 | base | `TestWarRecruitMatchesTheOriginal` | 與戰略層的徵兵是兩條路。（`docs/mechanics/40`） |
| 喇叭的位元序列逐段對回素材（含跳過首位元組） | 完成 | L1 | base | `TestSpeechBitStreamMatchesTheClip` | 量到的 165 個間隔換算回位元跑：從素材第 0 個位元組起只對上 36.4%，**殘留位元組 ＋ 第 1 個位元組起對上 165／165（100%）**。成因是 `L0`——兩個公開入口都把首位元組寫進 `cs:[0x28]`（IRQ0 的暫存器保存區），而播放迴圈讀的是 `cs:[0x36]`。remake 的取捨在 `docs/spec/008` R6。（`docs/re/09`、`docs/spec/008`） |
| 播出去的位元組就是容器裡的素材 | 完成 | L1 | base | `TestSpeechClipsAreTheAssetBytes` | 攔載入器（`0x5a76`）與播放入口（`0x5deb`），把遠指標指到的位元組倒出來與 `DATA1／S000.SND` 比，**雜湊相同**。槽 0 是音效、槽 1–3 是語音，原版自己的訊息字串（`Real#0>1000`／`Real#1-3>4200`）就是這樣分的。（`docs/re/09`、`docs/spec/008`） |

### 發行

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 推送到 origin | 完成 | — | — | 指令重數 | 推上去了（`e9fab0f`，2026-09-10）。⚠ **這個訊號是弱的**：`origin/main` 是 remote-tracking ref，讀它不用網路，所以它反映的是**上一次 fetch／push 當下**的狀態，不是遠端此刻的狀態。這一條會隨著新 commit 來回翻面（有東西沒推就報過期），那是它的用途。CONTEXT §8 原本記的「領先 187 筆」是舊資訊；2026-09-14 預設分支由 `master` 改名為 `main`。 |
| 三平台簽章 | 未做 | — | — | **要人判** | 現行 `tools/release.sh` 只產未簽章本機封包；Windows 憑證、macOS 真機簽章／公證及 GPG 流程均須另建收據。**簽章不在 M8 的出口條件裡**。（`docs/release/01-packaging.md`） |

### 素材

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 兩版抽檔分開放 `workplace/orig/{base,plus}/` | 未完成 | — | both | 指令重數 | `CLAUDE.md` §3.4 要求兩版都當一等公民抽檔、記雜湊、分開放。目前是直接唯讀掛 `org_game/`，那個目錄沒建。（`CLAUDE.md §3.4`） |
| 哪一則訊息配哪三段語音 | 未完成 | L3 | base | present：docs/spec/008-speaker-audio.md 的「哪一則訊息配哪三個索引還沒解」 | 訊息常式（`0x3273e`）有 111 個呼叫端，每則放三段 `R%03d.OKR`。索引怎麼來**沒解**——目前的對拍一次都沒走到訊息常式（走到的 320 次全是防拷畫面的動畫）。  補法：先找一條會吐帶語音訊息的按鍵路徑，再攔 `0x5a76` 記每次載入的檔名。（`docs/re/09`、`docs/spec/008`） |
| 音效與語音轉得出 WAV／OGG | 完成 | L1 | base | 指令重數 | `cmd/san1assets -what speech` 把 `S000.SND` 與 465 段 `.OKR` 解成 WAV（466 段、總長 240.7 秒），`tools/assets.sh` 再轉 OGG。  ⚠ **轉檔照素材整份轉，不跳第一個位元組**：原版播不出素材的第一個位元組（`docs/re/09` §5.1），那是它的臭蟲，轉檔沒有理由跟著掉。  ⚠ 取樣率是**模型不是量測**（`docs/spec/008` R8）。（`docs/spec/008`） |
| 自創君主的名字字模 | 完成 | L1 | base | `TestGlyphsSurviveASaveLoadRound` | 開自創君主的局時把**原版出貨的 `BASEPRE`** 帶進來（`DATA2` 的 `BASEPRE.SV1`；六個進度位元組完全相同，內容就是「新君主」三個字重複四次）。存檔寫它、讀檔讀回來。  一般君主的局不帶——存檔那一層沿用上一次寫出去的，**存讀一輪不該把別人畫好的字洗掉**。  ⚠ 要讓玩家改名還得自己產字模（從 remake 的字庫取），那一步沒做（`docs/spec/013` §7）。（`docs/spec/013`、`docs/re/08`） |
| 兩支 EXE 都有 `.i64`（M0 出口條件） | 完成 | L0 | plus | 指令重數 | 2026-09-10 補上加強版那支：`tools/ida.sh analyze ASV.EXE` → `workplace/ida/ASV.EXE.i64`（2.25 MB），拿 `probe.py` 驗過——SHA-256 `ad18a251…88be5` 與素材相符、274 個函式、3 個段。**exit code 不算證據**（`tools/ida.sh` 開頭那條），判準是探針的 JSON。  順帶量到一件事：加強版靜態只看得到解壓 stub。`ASV.EXE` 的 MZ 檔頭是**重定位 0 項、檔頭 512 B、最小配置 192,656 B**——自解壓的形狀；原版 `AA.EXE` 是 5,342 項／21,504 B／12,240 B。兩支都掃不到可辨識的壓縮簽章。**這解釋了為什麼兩版差異只能用執行期對讀解**（`docs/re/01` §3、`docs/spec/004`）。  worklist 原本把整條記成「`tools/ida.sh` 包裝器，對兩支 EXE 產 `.i64`」而沒打勾——包裝器與原版那支早就在了，這是 M0 的出口條件之一，所以「M0 完成」與這一條沒打勾長期並存。（`docs/re/01`） |
| remake 的 PC 喇叭播放器（音效與語音） | 完成 | L1 | base | `TestBankEnforcesTheOriginalLimits`、`TestMixerGates`、`TestRenderResamplesAndSmooths`、`TestClipReadsBitsHighFirst` | `internal/speaker`：一位元 PCM 解碼、四個槽與原版的長度上限、兩個開關（關音效連語音都不出聲）、一句話三段、非同步播。接進 `cmd/san1` 的 `voicebox`。  ⚠ **取樣率是模型不是量測**（`docs/spec/008` R8，`L3`）：原版走軟體延遲迴圈，音高本來就因機器而異。（`docs/spec/008`） |

### dosgolem

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 開機配方還是指令數觸發，不是行為觸發 | 未完成 | — | base | present：internal/parity/turn_oracle_test.go 的「o.Run(d+_d+_d+)」 | **2026-09-10：這一條的預言成真了。** 它原本寫著「指令數會隨執行器改動而變，所以現在的配方當不了回歸測試」——換 base 到 dosgolem `main` 之後，`bootToGame` 最後一步的 1.2 億道指令預算讓原版多跑了 29 格月迴圈，順序表整份不同，**四支對拍因此變紅**（月度、玩家命令、四季事件、策反）。\n\n原版在主畫面**不是靜止的**：沒有玩家輸入時它會自動跑完電腦的郡，一路跑到玩家的郡才停。所以「停在哪」由預算決定。新的停點（游標 28）其實**比舊的正確**——舊的游標 0 是預算用完的半路狀態。\n\n⚠ 已試過但**行不通**的修法：把停點改成「跑到游標歸零」。游標 0 一個月只出現一次，錯過就要等下個月，而原版停在玩家的郡不動——跑滿 2.4 億道指令游標還是 28。判準要換的是**視窗怎麼取**，不是只換停點。\n\nR46 已經把主選單那一步換成行為路標（攔 `36C9:02B0`），其餘幾步還沒換。`bootToGame` 現在會印停在第幾格（`monthCursor`），讓漂移看得見。（`docs/re/02`、`docs/playtest/03`） |
| probe 看不到 `B0000`（Hercules） | 未完成 | — | both | absent：../dosgolem-san/internal/machine 的「Hercules、HERCULES、monochrome graphic」 | 目前只看 `A0000` 與 `B8000`，選 Hercules 時會得到**假零**——畫面明明有東西而 probe 說沒有。 ⚠ pattern 原本寫 `herc`（不分大小寫），而那會誤中 `OtherChannels` 裡的「herC」——**太寬會反過來誤判已完成**。改成完整字。（`docs/re/00`） |
| 文件裡的 dosgolem 分支名要跟得上實際分支 | 完成 | — | — | 指令重數 | 2026-09-10 抓到第一次：`CLAUDE.md` 兩處與 `CONTEXT.md` 一處都記著 `san1-msc-oracle`，而實際分支早就是 `san1-draw-speed-and-speech`。  **同一天下午這條 verify 就開口第二次**——換 base 到 `origin/main` 之後分支變成 `san1-oracle-parity`，文件又落後了。加上去幾小時就抓到一次，這正是它存在的理由。  改法分兩層：`CLAUDE.md`（規則）**完全不寫分支名**，只留「開獨立分支、用前先問 `git branch --show-current`」；`CONTEXT.md` §1（現況）記實際的那一條。verify 因此綁 `CONTEXT.md` 不綁 `CLAUDE.md`——規則檔裡放易變的東西，本來就是過期斷言的來源。（`CLAUDE.md §4.1`） |

### 資料格式

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 諸侯記錄 72 個位元組裡還有 57 個沒解 | 未完成 | L2 | both | **要人判** | 已建立 15 bytes 的語意：offset 0 控制者、2 君主、4 AI 等級、6 軍師、8 人望、14–18 玉璽／四種寶物。**offset 10–13、19–71 仍是 unknown，不是 padding，也不能宣稱沒有讀寫**；完整覆蓋矩陣在 `docs/spec/003` §4.1。是否繼續完整考古仍需人決定。（`docs/spec/003`） |
| `.MSK` 解出來了：`REC10` 的天空遮罩 | 完成 | L1 | base | `TestCreditsAssetsMatchTheOriginal`、`TestDecodeMaskIsOnePlane` | 格式：表頭與 `.IMG` 相同（先高後寬），但**只有一個平面**（`assets.DecodeMask`）。整套素材裡只有一張 `ENDO4.MSK`，640×151。  **它是 `REC10`（山城風景）的天空遮罩**：位移 y=54 時逐點吻合 **99.39%**，而同一個判準對另一張山景 `REC11` 只有 51.31%（正對照）。用途是讓製作群的字幕從山後面升起來（`docs/spec/012`）。  ⚠ **怎麼合成是推測**：那一段跑在 `DATA0.GRP`／`DATA4.GRP` 兩支 還沒反組譯的 overlay。remake 的合成方式記在 `docs/spec/012` R2。（`docs/formats/07`、`docs/spec/012`） |
| 配樂的追認規格 | 完成 | L1 | both | `TestParseAllSongs`、`TestStreamSilenceKeepsTime`、`TestBadDataIsRejected` | `internal/music` 先寫完才補規格。規格只描述已經驗過的行為，沒驗的（滑音、每拍 tick 數、換曲時機、`MUSV` 在哪用）列在 §5。（`docs/spec/009`、`docs/formats/06`） |
| `.OKR` 是 PC 喇叭的語音（共 465 項 ＝ DATA2 418 ＋ DATA3 47） | 完成 | L1 | both | `TestSpeechClipsAreTheAssetBytes` | 整支播放器解出來了：`.OKR` 是**一位元 PCM**，最高位先送，沒有表頭、沒有壓縮。長串的 `FF` 與 `00` 是靜音與飽和段。  ⚠ 這一條原本記著「壓縮過、格式未解」而且推論「原版主戰場的地形版面在 `.OKR` 裡」——**兩句都錯**。戰場地圖在州郡記錄的第 55–174 個位元組（`docs/spec/003`）；`internal/battle` 的註解已經跟著改掉。推翻紀錄在 `CONTEXT.md` R54。（`docs/re/09`、`docs/spec/008`） |

### 畫面

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 長沙的填色對不上 | 未完成 | L1 | base | present：internal/ui/artfill_test.go 的「長沙」 | 原版畫圖樣 0（純淺紅），而州郡記錄的所屬寫的是 2。35 個有主的郡裡其餘 34 個全中。 訊號在 `internal/ui/artfill_test.go`（`include_tests`）：那一格是**已知例外**，容忍它的理由就寫在測試的註解裡。（`docs/formats/07`） |
| 多人（玩家人數那一層）還沒做 | 未完成 | L2 | base | absent：internal/session 的「Players []state[.]FactionID」 | 原版開新局是「時期 → **玩家人數** → 角色 → 難度」（手冊 p.7）。remake 只有單人：`session.Session` 的 `Player` 是一個勢力，月流程也只讓那一個下令。  多人要動的是 session 的回合結構（每個玩家依序下令），不是選單那一層。⚠ **對拍全是單人**，改動要小心別動到單人那條路。  ⚠ **為什麼先不做**：玩家與電腦的真相是 `Faction.ByComputer`，而它在讀檔時是用單一的 `Player` 重算的（`internal/game/restore.go`）。多人要改那個真相來源並加存檔欄位，而那正是月度對拍的根基。等對拍那一條回來再動。（`docs/reference/01-manual-20-mechanics`） |
| 主選單小飾框那六格動畫的圖從哪來 | 完成 | L1 | base | present：internal/assets/mainscreen.go 的「func CursorFrames」 | `DATA1` 的 `CURA0`–`CURA5` 加同名 `M` 遮罩，`(底 AND 遮罩) OR 圖`；它是主選單提示的輸入游標（回呼槽 0 `0x10d7a`，`docs/spec/014` §4.1）。`TestZZMenuOrnamentFrames` 1,000 次取樣定出順序與週期，`TestDosgolemMenuMatchesDosboxX` 兩個執行器各自是合法畫格。（`docs/re/10`、`docs/spec/005`、`docs/spec/014`） |
| 四種拉幕轉場與場景圖（48 個呼叫端＋尋訪） | 完成 | L1 | base | `TestWipeStepCountsMatchTheOriginal`、`TestWipeTouchedAreaGrowsFromTheEdge`、`TestWipeSlidesTheNewPictureIn` | 四個分支逐步量畫面（`TestZZTransitionFrames`）：**效果是「新畫面整塊滑進來」**——第 n 步把新畫面靠對邊的那一塊貼到這一邊，最後一步兩邊重合、歸位。  判準是**與前一步差異的範圍**：量到的是整個已蓋區（分支 0 第 5 步 `y80–99`），「逐條露出」的話那只會是新增的 `y96–99`——兩個模型在縮圖上看起來一樣。  拉幕搬的是場景圖 `SCG##`：`0x32dfa` 有 48 個呼叫端、三種落點，尋訪直接叫 `0x32e40`（`docs/spec/010` §1.1）。`TestZZSceneEffectMatchesTheOriginal` 四個方向每一步整張畫面逐像素相同；玩家命令那一擲的位置兩版逐命令比過抽樣序列（§8）。remake 是 `ui.NewSceneWipe` ＋ 佇列裡的 `Bubble{Scene, Style}`／`Speech{Scene, Style}`。⚠ **搬運常式 `es:[0x3efc]` 沒有反組譯**，結論是從行為量的（`docs/spec/010` §7）。（`docs/re/10`、`docs/spec/010`） |
| 君主→指定軍師：原版提示「<指定軍師>謀略須大於79／指定那一位」與「%s將任／%s的軍師」；三張表對拍 | 完成 | L1 | base | present：internal/parity/pickentry_oracle_test.go 的「func TestZZAppointChiefMatchesTheOri」 | `TestZZAppointChiefMatchesTheOriginal` 清單一格相同，指定之後三張表在結束回合那一刻逐位元組相同；場景圖、訊息與對白那幾格沒有對拍畫面；「君主不可任軍師」（模式 6 碰不到）沒接。（`docs/spec/014`、`docs/mechanics/20`） |
| 君主→指定太守：挑自己的其他郡 → 那一郡的清單（模式 2、鍵 3）→ 身分互換；不耗回合 | 完成 | L1 | base | present：internal/game/personnel.go 的「func (g *State) GovernorTarget」 | `TestZZAppointGovernorMatchesTheOriginal` 挑郡一格（字色逐郡）、下令的郡重問、清單一格相同，指定之後三張表逐位元組相同、原版回主選單；`TestAppointGovernorOrderTargetsAnotherPrefecture`。場景圖、對白與「%s將任／%s的太守」那幾格沒有對拍畫面。（`docs/spec/014`、`docs/mechanics/20`） |
| 原版素材畫面看得到指令表、子選單、挑選清單、分頁與戰場選項 | 完成 | L1 | base | `TestArtSessionDrawsWhatThePlayerNeeds`、`TestSubMenuLinesMatchTheOriginal`、`TestSubMenusShowInFullInEveryLanguage`、`TestCommandMenuLayout`、`TestPagesFitTheOverlayInEveryLanguage`、`TestArtBattleDrawsOptionsAndPages`、`TestBattleOptionsFitThePanelInEveryLanguage`、`TestArtBattleTallOptionsGoAboveThePanel` | 預設畫面（`-art`）先前**只畫提示**：打開子選單、挑選清單、數字輸入或分頁，畫出來與沒打開差 0 個位元組——玩家看不到任何選項，只能靠記憶操作。文字版一直都有畫，所以別的測試看不出來。  照原版補（`TestZZSubMenuScreens` 在 dosgolem 裡把九個子選單各存一張、逐列量墨水）：主提示時上面板是**十項指令表**（兩欄五列、雙倍字、偶數黃奇數洋紅），按 `0` 或方向鍵換郡才換成郡的資料；子選單畫在**下面板**四行，中文與原版字串逐行相同；分頁蓋整個內容區、藍底。  主戰場同一個洞：選了用計之後六種計謀看不到、查看部隊那一頁看不到。選項改成取代面板上的三行（中文仍是原版的三行三列），英日文放不下就在面板正上方畫選單框。（`docs/spec/014`） |
| 君主→郡縣自冶的問法：授權別的郡（同一主人、主事者不是君主）→ 下面板「那一種:」一位數 → 回到挑郡；不耗回合 | 完成 | L1 | base | present：internal/game/personnel.go 的「func (g *State) AutonomyTarget」 | `TestZZAutonomyAskMatchesTheOriginal` 挑郡一格（字色逐郡）、君主的郡重問、「那一種」下面板與游標相同、打 3 寫回 2 並回到挑郡；`TestAutonomyOrderTargetsAnotherPrefecture`。設定之後兩格對白的畫面沒有從這條路對拍。（`docs/spec/014`、`docs/mechanics/20`、`docs/mechanics/10`） |
| 主戰場下方花邊上的日期沒畫 | 完成 | L1 | base | `TestBattleDateCellsMatchTheOriginalSample`、`TestBattleDateCellsAlignRight`、`TestDrawRuneBoxDitherAlternates` | 十格版面（每格 24 寬、左緣 `128+40i`、上緣 `y=378`）與**兩色棋盤**（`(x+y)` 奇數色 7、偶數色 2）都量出來了，`drawBattleDate` 畫上去。  ⚠ 原本記著「網點的機制沒解，可能與 `8x8AND*`／`.MSK` 遮罩是同一件事」——**量相位就知道不必扯到遮罩**：(+1,0) 有 388、(0,+1) 有 329、右下 (+1,+1) 只有 4 點，那是 `(x+y)%2` 不是陰影。  ⚠ 還是**只有一個樣本**（建安二年九月秋）：年數三個中文字時怎麼排、西曆那條路寫什麼都沒量過（`docs/spec/011` §5）。字形也不與原版逐點比——remake 不內嵌原版字模（`CLAUDE.md` §3.3）。（`docs/spec/011`） |
| 戰場、對戰子畫面與單挑的對白走訊息框 | 完成 | L1 | base | `TestZZBattleSpeechMatchesTheOriginal` | 2026-09-17（Issue #52）：戰場的對白框是面板——主戰場與對戰子畫面用第三塊 (448,268)–(623,363)（呼叫端先填藍）、單挑用攻守各自那一塊側面板（位置 `DS:0x7b24`／`0x7b34`，肖像攻左守右 `DS:0x8894`）；47 個呼叫端的片語與說話者列在 `docs/spec/005` §9.7。remake：`internal/battle` 每一處原本就擲的字色那一擲改由 `say` 留下值排進 `Battle.Speeches`，`cmd/san1` 一格一格畫（`ui.DrawBattleSpeech`）、任意鍵收；骰序不變。戰役結束回郡那一句（478）在 #60 接上。（`docs/spec/005`、`docs/re/05`、`docs/re/12`） |
| 主戰場誘敵的特效（0x2b783） | 完成 | L1 | both | `TestLureFlashSteps` | 對白之後在施法者那一格閃 `EICON.GRP` 第 32–35 張二十二步；盤面辛 6 次每步逐格相同、速度相同（`TestZZUnitAIDayParity`，原版）。加強版 `0x28a4f` 讀碼同形（`L0`）。（`docs/spec/005`、`docs/re/05`、`docs/re/10`） |
| 8×10 版面的主戰場：面板在右側 x 448 疊放 | 完成 | L1 | base | `TestArtBattleNarrowFurnitureMatchesTheOriginal`、`TestArtBattleNarrowPortraitsMatchTheOriginal`、`TestArtBattleNarrowTextCellsMatchTheOriginal` | 2026-09-17（Issue #53）：州郡 offset 34 ≠ 9 的 21 個郡走窄版面（`0x225c8`–`0x226cb`）：上下階梯各 4 組（下緣用 `0x21fe0`，y 324）、左緣黑線到 356、右緣白線 x 440–441 到 373、三塊面板疊在 x 448、y 44／156／268（攻、守、指令），肖像與五行資料的位移表兩種版面共用。remake：`assets.BattleLayout`（`BattleWide`／`BattleNarrow`），`battle.Field.Narrow` 挑；對白框也跟著版面走。基準 `orig-battle-narrow.png`（`SAN1_BATTLETO=26`）整片場地 125,680 格逐格相同、肖像 F228／F002 逐點相同、45 格字落點相同；順手接上寬版面的右緣 x 632–633 與左欄第四框的真正下緣 323。（`docs/spec/005`、`docs/re/05`、`docs/playtest/03`） |
| 主戰場漏畫的字：軍力面板第六行「米」、左欄第四框的國字日數與時辰 | 完成 | L1 | base | `TestArtBattleTextCellsMatchTheOriginal`、`TestArtBattleNarrowTextCellsMatchTheOriginal`、`TestBattleDayNumeralsFollowTheTable` | 2026-09-17（Issue #57）：`0x22c94` 第六行 `米%6d`（y0＋80，`DS:0x7bc6`）；左欄第四框 `0x219b5`–`0x21b38`：國字日數三格各 32×16（sx2 sy1，位數表 `DS:0x78a8＋4×天` → `DS:0x7924` 起的「　十一…九」）、「日」32×16、時辰名 `DS:0x7946[((時＋1) mod 24)÷2]` 16×16 淺青、「時」16×32、時數 `%2d`；時刻 `es:0x31a6` 主戰場上永遠 6。remake：`bat.riceLine`、`drawBattleDayBox`／`battleDayNumerals`；英文用阿拉伯數字與 Day／Hour（remake 差異）。（`docs/spec/005`、`docs/re/05`） |
| 對戰子畫面的畫面與玩家選單 | 完成 | L1 | base | 指令重數 | 子地圖、將領標記、左欄時刻、輪到的人反白、第三塊面板的選單照 `0x2deb0` 畫；玩家在子畫面裡下令。標記以外的場地與原版逐像素相同，字格底色與墨、部隊面板、選單、左欄時刻相同。  ⚠ 加強版的子畫面畫法沒比過；DOSBox-X 交叉驗證沒做（`docs/spec/005` §8）。（`docs/spec/005`、`docs/re/05`） |
| 主戰場第三塊面板的文字視窗：紮寨、命令、休息／退兵確認、方向、策略照原版字串與順序 | 完成 | L1 | base | present：internal/ui/battlewindow.go 的「func BattleCommandWindow」 | `TestZZBattleWindowsMatchTheOriginal` 紮寨與命令兩句逐字相同、五格字格墨相同；計謀清單那一格未實拍。（`docs/spec/014`、`docs/re/05`） |
| 對戰子畫面的部隊面板與主戰場查看：面板換成當事部隊／那一位的肖像 | 完成 | L1 | base | `TestZZUnitPanelsMatchTheOriginal`、`TestZZInspectPanelMatchesTheOriginal` | 2026-09-17（Issue #55）：`0x320a6(軍力, 隊伍)` 在對戰子畫面進場與攻擊／單挑結算後把兩塊軍力面板換成那兩支部隊的面板——第 0 槽那一位的肖像（攻方翻面）、框 `FBRA`、32×32 名字、君主／軍力名／（空）／隊伍名與將數／兵數／（空）；`0x284a2(軍力, 人物)` 查看：第三塊清藍、肖像 (552,276)、框 `FBRB`、名字 x 512、六行 `體能／謀略／戰力／訓練／武裝／兵`。remake：`ui.UnitPanel`／`InspectPanel` 掛在 `ArtBattleInfo`，子畫面裡的對白記著兩支部隊（`battle.Speech.Units`）畫那一句時換面板；查看的覆蓋頁打開時第三塊畫第 0 槽那一位。順手：指令面板的字從 (448,268) 起、肖像框四片全畫。沒接：對戰子畫面本身（子地圖、時刻、選單）remake 沒有畫面。（`docs/spec/005`、`docs/re/05`） |
| 內政→建築關寨：兩道閘門訊息、「<建築關寨>須用%d金…那一位去」與建完的「現有%d個關寨／剩餘金」照原版 | 完成 | L1 | base | present：cmd/san1/main.go 的「func (a *app) buildFort」 | `TestZZBuildFortAskMatchesTheOriginal` 兩道閘門與清單一格相同；挑位置與建完的三張表由 `TestZZFortSpotMatchesTheOriginal`（#94）驗。（`docs/spec/014`） |
| 商業三項照原版問數字（買多少金的米／賣多少米／給多少米）、閘門訊息與成交後「現有」那一句；賣米零頭被吃掉 | 完成 | L1 | base | `TestZZCommerceAskMatchesTheOriginal` | `TestZZCommerceAskMatchesTheOriginal` 三項的範圍、下面板、游標與成交後三張表相同；賣米改成整份扣米（R84）。軍師評價在原版是問數字之前、remake 在之後；閘門與成交訊息照碼寫沒有實拍。（`docs/spec/014`、`docs/mechanics/60`） |
| 自創君主的名字畫成字模的「新君主」，不是標點（A141–A14C） | 完成 | L1 | base | present：internal/state/scenario.go 的「g.Name = ShowCustomGlyphs(name)」 | `state.ShowCustomGlyphs` 在人物表解碼時換字、IsPerson 照原始碼位判；`TestZZNewLordBornMatchesTheOriginal` 名字有墨直欄原版 39／remake 45／標點反對照 10；`TestOriginalCustomLord` 驗碼位寫回不變。（`docs/spec/013`） |
| 示範模式月底的鏡頭：換被看的郡與人、偶數月郡資料面板、奇數月人物卡（0x1e1fc） | 完成 | L1 | base | present：internal/game/democamera.go 的「func (g *State) RunDemoCamera」 | `TestZZDemoCameraMatchesTheOriginal` 1–4 月換到的郡／人、排的那一格、之後的亂數狀態相同，右側面板畫面對上；開發期的金米檢查不做。（`docs/re/06`、`docs/spec/005`、`docs/spec/019`） |
| 挑選畫面的 DOSBox-X 交叉驗證：主命令提示、挑郡清單、挑人清單、君主物品表四格 | 完成 | L1 | base | `TestZZDosboxScreensMatchTheRemake` | `TestZZDosboxScreensMatchTheRemake` 拿 `rec18`／`rec19` 的畫格對 remake 的算圖（盤面從劇本 001 重建）：四格都是字以外逐像素相同、字格墨相同。`TestZZDosgolemMatchesDosbox` 的驅動配方走不完這條含防拷密碼的路（第 12 步起岔開）。關寨的金閘門與挑位置畫面 DOSBox 這側錄不到，原因記在 docs/playtest/03。（`docs/playtest/03`） |
| 製作群畫面（原本記成「結局畫面」） | 完成 | L1 | base | `TestDrawCreditsRespectsTheMask`、`TestCreditsLength` | 那一組不是結局畫面，是**製作群**（`CONTEXT.md` R57）：`UPR00`–`UPR21` 的字幕解出來就是名單。remake 在統一之後播——朝堂圖定格 → 字幕從山後面升起來。  ⚠ **原版什麼時候播、怎麼動都沒解**（`docs/spec/012` R3／R4）：那一段跑在 `DATA0.GRP`／`DATA4.GRP` 兩支還沒反組譯的 overlay。接在統一之後是 remake 的推測。（`docs/spec/012`） |
| 建築關寨挑位置：場地圖、五行說明、MAPCUR1 的 XOR 游標與六方向鍵、確認(Y/N)照原版 | 完成 | L1 | base | `TestZZFortSpotMatchesTheOriginal` | `TestZZFortSpotMatchesTheOriginal`：按 3 三次到 (3,1)，第二頁整張字以外逐像素相同，「0」「Y」之後三張表逐位元組相同；`TestFortSpotStepWalksTheHexGrid`。說明五行只比有沒有墨；確認(Y/N) 那一格沒有實拍。（`docs/spec/014`） |
| 君主→登用他國人才的問法：兩道閘門（50 人、100 金）→ 挑郡（任何別人的郡）→ 那一郡的挑人清單（模式 5） | 完成 | L1 | base | present：cmd/san1/main.go 的「func (a *app) headhunt」 | `TestZZHeadhuntPickMatchesTheOriginal` 挑郡一格（字色逐郡）與不相鄰那一郡的清單一格，面板字格以外逐像素、下面板與游標相同；兩道閘門的訊息照碼寫，沒有實拍。（`docs/spec/014`、`docs/mechanics/20`） |
| 提示後面的輸入游標：回呼槽 0 的六格合成，主選單／主畫面／主戰場／開新局設定四組（0x10d7a） | 完成 | L1 | base | present：internal/ui/cursor.go 的「func drawInputCursor」 | 存檔六格、戰場四格、人數與選君主兩格逐像素相同（相位取原版剛畫好那一格）；DOSBox-X rec17／rec7 是合法 CURB／CURD 畫格。人物資料卡等鍵的兩格（查看、賞賜物品）下面板與游標相同（#81）；自創君主的兩行提示沒量。（`docs/spec/014`、`docs/re/10`） |
| 主命令提示照原版「%s主公,請對(%d)／%s下您的命令:」＋數字輸入 0–9 | 完成 | L1 | base | `TestZZMainPromptMatchesTheOriginal` | `TestZZMainPromptMatchesTheOriginal` 下面板與游標 (544,316)、打 4 的回顯、取消之後重印提示都相同（#97）。`0.狀態` 只換上面板、不印字。子選單那一層 remake 還是按一鍵（登記在 §4 差異表）。（`docs/spec/014`） |
| 主畫面命令的對白：軍師勸諫、人事與計略的回話、宣戰 | 完成 | L1 | base | `TestAdviseRollsOnceThenSpeaks`、`TestPlayerOrdersQueueTheirDialogue`、`TestPlayerSearchQueuesTheScreens` | 2026-09-17（Issue #51）：訊息常式在主畫面這一層的 38 個呼叫端逐個讀出框、肖像左右、說話者與片語（`docs/spec/005` §9.6）——全在玩家的命令裡，電腦諸侯本來就不叫它。remake：`game.State.Advise` 擲 `RND(5)+80 < 軍師謀略` 那一擲、回勸諫那一格（19 道命令；買賣看物價、尋訪／登用／挖角／計略看判定、`RND(18)+80` 決定準不準），`cmd/san1` 收完問「主公是否繼續呢(Y/N)」；登用、賞賜、撤職、指定軍師／太守、自治、賜物、挖角、計略的回話由 `Order.Apply` 排進 pending，宣戰兩句在進主戰場之前。郡地理誌那一句在 #60 接上；勸諫之後那一張場景圖與尋訪的拉幕在 #58 接上（`docs/spec/010`）。（`docs/spec/005`、`docs/re/12`） |
| 大地圖上的戰役動畫（電腦對電腦，0x1ecfc） | 完成 | L1 | base | `TestMarchLayoutFollowsTheOriginal` | `ui.March` 兩場每一格整張逐格相同、收尾還原相同、`speak` 速度序列相同（`TestZZMarchAnimationMatchesTheOriginal`）；事件 `Bubble.MapBattle` 不擲骰。往左與往上只讀碼；加強版不播。（`docs/spec/005`、`docs/re/05`、`docs/re/10`） |
| 訊息框：肖像＋對白泡泡（元月與繼承的六處） | 完成 | L1 | base | `TestZZBubbleMatchesTheOriginal` | 2026-09-16（Issue #49）：原版每一則對白都由訊息常式 `0x3273e` 畫肖像、名字、白泡泡與 16×32 的兩行字，字色是進去就擲的 `RND(8)`。remake：`game.Bubble` 掛在事件上、`ui.DrawBubble`、`session.Bubbles` 佇列、主畫面按任意鍵收一格。對拍直接呼叫原版的常式畫上格（肖像在右）與下格（肖像在左），肖像、泡泡、尾巴、名字的黑底逐像素相同。接上的是元月與繼承的六個呼叫端；其餘呼叫端在 #51、#52、#60 接上（防拷那一張的六處不做）。片語表 350–499 抽出在 `docs/re/12`。（`docs/spec/005`、`docs/re/12`） |
| 訊息常式剩下的五組呼叫端：新君主出現、用計三道門、戰役結束回郡、郡地理誌、計略的成敗回話 | 完成 | L1 | base | `TestZZNewLordBornMatchesTheOriginal`、`TestZZAtlasMatchesTheOriginal`、`TestZZBattleSpeechMatchesTheOriginal` | 2026-09-17（Issue #60）：`0x133f2` 自創君主收工——點數用完送 0 才收（`0x13826`／`0x1381e`），印「新君主出現!!」、還原面板、上格往上挪成 (424,66)–(615,161) 說 498，那一郡已歸新君主；`0x28d48` 用計三道門（金／謀略／地點）在第三塊面板說 479／480／481；`0x256fa` 戰役結束援軍統帥說 478 回郡（兩側都還有將才說）；`0x18663` 查看 5 的郡地理誌整張畫在顯示記憶體第二頁（合成常式旗標 −1、通道格疊鄰郡號、主事者說 356）；計略四種的成敗回話 482／483 在上格右（聯合出兵沒有）。remake：`menu.LordBorn` ＋ `ui.DrawNewLordBorn`、`Battle.SayPlotGate`／`SayHelperReturn`、`ui.DrawArtAtlas` ＋ `game.AtlasBubble`、`PlotOrder.Apply`。dosgolem 為第二頁多了 `IndexedEGAFrom`。（`docs/spec/005`、`docs/re/12`） |
| 開新局：自創君主做好了，多人還沒 | 完成 | L1 | base | `TestPicksACustomLord`、`TestOrdinaryLordDoesNotCarryCustomState` | **自創君主做好了**（`docs/spec/013`）：選角色那一層列出新君主欄，四項能力用 100 點分配、領地在空白郡之間換，寫進劇本之後開局。  資料是原版的：劇本 001 的槽 14／15 君主欄指向填充筆且沒有領地，與手冊「16（含 2 個新君主欄）」對得上；初始值取 `AA.EXE` `0x3e44c` 的四筆範本（年齡 20、90／60／80／90、肖像各異）。  ⚠ 三件沒做：**年齡與姓名照範本不給改**（原版怎麼收沒解）、**點數怎麼花是 `L3`**（手冊只說 100 點）、**多人還沒做**（見 `multiplayer`）。字模見 `custom-lord-glyphs`。（`docs/spec/013`） |
| 開機片頭整段（船隊、寫詞、淡出、頭像橫幅、三英圖捲入） | 完成 | L1 | base | `TestBeatsFollowTheOriginalSequence` | `internal/opening` 照 `DATA0` overlay 的兩頁畫面逐拍重做；dosgolem 1,308 拍每一拍顯示頁逐格相同、調色盤相同，DOSBox-X 三張逐點相同；remake 字庫的詞與 `TZUE` 比字格的墨。加強版未驗。（`docs/spec/005`、`docs/re/13`） |
| 開場第一幕的智冠商標畫面 | 完成 | L1 | base | `TestTrademarkMatchesDosboxX` | `CMARKL`／`CMARKR` 是 `AA.EXE` 開機第一幕（y 64），dosgolem 與 DOSBox-X 兩邊整張逐點相同；remake 開場第一幕放它。（`docs/spec/005`） |
| 分頁捲得動、長句折行不截字 | 完成 | — | both | `TestPageScrollsToTheEnd`、`TestTacticalReportsAreTranslatedAndFit` | 分頁先前沒有捲動（只放得下十九行，三十天的戰報、一郡五十位將軍讀不到後面），而且把超出寬度的部分截掉——戰報長句截掉的正好是句尾的傷亡數字。改成 ↑↓／PgUp／PgDn 捲動、長句折行（`PageLines`），標題帶位置。（`docs/spec/014`） |
| 人物資料卡的五個呼叫端：查看的迴圈與 Y/N 門、賞賜物品前後兩張、示範模式與除錯陷阱不做 | 完成 | L1 | base | `TestZZCardTimingsMatchTheOriginal` | 2026-09-17（Issue #59）：`0xf874` 的呼叫端是 `0x17ccb`（查看→3：別人的郡先問「此郡非我軍所有 確定查看(Y/N)」，名單不分勢力，卡片任意鍵之後回到「檢視那位」）、`0x1d183`／`0x1d4d1`（君主→4 賞賜物品：卡 → 「請按任一鍵 查看物品表」→ 物品表 → 那一樣(2-5) → `SCG24.IMG` → 道謝 405 → 再一次卡，`0x1058:0xe80` 延遲或按鍵）、`0x1e336`（示範模式月底的鏡頭：偶數月標郡、奇數月畫隨機一位的卡）、`0xc55d`（調整兵力份額為負的除錯陷阱）。remake：`inspectGeneral` 的 Y/N 門、不分勢力的名單與迴圈；賞賜物品先卡片與提示、任意鍵後物品表分頁、送完 `GiftOrder.Apply` 排道謝＋卡（`game.Bubble.Card`）。賞完回到「賞賜那一位」的兩層迴圈由 `gift-loop` 那一條接上（Issue #79）。（`docs/spec/005`、`docs/re/06`） |
| 挑一位將軍的清單（0x18024）與數字輸入（0x34ede）照原版：右側面板十二列、下面板回顯、欄寬與取消 | 完成 | L1 | base | present：internal/game/pick.go 的「func (g *State) PickRoster」 | `TestZZPickListMatchesTheOriginal` 名單順序、兩頁面板與下面板相同；`TestZZNumberEntryMatchesTheOriginal` 徵兵提示與回顯相同；DOSBox-X rec11 第 18 步面板相同。調動軍隊的挑人、攻打與計略的目標郡、挖角對象仍是 remake 的上面板清單；運送錢糧的來源郡只收下令的郡。（`docs/spec/014`、`docs/re/07`） |
| 五種計略的問法：先挑郡（一到三問）再挑使者，每一問收哪些郡照原版；訊息寫滿一行立刻換行 | 完成 | L1 | base | present：cmd/san1/main.go 的「func (a *app) plotFlow」 | `TestZZPlotPickMatchesTheOriginal` 偽書使疑兩問、遠交近攻三問畫面與字色逐郡相同；驅虎吞狼、策反人民、聯合出兵的問法照碼寫，沒有實拍；計略下完的三張表沒有從這條路對拍。（`docs/mechanics/30`、`docs/spec/014`） |
| 人物資料卡、尋訪找到人、選君主與新君主的肖像畫面 | 完成 | L1 | base | `TestZZPersonCardMatchesTheOriginal`、`TestZZSearchFaceMatchesTheOriginal`、`TestZZLordPickScreenMatchesTheOriginal`、`TestZZCustomLordScreenMatchesTheOriginal` | 2026-09-17（Issue #54）：`0xf7b4` 九個呼叫端裡主畫面這一層的四種畫面接上——查看→武將畫人物資料卡（`0xf874`：灰底、`SIDEC` 外框、`FBRC` 框、名字 32×32、八行資料），玩家尋訪找到人先亮那一位的肖像 (488,88) 再由尋訪者報結果（`Bubble{FaceOnly}`），開新局選君主是一頁六位的肖像、空心框、勢力色塊與編號（`0x124ca`），新君主分配能力畫肖像加框、名字與六行（`0x12df4`）。四張都對原版逐像素比（字模除外），選君主那一格另拿 DOSBox-X 的畫面交叉驗（只差游標）。順手量到：面板清底 `0x27e8` 只清外框裡面；`0x262c` 用樣式 mod 5 挑 `SIDEA`–`E`；文字常式 `0x1d4e` 的「背景」是描邊色；新君主開局的起始值是 18／80／50／50／50（與 spec/013 的範本不同，規則層另開 Issue）。（`docs/spec/005`、`docs/spec/013`） |
| 挑郡清單（0x1d4ec）與調動軍隊的多選清單（0x18286）照原版；玩家的調動軍隊一次搬一份名單、沒有接手閘門 | 完成 | L1 | base | present：internal/ui/prefpick.go 的「func DrawPrefPick」 | `TestZZPrefPickMatchesTheOriginal` 查看／攻打兩格面板、字色、下面板相同；`TestZZMovePickMatchesTheOriginal` 多選兩格、金那一問、搬完三張表逐位元組相同。來源郡只收下令的郡（#82）；主事者走了的「選擇新任太守」remake 自動挑。（`docs/spec/014`、`docs/mechanics/40`） |
| 儲存進度照原版：右側面板六筆名稱、組「n.姓名欄在郡名＋備註」（0x1e440） | 完成 | L1 | base | present：internal/session/save.go 的「func (s *Session) SaveName」 | `TestZZSaveScreenMatchesTheOriginal` 面板逐像素＋名稱墨相同，寫檔前那一筆 21 byte 相同；DOSBox-X rec17 只差游標。Esc 取消是 remake 差異。（`docs/spec/005`、`docs/re/08`） |
| 郡的資料面板右欄：軍師一行、現役將與兵士讀快照、主事者姓名欄從 520 起（0x33020） | 完成 | L1 | base | present：internal/ui/artscreen.go 的「stat.chief」 | `TestZZStatusPanelMatchesTheOriginal` 37 個有主的郡文字格墨相同、其餘逐像素相同（軍師在郡裡 9、不在 28）；`TestArtStatusShowsTheStoredSnapshots` 釘住快照。（`docs/spec/005`） |
| 主選單「使用楷書字／使用隸書字」真的換字型（0x11bb4／0x11bc2） | 完成 | L1 | base | `TestSwitchableFontsCoverTheSameRunes`、`TestSwitchableFontsAreNotUnifont` | 原版換完字型直接重畫主選單、不印訊息。remake 不內嵌原版字模，改用王漢宗顏楷體與中隸書（GPL v2，**沒有字型例外**）點陣化的兩套 16×16；`tools/mkhexfont` 照 unifont 的碼位與字寬產生，TTF 沒有的碼位沿用 unifont。點陣化要先用 3 倍字級畫再按覆蓋率降取樣——毛筆字直接用 16 點渲染會被反鋸齒切成一段一段。設定記在 `Options.Font` 並進存檔；`FontDefault` 是「沒挑過」，因為原版開機用哪一套沒量過。發行包要帶 `LICENSE-wangfonts.txt`。（`docs/spec/014`、`fonts/README.md`） |
| 設難度留在選君主那一頁、印玩家序號（0x122ea） | 完成 | L1 | base | present：cmd/san1/title.go 的「title.difficultyPrompt」 | `TestZZDifficultyScreenMatchesTheOriginal` 框外逐像素相同、提示框兩邊有墨、序號外框重合。新君主欄那條路照原版先問難度再分配能力（#68 對齊，§9.5）。（`docs/spec/005`） |
| 載入進度照原版換直牌與六個按鈕（0x13fb6） | 完成 | L1 | base | present：internal/menu/menu.go 的「func LoadLine」 | `TestZZLoadPickMatchesTheOriginal` 124 個字格墨相同、其餘逐像素相同、反對照差 36 格；DOSBox-X rec16 只差小飾框。空槽只寫「n.」是 remake 差異。（`docs/spec/005`） |
| 音樂欣賞照原版換直牌與五個曲名，選了播並回主選單（0x145ca） | 完成 | L1 | base | present：internal/menu/menu.go 的「MusicTracks = 5」 | `TestZZMusicPickMatchesTheOriginal` 124 個字格墨相同、其餘逐像素相同、反對照差 38 格；送「3」原版 `0x4fb:0x12a(2)` 後回主選單。DOSBox-X rec16 只差小飾框。（`docs/spec/005`、`docs/formats/06`） |
| 選擇年代那一層照原版換字（0x11c7e） | 完成 | L1 | base | present：cmd/san1/title.go 的「DrawTitleLayer」 | 直牌「選擇年代」字色 15、按鈕原版字串逐字排；`TestZZScenarioPickMatchesTheOriginal` 字格墨相同、其餘逐格相同。英日版直牌留白。（`docs/spec/005`） |
| 君主物品表（0x14de6）畫在右側面板；賞賜物品的「那一樣(2-5):」與查看→物品照原版 | 完成 | L1 | base | present：internal/ui/treasury.go 的「func DrawTreasuryPanel」 | `TestZZGiftItemAskMatchesTheOriginal` 「那一樣」與查看物品兩格：面板字格以外逐像素、字格有墨、標題與列的字色、下面板與游標相同；拿掉 remake 物品表的反向對照會紅。得到玉璽事件（`0x15181`）那一條沒接。（`docs/spec/014`、`docs/mechanics/20`） |
| 英日語系照播原版的中文語音（不靜音、不另配） | 完成 | L1 | both | `TestVoicePlaysInEveryLocale` | 裁定（2026-09-18）是沿用原版片段。語音出不出聲只看「音效」與「語音」兩個開關，**不看語系**——多一道語系閘門，英日文玩家聽到的是一片安靜，而安靜與「沒有音效卡」長得一樣。語音是原版素材，發行包一段都不帶。`R499` 的語意仍未解（沒有逐段聽辨證據不得命名），但沿用原版不需要先知道它是什麼聲音。（`docs/spec/008`、`docs/release/01-packaging.md`） |

### 多語系

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 戰報、部隊名與訊息紀錄有譯文 | 完成 | — | both | `TestTacticalReportsAreTranslatedAndFit` | 戰術層的 33 則逐日紀錄全部寫死中文、部隊名用中文的 `String()` 拼，英日文玩家的戰報內文與戰場提示都是中文。改走譯文（`blog.*`、`unit.name`，天氣／軍別／隊形／計謀／地形給 `Label()`），`ui` 的名稱函式回頭呼叫它們。判準是英文戰報裡**沒有任何漢字**，打一批真的戰役（1,484 行）量。  session 寫進訊息紀錄的 17 句（「已存入第 1 個進度」「電腦 AI 換成…」，下面板顯示的就是最後一則）、AI 的名字、曆法名（`cal.*` 鍵早就有，只是沒人用）一起接上。判準是英文下整局跑過（推兩年、存讀檔、換 AI），訊息紀錄的每一行都沒有漢字（`TestEnglishLogHasNoChinese`）。（`docs/spec/014`） |
| 槽位溢出：每一塊畫面都量過了 | 完成 | — | — | `TestSubMenusShowInFullInEveryLanguage`、`TestArtStatusFitsEveryLanguage`、`TestArtDateStaysInTheStrip`、`TestPagesFitTheOverlayInEveryLanguage`、`TestTextScreenClipsNothing`、`TestTacticalReportsAreTranslatedAndFit`、`TestBattleOptionsFitThePanelInEveryLanguage`、`TestArtBattleEnglishStaysInThePanel`、`TestBattleSidePanelsFitEveryLanguage` | **每一塊都有一支測試量三個語系**（`docs/spec/014` §6 有對照表）：下面板的子選單、上面板的指令表與郡的資料、左側直條的年月、分頁、開局選單、戰場的選項與左右面板、文字版的每一種狀態（`Canvas.Clipped` 量右緣被截的字）。  量出來並修掉的：英文五類子選單原尺寸排不進下面板（改用小字）；表格寫死的欄寬會截斷（`GovernorGold`、`Floo`、中文將軍列表的忠誠欄標成「民眾」→ `cells.Columns`）；郡的資料面板欄名寫死中文、拼音郡名壓到州名、州名的字不在拼音表；英文的年月一個字母一列畫到花邊上（→ 轉 90° 排）；季節接成「month 1Spring」；戰場用計的選項英文六行（→ 面板上方選單框）；文字版的分頁只到地圖區、清底色沒清；文字版指令欄兩欄疊字；存檔描述疊兩個編號。英文放不下原版格子的地方改用 6×10 小字、留在原版位置，不縮短名稱（使用者裁定 2026-09-11）。  戰報頁也量了：真的打一批戰術層戰役（每個語系 1,484 行），長句連中文都超過分頁（125 格），改成折行不截字、加上捲動（`docs/spec/014` §8）。（`CLAUDE.md §3.3`、`docs/spec/014`） |
| 英文下的訊息紀錄沒有漢字 | 完成 | — | both | `TestEnglishLogHasNoChinese` | session 自己寫的 17 句、AI 名字、曆法名先前寫死中文；整局跑過（推兩年、存讀檔、換 AI）量每一行。（`docs/spec/014`） |

### 規則層

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| 年號怎麼數還沒查證 | 未完成 | L2 | base | **要人判** | 年號表在手冊給的六個時期上全部對上，但**原版開局後怎麼數**沒查證——推過一年之後它顯示「中平七年」還是「初平元年」。（`docs/mechanics/10`） |
| 強化 AI 真的在推動局面（與 base 的差異量出來了） | 完成 | L1 | base | `TestZZAIModesMoveTheBoard` | 全電腦跑劇本 001 三十六個月，兩種 AI 各一次（強化那一邊是預設的**每郡每月一道令**）：  \| \| `base`（還原）\| `enhanced`（強化）\| \|---\|---\|---\| \| 存活勢力 \| 14 \| 13 \| \| 最大勢力 \| 4 郡 \| **10 郡** \| \| 空白郡 \| 14 \| 9 \| \| 易主 \| 4 郡 \| 12 郡 \| \| 總兵 \| 166,250 \| 110,377 \| \| 總金 \| 7,320 \| **47,260** \|  強化那一邊總兵比較少不是它不會徵兵——它**一個月只下一道令**，那一道多半花在內政上；換來的是六倍多的金與兩倍半的領土。把「電腦指令」調到 2 以上，兵與金就同時超過（`TestZZAIOrdersSweep`）。  ⚠ 這**不是「誰比較強」的裁決**——兩者的驗收標準本來就不同。量的是「局面有沒有被推動」，那是「有沒有作用」的最低標準。  ⚠ 先前沒有任何測試在驗這件事：`internal/ai` 的三支 enhanced 測試（合法、決定性、一郡一令）在一個**什麼都不做**的 AI 上也全部成立。（`docs/mechanics/70-ai`、`docs/design/02`） |
| 戰場 AI 的攻守不對稱 | 完成 | L1 | base | `TestDefenceIsWorthSomething`、`TestAutoUsesTheWholeRepertoire`、`TestAutoAcrossManyBattles` | 打滿三十天而城池未被奪就算守方衛郡成功（手冊 p.35），所以兩邊的最佳策略本來就不同。先前 remake 攻守走同一套，而且**守方會追出去**——那會把守軍一個一個引離城池。  \| \| 順序 \| \|---\|---\| \| 攻方 \| 近戰 → 推進 → 射箭 → 休息 \| \| 守方 \| 射箭 → 近戰（只在佔上風時）→ 守城 → 休息 \|  ⚠ **射箭不能排在推進前面**：射得到的距離是 2，正好是「再走一步就貼上」的距離，排前面的話攻方停在兩格外把箭射完。量到過——單挑一次都沒出現。  效果（各 126 場）：等量兵力時攻方勝率 55% → **26%**，而 1.5 倍兵力仍贏 86%。（`docs/design/02`、`docs/mechanics/40`） |
| 戰場上部隊每天怎麼選動作（九支與對戰子畫面） | 完成 | L1 | base | `TestZZUnitAIDayParity`、`TestZZUnitAIDayParityPlus` | 九支的角色、條件與骰序（`docs/re/05` §12.1–12.2，Issue #22／#24）、加強版的改動（§12.5，#28）與選項 7 進的對戰子畫面（§10，#30：版型、起點、每一時刻的判斷式與六處骰、尋路、攻擊、俘虜）都對回原版；`SAN1_NORESYNC=1 TestZZUnitAIDayParity` 五張盤面整場逐條相同（丁含一整場對戰）。2026-09-16 補齊：加強版的對戰子畫面（§10.9，兩版指令流只差五處）與單挑常式的整串骰（§9.0，接受判定後兩道比挑戰者的兵、一回合一擊輪流打、對白與特效各一擲）——`TestZZUnitAIDayParity` 六張（己：兩場單挑）、`TestZZUnitAIDayParityPlus` 七張（庚：一場單挑）不重拍逐條相同（Issue #37／#38）。（`docs/re/05`、`docs/mechanics/40`、`docs/design/02`） |
| 戰場上的被擒處置與中途紮寨（玩家當場四選一） | 完成 | L1 | base | `TestPlayerCaptiveAsksAgainAfterARefusal`、`TestPlayerReleaseRollsTheDestinationFirst`、`TestPlayerJailNeedsIdleRoom`、`TestPlayerCampAsksUntilTheCellFits` | 玩家親自指揮時抓到人當場出「1.斬首 2.囚禁 3.釋放 4.招降」，拒絕就再問，招降進空部隊當場紮寨（`docs/spec/018`）。直接呼叫原版 `0x259fe` 走六種鍵序，擲骰序列、人物三格、去處主事者、紮寨位置全部相同。順帶對回：囚禁是身分 10；釋放回自己勢力的郡、所屬不變。  ⚠ 不是玩家指揮的戰役裡玩家那一方的捕獲仍不處置；電腦的中途紮寨 `0x2020:0x293a` 沒讀（`docs/spec/018` §7）。（`docs/spec/018`、`docs/mechanics/40`、`docs/re/05`） |
| 聯合出兵打過來時玩家自己挑助守軍那一郡（0x2dd14），可以不求援 | 完成 | L1 | base | `TestJointAttackStopsToAskThePlayer`、`TestAnswerDefenderAidLaunches` | 候選是守方郡的同勢力鄰郡；守方的諸侯 offset 0 == 1（玩家）才開挑郡清單（`0x2dd14`，提示 `DS:0x8588`），取消印「不聯合守方」就是不求援。電腦那一條仍取掃到的最後一個（`0x2dd24`）。月流程停在那一格（`PendingAid`），`session.FinishAid` 收尾；順帶補上聯合出兵那一條先前不看「守城」開關、一律自動打完的洞。沒有介面的路照電腦那一條。`TestJointAttackStopsToAskThePlayer`（含守方是電腦的反向對照）、`TestAnswerDefenderAidLaunches`（求援／不求援／不在候選裡被擋）、`TestDefenderAidAsksThePlayer`。盤面自己擺——劇本一的劉備只有一個郡，等它湊出來的話測試會永遠 skip。（`docs/mechanics/30`） |
| 強化 AI 會經營收入了（秋收公式的四個槓桿） | 完成 | L1 | base | `TestZZEnhancedCommandMix`、`TestEnhancedNeverGivesAwayAPrefecture` | 先前的強化 AI **只會把錢變成兵，沒有人在賺錢**：開墾排在優先序最後一條，而徵兵幾乎永遠有空額可補，於是開墾從來輪不到——三十六個月跑下來平均地力只剩 7。  收入那一段的槓桿全部來自秋收公式（`game.HarvestGold`／`HarvestRice`，`L0`）：地力金 ×4 米 ×3、民忠 ×2、洪水率只進米那一半、太守魅力 ×1 但**改一次就長期生效**。所以順序是「指定太守 → 開墾 → 防洪 → 賑民」，整段排在**「兵力補到上限」前面**。  改動前後（`TestZZAIOrdersSweep`，劇本 001 全電腦三十六個月）：  \| 每郡每月 \| 總兵 \| 總金 \| 平均地力 \| \|---\|---\|---\|---\| \| 1 道（改前）\| 134,799 \| 14,577 \| 7 \| \| **1 道（改後）**\| 110,377 \| **47,260** \| **21** \| \| 2 道（改前）\| 143,339 \| 22,432 \| 10 \| \| **2 道（改後）**\| **143,265** \| 31,413 \| **35** \| \| 5 道（改前）\| 148,656 \| 31,593 \| 13 \| \| **5 道（改後）**\| **155,401** \| 40,060 \| **35** \|  一道令那一格兵變少了——那一格的錢還沒轉成兵，三十六個月只夠看到地力與金三倍。更硬的證據是整局跑得完：預設改成一道令之後 `TestGameReachesAConclusion` 一度九十年還剩三個勢力，接上收入這一段之後回到**九十年內一統**。  ⚠ 「指定太守」開局那一輪是 0 次——**那不是沒接上**：劇本出貨的太守本來就已經是郡裡魅力最高的那一位（24 個有主的郡：14 個君主親臨、10 個現任就是最高）。跑三十六個月量得到 5 次（`TestZZEnhancedCommandMix`）。  ⚠ 「指定太守」的候選**只收自己勢力的人**，那一道是強化 AI 自己加的。原版的名單本來就限制在同一個郡（`buildRoster` 模式 2：所在郡相同 ＋ 身分 0–3），只是不比對勢力——郡的所屬每回合由駐軍重算、混編是表得出來的盤面，主事者換人郡跟著改所屬（`0xd74d`）是同一套規則的一部分。強化 AI 不想讓郡易主，所以多擋一層（`TestEnhancedNeverGivesAwayAPrefecture`）；**這是它與還原版分岔的地方，不是在修正原版**。（`docs/design/02`、`docs/mechanics/70-ai`） |
| 強化 AI 會留兵防守了 | 完成 | L1 | base | `TestZZGarrisonRatioSweep`、`TestZZAIModesMoveTheBoard` | 先前的強化 AI **只會擴張不會守**：沒有足夠的兵力防守，再多的土地與金子都是白搭——不設防那一端三十六個月打下 33 個郡，而總兵只剩 39,983（`base` 是 166,250）。  三處改動： ① **守不住就先補兵**（`garrisonNeed`：鄰郡最強敵人的守備戰力 × 60%），排在出兵前面； ② **出兵要留得住家**：從最強的開始編隊，但留下的戰力不能低於門檻（先前是「除了一位全部帶走」）； ③ 一個郡一個月下幾道令由玩家決定（「其他 → 電腦指令」，1–5，預設 1）——電腦本來就不受那條限制（`docs/mechanics/70-ai` §2.12）。  門檻 60% 是掃出來的：不設防與 80% 以上兩端都是壞的（前者把世界打空、後者曲線就平了），60% 落在「還打得動」與「守得住」之間。完整曲線在 `docs/design/02` §3。（`docs/design/02`） |
| 君主→賞賜物品的兩層迴圈：那一郡 → 那一位 → 物品 → 再問那一位；玩家那一條的效果（0x1cfd6／0x1d118） | 完成 | L1 | base | present：internal/game/gift.go 的「func (g *State) CloseGift」 | `TestZZGiftLoopMatchesTheOriginal` 逐問提示（含已賞賜過了、取消、收掉後回合結束）與三張表逐位元組相同；謀略 91 收兵書忠誠 20→77，看截斷前的能力。清單排序鍵歸 #77。（`docs/spec/005`、`docs/mechanics/20`） |
| 指定太守會把郡的所屬改成新太守的勢力 | 完成 | L0 | base | 指令重數 | `0xd74d`：`州郡 offset 30 ← 新主事者的勢力`。名單不比對勢力，所以混編的郡指了別家的人當太守就當場易主，**同一個回合裡剩下的表全部換一個諸侯記錄**（原版每一支都是現讀 offset 30）。  ⚠ 這一行 2026-09-09 就寫在 `70-ai` §2.6 的虛擬碼裡，只是 remake 沒實作；2026-09-10 追月度對拍時繞了四輪才回頭看自己的文件（`CLAUDE.md` §7 第 1 條）。**文件寫了不等於程式做了**——這就是每條要掛 verify 的理由。（`docs/mechanics/70-ai`） |
| 玩家的君主死掉由玩家自己挑繼承人（0x14f7c），不能取消 | 完成 | L1 | base | present：internal/game/succession.go 的「func (g *State) AssignHeir」 | `TestZZHeirMatchesTheOriginal`：玩家的君主老死，那一份清單與原版相同，挑第三位（不是預設的排頭）之後三張表逐位元組相同。候選是整個勢力、照原版的交換排序排（不是穩定排序）；規則層先扶排頭上去再讓玩家覆蓋，人望用繼承之前的值重算。原版「死亡對白 → 問 → 繼承對白」的順序靠佇列空了才問達成，同月還有別人死掉時那幾則會插在這一問之前。（`docs/spec/014`、`docs/mechanics/80`） |
| 主事者被調走之後由玩家挑新任太守（0x1d6ed），不能取消 | 完成 | L1 | base | present：internal/game/state.go 的「func (g *State) AssignGovernor」 | `TestZZNewGovernorMatchesTheOriginal`：清單那一格相同，挑第三位（不是預設）之後三張表逐位元組相同；挑預設那一位的反向對照差 3 個位元組。規則層先填預設值再讓玩家覆蓋，所以電腦與無畫面的路不變。戰役收尾、太守被挖角那幾條時機還沒各自對拍。（`docs/spec/014`） |
| 沒有介面時玩家那一方的捕獲照電腦的判斷式處置（不再留 FateNone） | 完成 | L1 | base | `TestNoInterfaceCaptiveCounterFires`、`TestCaptiveDiceForComputerCaptor` | 原版不管那一場由誰指揮，玩家那一方抓到人都當場出四選一（`0x259fe`）。remake 在沒有人可以答的路上（示範模式、月度對拍、批次跑、「守城」關著）先前留 `FateNone`——被抓了卻什麼都沒發生，原版沒有這個狀態，三張表會不一樣。改走電腦那一套。⚠ **這是差異不是還原**：會多擲一次 `RND(10)`，原版在那一刻問人不擲。**先量再改**：六張逐日對拍盤面與三十六個月的無畫面局一次都沒走到這一條，所以既有對拍的骰序不受影響；0 有意義是因為計數器有正對照（`TestNoInterfaceCaptiveCounterFires`）。（`docs/spec/018`） |
| 「其他 → 守城」：電腦來攻時玩家親自指揮守方（預設自動） | 完成 | L1 | both | present：internal/game/aiattack.go 的「func (g *State) FinishDefence」 | 原版被打時由玩家自己編隊、佈陣、指揮（說明書 p.26–28，`docs/reference/01` §B）；remake 做成開關，預設自動打完——無畫面的路（月度對拍、示範模式、批次跑）沒有人下令，預設親自守會停在戰場上等一個不會來的鍵。開了之後 `ComputerAttack` 整編完把戰役交給 `PendingDefence`，月流程停在那一格，`cmd/san1` 的 `startDefence` 接指揮權，`FinishDefence` 收尾並推游標。規則層不因開關而不同。「其他」因此從十項變十一項：第十一項的鍵改用字母 `A`，第十項的名稱縮成「AI指令」（下面板 4 列 × 24 格排不下）。驗證：`TestPlayerDefenceHandsOverTheBattle`、`TestLoadKeepsPlayerDefence`；關著那一邊的骰序由 `TestZZMonthParity`／`TestZZUnitAIDayParity` 守著。（`docs/spec/014`、`docs/design/02`） |
| 軍團整編逐位問「分配那一位將軍」「分到那一軍」（0x21457／0x21509） | 完成 | L1 | base | present：internal/battle/battle.go 的「func (b *Battle) Reform」 | 原版的整編**同時決定誰出征與分到哪一軍**；先前 remake 是「留一位在家、其餘全部出征」再按戰力輪流分配。現在照原版逐位問，沒分配到的留在家裡；主守軍必須派出所有兵力，所以守方收工時補齊。軍號 1–5 ＝ 中軍／先鋒／左軍／右軍／後軍，證據是原版自己印的軍名（`docs/re/05` §5.2）。⚠ 部隊記錄的索引是**分配順序**不是軍別，比對只能比「誰跟誰同一隊」。沒有介面的那一方（月度對拍、示範模式、批次跑）照電腦的整編填，那是登記在案的差異。`TestZZPlayerSortieDriven`：同一串按鍵之後分組與原版相同（[[13 29] [30]]），三位各自一軍的反向對照會紅。（`docs/re/05`、`docs/mechanics/40`、`docs/spec/014`） |
| 玩家人數 0–16：逐位選君主、照郡順序輪流下令、示範模式（0x12072／0x15763） | 完成 | L1 | base | present：internal/session/session.go 的「func (s *Session) AdvanceToHuman」 | 2 人／0 人開局的表只差開月物價、`es:0x3120` 序號表相同；人數與「第2位」兩格畫面框外逐像素相同；兩位玩家第一個月的停點 [郡 11, 郡 8] remake 逐一相同。整月位元組、玩家打玩家的守方、示範模式鏡頭未做（spec/019 §5）。（`docs/spec/019`、`docs/mechanics/10`、`docs/spec/005`） |
| 計謀那一擲 RND(4)（場景圖拉幕）：遠交與驅虎判定前、離間與策反得手後 | 完成 | L1 | base | 指令重數 | `0x32e4f` 的 `RND(4)` 是拉幕 `0x32e40` 挑方向那一擲，由場景圖特效 `0x32dfa` 進去（`docs/spec/010` §1.1）。遠交近攻與驅虎吞狼在玩家的選單常式裡、真正的判定之前擲，不看成敗；離間君臣與策反人民在得手之後的效果常式末尾擲（`0x2d1fa`／`0x2d6e0`），沒得手不擲。  月度對拍：電腦策反人民沒得手的那個月，計略那一段原版抽 34 次、沒有 `RND(4)`；改成判定前一律擲時 remake 多一次、郡 3 的金米差 4 個位元組。  ⚠ 電腦的遠交與驅虎走哪一條路沒有量過（`L2`）。（`docs/mechanics/70-ai`、`docs/spec/010`） |
| 退兵問玩家「逃向那一郡」、電腦照判斷式，兩邊都把去處記下來（0x23dd4） | 完成 | L0 | base | `TestPlayerRetreatAsksWhichPrefecture`、`TestPlayerRetreatCancelKeepsTheUnitOnTheField`、`TestNoEscapeIsNotTheSameAsSurrounded`、`TestComputerRetreatIgnoresThePlayerHook` | 按 8 退兵時原版問玩家「%s逃向那一郡」（`0x2408e`，收郡編號 `0x115e(1,42)`），電腦照判斷式（主守軍以外、出兵郡現役＋本隊將領 ≤ 50 就回出兵郡，否則 `RND(鄰郡數)`）。remake 先前玩家那一邊連問都沒問、去處也沒人讀。現在玩家那一邊出候選清單，空 Enter（清單的 `0`）取消整個退兵、那一支留在戰場（`0x2416a`）。**去處會寫入生還者的所在郡**：原版 `0x23dd4` 在 `0x242fb` 呼叫 `0x24318`，`0x245d0` 寫人物 offset 19；若目的郡無主，`0x245f5` 亦寫所屬與主事者。舊斷言只檢查上層常式的直接寫入，漏了下層呼叫；修正依 `docs/re/05` §14（`L0`、`[base]`）。「無郡可逃」（鄰郡的問題，`0x23f03`）與「被完全包圍」（格子的問題）兩條訊息分開。remake 差異：原版收郡編號，戰場面板只讀單鍵，所以列成清單按序號選（`docs/spec/014` §7.2）；沒有介面的路取候選第一個、不擲，理由與 #64／#98／#99／#100 同一個。（`docs/re/05`、`docs/mechanics/40`、`docs/spec/014`） |
| 人事→賞賜金帛是一道命令賞好幾位的迴圈；玩家增幅 魅力 ÷ max(謀略, 戰力) × 金 × 0.5 | 完成 | L1 | base | `TestZZRewardLoopMatchesTheOriginal` | `TestZZRewardLoopMatchesTheOriginal`：賞 A 50、擋重複、賞 B 30、收掉，三張表在結束回合那一刻逐位元組相同；公式從 `0x1c458` 讀出（R85）。軍師評價在原版是命令一開始，remake 也在開頭走 `withAdvice`；對白、場景圖沒有逐格比。（`docs/mechanics/20`、`docs/spec/014`、`docs/playtest/04`） |
| 把 es:[0x58c] 當成跨表共用、會被就地重排的清單 | 完成 | L1 | base | 指令重數 | 十八張表共用同一份守將清單（`es:0x58c`／長度 `es:0xc`），表自己不建清單、只就地重排或截短它。整輪的順序演變逐格對過四把鍵（含兩組平手），寫在 `docs/mechanics/70-ai` §2.5b。  **量到會咬人的一格已經修好**：郡 13 的指定太守。指定軍師排在它前面而且會對調兩個人的身分，146 與 65 魅力都是 90——原版用行動者排完留下的順序拿到 146。remake 是 `faithful.order`。  ⚠ 其餘幾段的重排（指定太守、四支賞賜、調整兵力）**目前沒有量到造成差異**，所以沒有模擬。要再動之前先量到一格真的因此不同（`CONTEXT.md` R51 教訓二）。（`docs/mechanics/70-ai`） |
| 電腦諸侯的移防：整份名單、金米夾值、第二次編隊接第一次 | 完成 | L0 | base | absent：docs/spec/007-ai-relocation.md 的「^狀態：`READY`」 | `docs/spec/007`（`READY`）。三條 `L0`：  1. `0x1938a` 的迴圈把**整份出征名單**的人物 offset 19 都寫成目標郡（remake 原本只搬 `force[0]`）。 2. 來源郡的金米減完夾 0，目標郡加完在 16 位元變負時設成 30000。 3. 沒有「主事者要有人接手」這道閘門——整郡搬空是允許的，歸屬由 `0x1e394` 從人物表重算。玩家的「調動軍隊」（`0x18ce1`）是另一支碼，**不合併**。  同一輪修掉的還有編隊：`0xb2b4` 收尾把清單截短並寫回 `es:0xc`，`0xb706` 洗的是那一份截短的。remake 原本兩次都重新拿整份守軍，郡 6 因此多抽一次亂數（原版 7／remake 8），而它是視窗裡的第一格。（`docs/spec/007`、`docs/mechanics/70-ai`、`docs/re/05`） |
| 發動戰役／調動軍隊／運送錢糧的「從那一郡」收任何自己的郡；回合記在下令的郡 | 完成 | L1 | base | present：cmd/san1/main.go 的「func (a *app) askSource」 | `TestZZMoveFromAnotherPrefectureMatchesTheOriginal` 來源清單與字色相同、從別的郡搬完三張表逐位元組相同、結束回合一次且月份不變。量到原版沒有「這個月下過令」的旗標（`0x1746e`），月順序是洗過的（`0x1740a`），所以來源郡自己的回合還在。運送與發動戰役共用 `askSource`，那兩條沒有各自的對拍。（`docs/spec/014`、`docs/mechanics/40`） |

### 介面

| 項目 | 狀態 | 等級 | 版本 | 核實訊號 | 說明 |
|---|---|---|---|---|---|
| AI 版本與電腦指令數可以在遊戲裡調 | 完成 | — | both | `TestLoadKeepsTheAIChosenInGame`、`TestSetBrainLeavesATrace` | 「其他」加了第九項「電腦AI」與第十項「電腦指令」（原版只有八項，`docs/re/04` §3）。**加在後面**，原版那八項留在原來的編號上。  三個坑： ① **第十項的按鍵是 `0` 不是 `:`**——鍵盤只送得進 `0`–`9`，`byte('1'+i)` 到第十項會算出 `:`，那一項會永遠按不動而畫面上完全正常（`ui.MenuKey`）； ② 切 AI 只在**跑得動這一版規則**的版本裡繞（`ai.ModesFor`／`NextMode`）——還原型的 AI 配另一版的規則會安靜地算錯； ③ 兩項都進存檔（`optMeta` 的 `ai_mode`／`ai_orders`，都 `omitempty`），**讀檔時存檔裡有就聽存檔的**，否則玩家換過 AI 再讀檔會看起來像設定沒存到。（`docs/design/02`） |

<!-- worklist:end -->
