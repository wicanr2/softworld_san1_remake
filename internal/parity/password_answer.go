//go:build oracle

package parity

// 防拷密碼。
//
// 載入舊進度進得了主畫面，但第一道帶 ＊ 的指令（使用後轉移控制權，
// 說明書 p.42）用掉之後密碼關就跳出來：兩張人物圖 ＋ 一個地支 ＋
// 四個 `?`，提示 `請輸入密碼(-寅-)`（`docs/re/02` §3.3）。
//
// **這不是那 1728 組表**，是這一局的那一組。密碼由三個座標決定
//（首圖 × 橫圖 × 地支），而 dosgolem 是決定性的：同一段開機序列
// 每次都拿到同一組座標，所以答案固定，對拍每次都能重用。
//
// 怎麼問出來的：主畫面拍一張快照，觸發密碼關之後看哪些位址變了，
// 從裡面挑「值是四位數」的候選逐個試——原版自己得先算出答案才畫得出
// 那個畫面，所以答案一定在那批變動裡。
const passwordAnswer = "4029"
